// Code generated by smithy-go-codegen DO NOT EDIT.

package bcmdashboards

import (
	"context"
	"fmt"
	"github.com/aws/aws-sdk-go-v2/service/bcmdashboards/types"
	"github.com/aws/smithy-go/middleware"
	smithyhttp "github.com/aws/smithy-go/transport/http"
)

// Returns a list of scheduled reports in your account.
func (c *Client) ListScheduledReports(ctx context.Context, params *ListScheduledReportsInput, optFns ...func(*Options)) (*ListScheduledReportsOutput, error) {
	if params == nil {
		params = &ListScheduledReportsInput{}
	}

	result, metadata, err := c.invokeOperation(ctx, "ListScheduledReports", params, optFns, c.addOperationListScheduledReportsMiddlewares)
	if err != nil {
		return nil, err
	}

	out := result.(*ListScheduledReportsOutput)
	out.ResultMetadata = metadata
	return out, nil
}

type ListScheduledReportsInput struct {

	// The maximum number of results to return in a single call. Valid range is 1 to
	// 100. The default value is 50.
	MaxResults *int32

	// The token for the next page of results. Use the value returned in the previous
	// response.
	NextToken *string

	noSmithyDocumentSerde
}

type ListScheduledReportsOutput struct {

	// An array of scheduled report summaries, containing basic information about each
	// scheduled report.
	//
	// This member is required.
	ScheduledReports []types.ScheduledReportSummary

	// The token to use to retrieve the next page of results. Not returned if there
	// are no more results to retrieve.
	NextToken *string

	// Metadata pertaining to the operation's result.
	ResultMetadata middleware.Metadata

	noSmithyDocumentSerde
}

func (c *Client) addOperationListScheduledReportsMiddlewares(stack *middleware.Stack, options Options) (err error) {
	err = stack.Serialize.Add(&awsAwsjson10_serializeOpListScheduledReports{}, middleware.After)
	if err != nil {
		return err
	}
	err = stack.Deserialize.Add(&awsAwsjson10_deserializeOpListScheduledReports{}, middleware.After)
	if err != nil {
		return err
	}

	if err = addlegacyEndpointContextSetter(stack, options); err != nil {
		return err
	}
	if err = addComputeContentLength(stack); err != nil {
		return err
	}
	if err = addResolveEndpointMiddleware(stack, options); err != nil {
		return err
	}
	if err = addComputePayloadSHA256(stack); err != nil {
		return err
	}
	if err = addRecordResponseTiming(stack); err != nil {
		return err
	}
	if err = smithyhttp.AddErrorCloseResponseBodyMiddleware(stack); err != nil {
		return err
	}
	if err = smithyhttp.AddCloseResponseBodyMiddleware(stack); err != nil {
		return err
	}
	if err = addCredentialSource(stack, options); err != nil {
		return err
	}
	if err = stack.Initialize.Add(newServiceMetadataMiddleware(options.Region, "ListScheduledReports"), middleware.Before); err != nil {
		return err
	}
	if err = addRequestIDRetrieverMiddleware(stack); err != nil {
		return err
	}
	if err = addResponseErrorMiddleware(stack); err != nil {
		return err
	}
	if err = addRequestResponseLogging(stack, options); err != nil {
		return err
	}
	if err = addDisableHTTPSMiddleware(stack, options); err != nil {
		return err
	}
	if err = addInterceptors(stack, options); err != nil {
		return err
	}
	return nil
}

// ListScheduledReportsPaginatorOptions is the paginator options for
// ListScheduledReports
type ListScheduledReportsPaginatorOptions struct {
	// The maximum number of results to return in a single call. Valid range is 1 to
	// 100. The default value is 50.
	Limit int32

	// Set to true if pagination should stop if the service returns a pagination token
	// that matches the most recent token provided to the service.
	StopOnDuplicateToken bool
}

// ListScheduledReportsPaginator is a paginator for ListScheduledReports
type ListScheduledReportsPaginator struct {
	options   ListScheduledReportsPaginatorOptions
	client    ListScheduledReportsAPIClient
	params    *ListScheduledReportsInput
	nextToken *string
	firstPage bool
}

// NewListScheduledReportsPaginator returns a new ListScheduledReportsPaginator
func NewListScheduledReportsPaginator(client ListScheduledReportsAPIClient, params *ListScheduledReportsInput, optFns ...func(*ListScheduledReportsPaginatorOptions)) *ListScheduledReportsPaginator {
	if params == nil {
		params = &ListScheduledReportsInput{}
	}

	options := ListScheduledReportsPaginatorOptions{}
	if params.MaxResults != nil {
		options.Limit = *params.MaxResults
	}

	for _, fn := range optFns {
		fn(&options)
	}

	return &ListScheduledReportsPaginator{
		options:   options,
		client:    client,
		params:    params,
		firstPage: true,
		nextToken: params.NextToken,
	}
}

// HasMorePages returns a boolean indicating whether more pages are available
func (p *ListScheduledReportsPaginator) HasMorePages() bool {
	return p.firstPage || (p.nextToken != nil && len(*p.nextToken) != 0)
}

// NextPage retrieves the next ListScheduledReports page.
func (p *ListScheduledReportsPaginator) NextPage(ctx context.Context, optFns ...func(*Options)) (*ListScheduledReportsOutput, error) {
	if !p.HasMorePages() {
		return nil, fmt.Errorf("no more pages available")
	}

	params := *p.params
	params.NextToken = p.nextToken

	var limit *int32
	if p.options.Limit > 0 {
		limit = &p.options.Limit
	}
	params.MaxResults = limit

	optFns = append([]func(*Options){
		addIsPaginatorUserAgent,
	}, optFns...)
	result, err := p.client.ListScheduledReports(ctx, &params, optFns...)
	if err != nil {
		return nil, err
	}
	p.firstPage = false

	prevToken := p.nextToken
	p.nextToken = result.NextToken

	if p.options.StopOnDuplicateToken &&
		prevToken != nil &&
		p.nextToken != nil &&
		*prevToken == *p.nextToken {
		p.nextToken = nil
	}

	return result, nil
}

// ListScheduledReportsAPIClient is a client that implements the
// ListScheduledReports operation.
type ListScheduledReportsAPIClient interface {
	ListScheduledReports(context.Context, *ListScheduledReportsInput, ...func(*Options)) (*ListScheduledReportsOutput, error)
}

var _ ListScheduledReportsAPIClient = (*Client)(nil)
