// Code generated by smithy-go-codegen DO NOT EDIT.

package outposts

import (
	"context"
	"fmt"
	"github.com/aws/aws-sdk-go-v2/service/outposts/types"
	"github.com/aws/smithy-go/middleware"
	smithyhttp "github.com/aws/smithy-go/transport/http"
)

// Lists the quotes for your Amazon Web Services account.
func (c *Client) ListQuotes(ctx context.Context, params *ListQuotesInput, optFns ...func(*Options)) (*ListQuotesOutput, error) {
	if params == nil {
		params = &ListQuotesInput{}
	}

	result, metadata, err := c.invokeOperation(ctx, "ListQuotes", params, optFns, c.addOperationListQuotesMiddlewares)
	if err != nil {
		return nil, err
	}

	out := result.(*ListQuotesOutput)
	out.ResultMetadata = metadata
	return out, nil
}

type ListQuotesInput struct {

	// The maximum page size.
	MaxResults *int32

	// The pagination token.
	NextToken *string

	noSmithyDocumentSerde
}

type ListQuotesOutput struct {

	// The pagination token.
	NextToken *string

	// Information about the quotes.
	Quotes []types.QuoteSummary

	// Metadata pertaining to the operation's result.
	ResultMetadata middleware.Metadata

	noSmithyDocumentSerde
}

func (c *Client) addOperationListQuotesMiddlewares(stack *middleware.Stack, options Options) (err error) {
	err = stack.Serialize.Add(&awsRestjson1_serializeOpListQuotes{}, middleware.After)
	if err != nil {
		return err
	}
	err = stack.Deserialize.Add(&awsRestjson1_deserializeOpListQuotes{}, middleware.After)
	if err != nil {
		return err
	}

	if err = addlegacyEndpointContextSetter(stack, options); err != nil {
		return err
	}
	if err = addComputeContentLength(stack); err != nil {
		return err
	}
	if err = addResolveEndpointMiddleware(stack, options); err != nil {
		return err
	}
	if err = addComputePayloadSHA256(stack); err != nil {
		return err
	}
	if err = addRecordResponseTiming(stack, options); err != nil {
		return err
	}
	if err = smithyhttp.AddErrorCloseResponseBodyMiddleware(stack); err != nil {
		return err
	}
	if err = smithyhttp.AddCloseResponseBodyMiddleware(stack); err != nil {
		return err
	}
	if err = addCredentialSource(stack, options); err != nil {
		return err
	}
	if err = stack.Initialize.Add(newServiceMetadataMiddleware(options.Region, "ListQuotes"), middleware.Before); err != nil {
		return err
	}
	if err = addRequestIDRetrieverMiddleware(stack); err != nil {
		return err
	}
	if err = addResponseErrorMiddleware(stack); err != nil {
		return err
	}
	if err = addRequestResponseLogging(stack, options); err != nil {
		return err
	}
	if err = addDisableHTTPSMiddleware(stack, options); err != nil {
		return err
	}
	if err = addInterceptors(stack, options); err != nil {
		return err
	}
	return nil
}

// ListQuotesPaginatorOptions is the paginator options for ListQuotes
type ListQuotesPaginatorOptions struct {
	// The maximum page size.
	Limit int32

	// Set to true if pagination should stop if the service returns a pagination token
	// that matches the most recent token provided to the service.
	StopOnDuplicateToken bool
}

// ListQuotesPaginator is a paginator for ListQuotes
type ListQuotesPaginator struct {
	options   ListQuotesPaginatorOptions
	client    ListQuotesAPIClient
	params    *ListQuotesInput
	nextToken *string
	firstPage bool
}

// NewListQuotesPaginator returns a new ListQuotesPaginator
func NewListQuotesPaginator(client ListQuotesAPIClient, params *ListQuotesInput, optFns ...func(*ListQuotesPaginatorOptions)) *ListQuotesPaginator {
	if params == nil {
		params = &ListQuotesInput{}
	}

	options := ListQuotesPaginatorOptions{}
	if params.MaxResults != nil {
		options.Limit = *params.MaxResults
	}

	for _, fn := range optFns {
		fn(&options)
	}

	return &ListQuotesPaginator{
		options:   options,
		client:    client,
		params:    params,
		firstPage: true,
		nextToken: params.NextToken,
	}
}

// HasMorePages returns a boolean indicating whether more pages are available
func (p *ListQuotesPaginator) HasMorePages() bool {
	return p.firstPage || (p.nextToken != nil && len(*p.nextToken) != 0)
}

// NextPage retrieves the next ListQuotes page.
func (p *ListQuotesPaginator) NextPage(ctx context.Context, optFns ...func(*Options)) (*ListQuotesOutput, error) {
	if !p.HasMorePages() {
		return nil, fmt.Errorf("no more pages available")
	}

	params := *p.params
	params.NextToken = p.nextToken

	var limit *int32
	if p.options.Limit > 0 {
		limit = &p.options.Limit
	}
	params.MaxResults = limit

	optFns = append([]func(*Options){
		addIsPaginatorUserAgent,
	}, optFns...)
	result, err := p.client.ListQuotes(ctx, &params, optFns...)
	if err != nil {
		return nil, err
	}
	p.firstPage = false

	prevToken := p.nextToken
	p.nextToken = result.NextToken

	if p.options.StopOnDuplicateToken &&
		prevToken != nil &&
		p.nextToken != nil &&
		*prevToken == *p.nextToken {
		p.nextToken = nil
	}

	return result, nil
}

// ListQuotesAPIClient is a client that implements the ListQuotes operation.
type ListQuotesAPIClient interface {
	ListQuotes(context.Context, *ListQuotesInput, ...func(*Options)) (*ListQuotesOutput, error)
}

var _ ListQuotesAPIClient = (*Client)(nil)
