// Code generated by smithy-go-codegen DO NOT EDIT.

package bedrockagentcorecontrol

import (
	"context"
	"fmt"
	"github.com/aws/aws-sdk-go-v2/service/bedrockagentcorecontrol/types"
	"github.com/aws/smithy-go/middleware"
)

// Lists all of the consent portals in your account.
func (c *Client) ListConsentPortals(ctx context.Context, params *ListConsentPortalsInput, optFns ...func(*Options)) (*ListConsentPortalsOutput, error) {
	if params == nil {
		params = &ListConsentPortalsInput{}
	}

	result, metadata, err := c.invokeOperation(ctx, "ListConsentPortals", params, optFns, c.addOperationListConsentPortalsMiddlewares)
	if err != nil {
		return nil, err
	}

	out := result.(*ListConsentPortalsOutput)
	out.ResultMetadata = metadata
	return out, nil
}

type ListConsentPortalsInput struct {

	// The maximum number of consent portals to return in a single call.
	MaxResults *int32

	// A token to retrieve the next page of results. Use the value returned in a
	// previous response to request the next page.
	NextToken *string

	noSmithyDocumentSerde
}

type ListConsentPortalsOutput struct {

	// The list of consent portals.
	//
	// This member is required.
	ConsentPortals []types.ConsentPortalSummary

	// The token to use in a subsequent request to retrieve the next page of results.
	// This value is null when there are no more results to return.
	NextToken *string

	// Metadata pertaining to the operation's result.
	ResultMetadata middleware.Metadata

	noSmithyDocumentSerde
}

func (c *Client) addOperationListConsentPortalsMiddlewares(stack *middleware.Stack, options Options) (err error) {
	err = stack.Serialize.Add(&awsRestjson1_serializeOpListConsentPortals{}, middleware.After)
	if err != nil {
		return err
	}
	err = stack.Deserialize.Add(&awsRestjson1_deserializeOpListConsentPortals{}, middleware.After)
	if err != nil {
		return err
	}

	if err = addResolveEndpointMiddleware(stack, options); err != nil {
		return err
	}
	if err = addComputePayloadSHA256(stack); err != nil {
		return err
	}
	if err = addRecordResponseTiming(stack, options); err != nil {
		return err
	}
	if err = addCredentialSource(stack, options); err != nil {
		return err
	}
	if err = addRequestIDRetrieverMiddleware(stack); err != nil {
		return err
	}
	if err = addResponseErrorMiddleware(stack); err != nil {
		return err
	}
	if err = addRequestResponseLogging(stack, options); err != nil {
		return err
	}
	if err = addDisableHTTPSMiddleware(stack, options); err != nil {
		return err
	}
	if err = addInterceptors(stack, options); err != nil {
		return err
	}
	return nil
}

// ListConsentPortalsPaginatorOptions is the paginator options for
// ListConsentPortals
type ListConsentPortalsPaginatorOptions struct {
	// The maximum number of consent portals to return in a single call.
	Limit int32

	// Set to true if pagination should stop if the service returns a pagination token
	// that matches the most recent token provided to the service.
	StopOnDuplicateToken bool
}

// ListConsentPortalsPaginator is a paginator for ListConsentPortals
type ListConsentPortalsPaginator struct {
	options   ListConsentPortalsPaginatorOptions
	client    ListConsentPortalsAPIClient
	params    *ListConsentPortalsInput
	nextToken *string
	firstPage bool
}

// NewListConsentPortalsPaginator returns a new ListConsentPortalsPaginator
func NewListConsentPortalsPaginator(client ListConsentPortalsAPIClient, params *ListConsentPortalsInput, optFns ...func(*ListConsentPortalsPaginatorOptions)) *ListConsentPortalsPaginator {
	if params == nil {
		params = &ListConsentPortalsInput{}
	}

	options := ListConsentPortalsPaginatorOptions{}
	if params.MaxResults != nil {
		options.Limit = *params.MaxResults
	}

	for _, fn := range optFns {
		fn(&options)
	}

	return &ListConsentPortalsPaginator{
		options:   options,
		client:    client,
		params:    params,
		firstPage: true,
		nextToken: params.NextToken,
	}
}

// HasMorePages returns a boolean indicating whether more pages are available
func (p *ListConsentPortalsPaginator) HasMorePages() bool {
	return p.firstPage || (p.nextToken != nil && len(*p.nextToken) != 0)
}

// NextPage retrieves the next ListConsentPortals page.
func (p *ListConsentPortalsPaginator) NextPage(ctx context.Context, optFns ...func(*Options)) (*ListConsentPortalsOutput, error) {
	if !p.HasMorePages() {
		return nil, fmt.Errorf("no more pages available")
	}

	params := *p.params
	params.NextToken = p.nextToken

	var limit *int32
	if p.options.Limit > 0 {
		limit = &p.options.Limit
	}
	params.MaxResults = limit

	optFns = append([]func(*Options){
		addIsPaginatorUserAgent,
	}, optFns...)
	result, err := p.client.ListConsentPortals(ctx, &params, optFns...)
	if err != nil {
		return nil, err
	}
	p.firstPage = false

	prevToken := p.nextToken
	p.nextToken = result.NextToken

	if p.options.StopOnDuplicateToken &&
		prevToken != nil &&
		p.nextToken != nil &&
		*prevToken == *p.nextToken {
		p.nextToken = nil
	}

	return result, nil
}

// ListConsentPortalsAPIClient is a client that implements the ListConsentPortals
// operation.
type ListConsentPortalsAPIClient interface {
	ListConsentPortals(context.Context, *ListConsentPortalsInput, ...func(*Options)) (*ListConsentPortalsOutput, error)
}

var _ ListConsentPortalsAPIClient = (*Client)(nil)
