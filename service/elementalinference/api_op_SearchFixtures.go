// Code generated by smithy-go-codegen DO NOT EDIT.

package elementalinference

import (
	"context"
	"fmt"
	"github.com/aws/aws-sdk-go-v2/service/elementalinference/types"
	"github.com/aws/smithy-go/middleware"
)

// Searches for the fixtures (sports events, such as a specific basketball game)
// that are available for a sport in a date window. Each fixture in the response
// includes a fixtureId that you specify in the clipping output of a feed, so that
// Elemental Inference maps the event data for that fixture onto the clipping
// metadata. This operation is paginated: if there are more fixtures than fit in
// one page, the response includes a nextToken that you pass in a subsequent
// request.
func (c *Client) SearchFixtures(ctx context.Context, params *SearchFixturesInput, optFns ...func(*Options)) (*SearchFixturesOutput, error) {
	if params == nil {
		params = &SearchFixturesInput{}
	}

	result, metadata, err := c.invokeOperation(ctx, "SearchFixtures", params, optFns, c.addOperationSearchFixturesMiddlewares)
	if err != nil {
		return nil, err
	}

	out := result.(*SearchFixturesOutput)
	out.ResultMetadata = metadata
	return out, nil
}

type SearchFixturesInput struct {

	// The sport to search for fixtures. Valid values: basketball (search for
	// basketball fixtures), american-football (search for american-football fixtures).
	//
	// This member is required.
	Sport types.DataSourceSport

	// The first day of the search window, in UTC. The search includes fixtures that
	// are scheduled on this day.
	//
	// Specify the date in ISO 8601 format, as YYYY-MM-DD . For example, 2026-03-14.
	//
	// This member is required.
	StartDate *string

	// The last day of the search window, in UTC. The search includes fixtures that
	// are scheduled on this day. Specify the date in ISO 8601 format, as YYYY-MM-DD .
	//
	// If you omit this parameter, Elemental Inference searches only the day that you
	// specified in startDate. The window from startDate through endDate must not
	// exceed seven days.
	EndDate *string

	// An array of filters that narrow the results. Each filter applies to one
	// dimension of a fixture, such as the competitor. You can specify up to 10
	// filters.
	//
	// A fixture must satisfy every filter in the array in order to appear in the
	// results. Within one filter, a fixture must match at least one of the values.
	Filters []types.SearchFilter

	// The maximum number of fixtures to return for each API request.
	//
	// The service might return fewer fixtures than the maxResults value. When more
	// fixtures match the search, the response also includes a nextToken value that you
	// can use to fetch the next batch of results.
	MaxResults *int32

	// The token that identifies the batch of results that you want to see.
	//
	// For example, you submit a SearchFixtures request with maxResults set at 5. The
	// service returns the first batch of results (up to 5) and a nextToken value. To
	// see the next batch of results, you submit the SearchFixtures request a second
	// time, with the same search criteria, and specify the nextToken value.
	NextToken *string

	noSmithyDocumentSerde
}

type SearchFixturesOutput struct {

	// An array of FixtureSummary objects, one for each fixture that matches the
	// search. The array is empty if no fixtures match.
	//
	// This member is required.
	Fixtures []types.FixtureSummary

	// The token that identifies the next batch of results. To see the next batch,
	// submit the SearchFixtures request again, with the same search criteria, and
	// specify this value in nextToken.
	//
	// This parameter is absent when there are no more results to return.
	NextToken *string

	// Metadata pertaining to the operation's result.
	ResultMetadata middleware.Metadata

	noSmithyDocumentSerde
}

func (c *Client) addOperationSearchFixturesMiddlewares(stack *middleware.Stack, options Options) (err error) {
	err = stack.Serialize.Add(&awsRestjson1_serializeOpSearchFixtures{}, middleware.After)
	if err != nil {
		return err
	}
	err = stack.Deserialize.Add(&awsRestjson1_deserializeOpSearchFixtures{}, middleware.After)
	if err != nil {
		return err
	}

	if err = addComputeContentLength(stack); err != nil {
		return err
	}
	if err = addResolveEndpointMiddleware(stack, options); err != nil {
		return err
	}
	if err = addComputePayloadSHA256(stack); err != nil {
		return err
	}
	if err = addRecordResponseTiming(stack, options); err != nil {
		return err
	}
	if err = addCredentialSource(stack, options); err != nil {
		return err
	}
	if err = addOpSearchFixturesValidationMiddleware(stack); err != nil {
		return err
	}
	if err = addRequestIDRetrieverMiddleware(stack); err != nil {
		return err
	}
	if err = addResponseErrorMiddleware(stack); err != nil {
		return err
	}
	if err = addRequestResponseLogging(stack, options); err != nil {
		return err
	}
	if err = addDisableHTTPSMiddleware(stack, options); err != nil {
		return err
	}
	if err = addInterceptors(stack, options); err != nil {
		return err
	}
	return nil
}

// SearchFixturesPaginatorOptions is the paginator options for SearchFixtures
type SearchFixturesPaginatorOptions struct {
	// The maximum number of fixtures to return for each API request.
	//
	// The service might return fewer fixtures than the maxResults value. When more
	// fixtures match the search, the response also includes a nextToken value that you
	// can use to fetch the next batch of results.
	Limit int32

	// Set to true if pagination should stop if the service returns a pagination token
	// that matches the most recent token provided to the service.
	StopOnDuplicateToken bool
}

// SearchFixturesPaginator is a paginator for SearchFixtures
type SearchFixturesPaginator struct {
	options   SearchFixturesPaginatorOptions
	client    SearchFixturesAPIClient
	params    *SearchFixturesInput
	nextToken *string
	firstPage bool
}

// NewSearchFixturesPaginator returns a new SearchFixturesPaginator
func NewSearchFixturesPaginator(client SearchFixturesAPIClient, params *SearchFixturesInput, optFns ...func(*SearchFixturesPaginatorOptions)) *SearchFixturesPaginator {
	if params == nil {
		params = &SearchFixturesInput{}
	}

	options := SearchFixturesPaginatorOptions{}
	if params.MaxResults != nil {
		options.Limit = *params.MaxResults
	}

	for _, fn := range optFns {
		fn(&options)
	}

	return &SearchFixturesPaginator{
		options:   options,
		client:    client,
		params:    params,
		firstPage: true,
		nextToken: params.NextToken,
	}
}

// HasMorePages returns a boolean indicating whether more pages are available
func (p *SearchFixturesPaginator) HasMorePages() bool {
	return p.firstPage || (p.nextToken != nil && len(*p.nextToken) != 0)
}

// NextPage retrieves the next SearchFixtures page.
func (p *SearchFixturesPaginator) NextPage(ctx context.Context, optFns ...func(*Options)) (*SearchFixturesOutput, error) {
	if !p.HasMorePages() {
		return nil, fmt.Errorf("no more pages available")
	}

	params := *p.params
	params.NextToken = p.nextToken

	var limit *int32
	if p.options.Limit > 0 {
		limit = &p.options.Limit
	}
	params.MaxResults = limit

	optFns = append([]func(*Options){
		addIsPaginatorUserAgent,
	}, optFns...)
	result, err := p.client.SearchFixtures(ctx, &params, optFns...)
	if err != nil {
		return nil, err
	}
	p.firstPage = false

	prevToken := p.nextToken
	p.nextToken = result.NextToken

	if p.options.StopOnDuplicateToken &&
		prevToken != nil &&
		p.nextToken != nil &&
		*prevToken == *p.nextToken {
		p.nextToken = nil
	}

	return result, nil
}

// SearchFixturesAPIClient is a client that implements the SearchFixtures
// operation.
type SearchFixturesAPIClient interface {
	SearchFixtures(context.Context, *SearchFixturesInput, ...func(*Options)) (*SearchFixturesOutput, error)
}

var _ SearchFixturesAPIClient = (*Client)(nil)
