// Code generated by smithy-go-codegen DO NOT EDIT.

package odb

import (
	"context"
	"fmt"
	"github.com/aws/aws-sdk-go-v2/service/odb/types"
	"github.com/aws/smithy-go/middleware"
	smithyhttp "github.com/aws/smithy-go/transport/http"
)

// Returns information about the Autonomous Databases owned by your Amazon Web
// Services account in the current Amazon Web Services Region.
func (c *Client) ListAutonomousDatabases(ctx context.Context, params *ListAutonomousDatabasesInput, optFns ...func(*Options)) (*ListAutonomousDatabasesOutput, error) {
	if params == nil {
		params = &ListAutonomousDatabasesInput{}
	}

	result, metadata, err := c.invokeOperation(ctx, "ListAutonomousDatabases", params, optFns, c.addOperationListAutonomousDatabasesMiddlewares)
	if err != nil {
		return nil, err
	}

	out := result.(*ListAutonomousDatabasesOutput)
	out.ResultMetadata = metadata
	return out, nil
}

type ListAutonomousDatabasesInput struct {

	// The maximum number of items to return for this request. To get the next page of
	// items, make another request with the token returned in the output.
	MaxResults *int32

	// The token returned from a previous paginated request. Pagination continues from
	// the end of the items returned by the previous request.
	NextToken *string

	noSmithyDocumentSerde
}

type ListAutonomousDatabasesOutput struct {

	// The list of Autonomous Databases along with their properties.
	//
	// This member is required.
	AutonomousDatabases []types.AutonomousDatabaseSummary

	// The token to include in another request to get the next page of items. This
	// value is null when there are no more items to return.
	NextToken *string

	// Metadata pertaining to the operation's result.
	ResultMetadata middleware.Metadata

	noSmithyDocumentSerde
}

func (c *Client) addOperationListAutonomousDatabasesMiddlewares(stack *middleware.Stack, options Options) (err error) {
	err = stack.Serialize.Add(&awsAwsjson10_serializeOpListAutonomousDatabases{}, middleware.After)
	if err != nil {
		return err
	}
	err = stack.Deserialize.Add(&awsAwsjson10_deserializeOpListAutonomousDatabases{}, middleware.After)
	if err != nil {
		return err
	}

	if err = addlegacyEndpointContextSetter(stack, options); err != nil {
		return err
	}
	if err = addComputeContentLength(stack); err != nil {
		return err
	}
	if err = addResolveEndpointMiddleware(stack, options); err != nil {
		return err
	}
	if err = addComputePayloadSHA256(stack); err != nil {
		return err
	}
	if err = addRecordResponseTiming(stack, options); err != nil {
		return err
	}
	if err = smithyhttp.AddErrorCloseResponseBodyMiddleware(stack); err != nil {
		return err
	}
	if err = smithyhttp.AddCloseResponseBodyMiddleware(stack); err != nil {
		return err
	}
	if err = addCredentialSource(stack, options); err != nil {
		return err
	}
	if err = stack.Initialize.Add(newServiceMetadataMiddleware(options.Region, "ListAutonomousDatabases"), middleware.Before); err != nil {
		return err
	}
	if err = addRequestIDRetrieverMiddleware(stack); err != nil {
		return err
	}
	if err = addResponseErrorMiddleware(stack); err != nil {
		return err
	}
	if err = addRequestResponseLogging(stack, options); err != nil {
		return err
	}
	if err = addDisableHTTPSMiddleware(stack, options); err != nil {
		return err
	}
	if err = addInterceptors(stack, options); err != nil {
		return err
	}
	return nil
}

// ListAutonomousDatabasesPaginatorOptions is the paginator options for
// ListAutonomousDatabases
type ListAutonomousDatabasesPaginatorOptions struct {
	// The maximum number of items to return for this request. To get the next page of
	// items, make another request with the token returned in the output.
	Limit int32

	// Set to true if pagination should stop if the service returns a pagination token
	// that matches the most recent token provided to the service.
	StopOnDuplicateToken bool
}

// ListAutonomousDatabasesPaginator is a paginator for ListAutonomousDatabases
type ListAutonomousDatabasesPaginator struct {
	options   ListAutonomousDatabasesPaginatorOptions
	client    ListAutonomousDatabasesAPIClient
	params    *ListAutonomousDatabasesInput
	nextToken *string
	firstPage bool
}

// NewListAutonomousDatabasesPaginator returns a new
// ListAutonomousDatabasesPaginator
func NewListAutonomousDatabasesPaginator(client ListAutonomousDatabasesAPIClient, params *ListAutonomousDatabasesInput, optFns ...func(*ListAutonomousDatabasesPaginatorOptions)) *ListAutonomousDatabasesPaginator {
	if params == nil {
		params = &ListAutonomousDatabasesInput{}
	}

	options := ListAutonomousDatabasesPaginatorOptions{}
	if params.MaxResults != nil {
		options.Limit = *params.MaxResults
	}

	for _, fn := range optFns {
		fn(&options)
	}

	return &ListAutonomousDatabasesPaginator{
		options:   options,
		client:    client,
		params:    params,
		firstPage: true,
		nextToken: params.NextToken,
	}
}

// HasMorePages returns a boolean indicating whether more pages are available
func (p *ListAutonomousDatabasesPaginator) HasMorePages() bool {
	return p.firstPage || (p.nextToken != nil && len(*p.nextToken) != 0)
}

// NextPage retrieves the next ListAutonomousDatabases page.
func (p *ListAutonomousDatabasesPaginator) NextPage(ctx context.Context, optFns ...func(*Options)) (*ListAutonomousDatabasesOutput, error) {
	if !p.HasMorePages() {
		return nil, fmt.Errorf("no more pages available")
	}

	params := *p.params
	params.NextToken = p.nextToken

	var limit *int32
	if p.options.Limit > 0 {
		limit = &p.options.Limit
	}
	params.MaxResults = limit

	optFns = append([]func(*Options){
		addIsPaginatorUserAgent,
	}, optFns...)
	result, err := p.client.ListAutonomousDatabases(ctx, &params, optFns...)
	if err != nil {
		return nil, err
	}
	p.firstPage = false

	prevToken := p.nextToken
	p.nextToken = result.NextToken

	if p.options.StopOnDuplicateToken &&
		prevToken != nil &&
		p.nextToken != nil &&
		*prevToken == *p.nextToken {
		p.nextToken = nil
	}

	return result, nil
}

// ListAutonomousDatabasesAPIClient is a client that implements the
// ListAutonomousDatabases operation.
type ListAutonomousDatabasesAPIClient interface {
	ListAutonomousDatabases(context.Context, *ListAutonomousDatabasesInput, ...func(*Options)) (*ListAutonomousDatabasesOutput, error)
}

var _ ListAutonomousDatabasesAPIClient = (*Client)(nil)
