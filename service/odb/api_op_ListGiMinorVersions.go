// Code generated by smithy-go-codegen DO NOT EDIT.

package odb

import (
	"context"
	"fmt"
	"github.com/aws/aws-sdk-go-v2/service/odb/types"
	"github.com/aws/smithy-go/middleware"
)

// Returns a list of the Oracle Grid Infrastructure (GI) minor versions for the
// specified major version.
func (c *Client) ListGiMinorVersions(ctx context.Context, params *ListGiMinorVersionsInput, optFns ...func(*Options)) (*ListGiMinorVersionsOutput, error) {
	if params == nil {
		params = &ListGiMinorVersionsInput{}
	}

	result, metadata, err := c.invokeOperation(ctx, "ListGiMinorVersions", params, optFns, c.addOperationListGiMinorVersionsMiddlewares)
	if err != nil {
		return nil, err
	}

	out := result.(*ListGiMinorVersionsOutput)
	out.ResultMetadata = metadata
	return out, nil
}

type ListGiMinorVersionsInput struct {

	// The Oracle Grid Infrastructure (GI) major version.
	//
	// This member is required.
	GiVersion *string

	// The Availability Zone to filter GI minor versions.
	AvailabilityZone *string

	// The Availability Zone ID to filter GI minor versions.
	AvailabilityZoneId *string

	// The maximum number of items to return for this request. To get the next page of
	// items, make another request with the token returned in the output.
	MaxResults *int32

	// The token returned from a previous paginated request. Pagination continues from
	// the end of the items returned by the previous request.
	NextToken *string

	// The shape family for the GI minor version.
	ShapeFamily *string

	noSmithyDocumentSerde
}

type ListGiMinorVersionsOutput struct {

	// The list of GI minor versions.
	//
	// This member is required.
	GiMinorVersions []types.GiMinorVersionSummary

	// The token to include in another request to get the next page of items. This
	// value is null when there are no more items to return.
	NextToken *string

	// Metadata pertaining to the operation's result.
	ResultMetadata middleware.Metadata

	noSmithyDocumentSerde
}

func (c *Client) addOperationListGiMinorVersionsMiddlewares(stack *middleware.Stack, options Options) (err error) {
	err = stack.Serialize.Add(&awsAwsjson10_serializeOpListGiMinorVersions{}, middleware.After)
	if err != nil {
		return err
	}
	err = stack.Deserialize.Add(&awsAwsjson10_deserializeOpListGiMinorVersions{}, middleware.After)
	if err != nil {
		return err
	}

	if err = addComputeContentLength(stack); err != nil {
		return err
	}
	if err = addResolveEndpointMiddleware(stack, options); err != nil {
		return err
	}
	if err = addComputePayloadSHA256(stack); err != nil {
		return err
	}
	if err = addRecordResponseTiming(stack, options); err != nil {
		return err
	}
	if err = addCredentialSource(stack, options); err != nil {
		return err
	}
	if err = addOpListGiMinorVersionsValidationMiddleware(stack); err != nil {
		return err
	}
	if err = addRequestIDRetrieverMiddleware(stack); err != nil {
		return err
	}
	if err = addResponseErrorMiddleware(stack); err != nil {
		return err
	}
	if err = addRequestResponseLogging(stack, options); err != nil {
		return err
	}
	if err = addDisableHTTPSMiddleware(stack, options); err != nil {
		return err
	}
	if err = addInterceptors(stack, options); err != nil {
		return err
	}
	return nil
}

// ListGiMinorVersionsPaginatorOptions is the paginator options for
// ListGiMinorVersions
type ListGiMinorVersionsPaginatorOptions struct {
	// The maximum number of items to return for this request. To get the next page of
	// items, make another request with the token returned in the output.
	Limit int32

	// Set to true if pagination should stop if the service returns a pagination token
	// that matches the most recent token provided to the service.
	StopOnDuplicateToken bool
}

// ListGiMinorVersionsPaginator is a paginator for ListGiMinorVersions
type ListGiMinorVersionsPaginator struct {
	options   ListGiMinorVersionsPaginatorOptions
	client    ListGiMinorVersionsAPIClient
	params    *ListGiMinorVersionsInput
	nextToken *string
	firstPage bool
}

// NewListGiMinorVersionsPaginator returns a new ListGiMinorVersionsPaginator
func NewListGiMinorVersionsPaginator(client ListGiMinorVersionsAPIClient, params *ListGiMinorVersionsInput, optFns ...func(*ListGiMinorVersionsPaginatorOptions)) *ListGiMinorVersionsPaginator {
	if params == nil {
		params = &ListGiMinorVersionsInput{}
	}

	options := ListGiMinorVersionsPaginatorOptions{}
	if params.MaxResults != nil {
		options.Limit = *params.MaxResults
	}

	for _, fn := range optFns {
		fn(&options)
	}

	return &ListGiMinorVersionsPaginator{
		options:   options,
		client:    client,
		params:    params,
		firstPage: true,
		nextToken: params.NextToken,
	}
}

// HasMorePages returns a boolean indicating whether more pages are available
func (p *ListGiMinorVersionsPaginator) HasMorePages() bool {
	return p.firstPage || (p.nextToken != nil && len(*p.nextToken) != 0)
}

// NextPage retrieves the next ListGiMinorVersions page.
func (p *ListGiMinorVersionsPaginator) NextPage(ctx context.Context, optFns ...func(*Options)) (*ListGiMinorVersionsOutput, error) {
	if !p.HasMorePages() {
		return nil, fmt.Errorf("no more pages available")
	}

	params := *p.params
	params.NextToken = p.nextToken

	var limit *int32
	if p.options.Limit > 0 {
		limit = &p.options.Limit
	}
	params.MaxResults = limit

	optFns = append([]func(*Options){
		addIsPaginatorUserAgent,
	}, optFns...)
	result, err := p.client.ListGiMinorVersions(ctx, &params, optFns...)
	if err != nil {
		return nil, err
	}
	p.firstPage = false

	prevToken := p.nextToken
	p.nextToken = result.NextToken

	if p.options.StopOnDuplicateToken &&
		prevToken != nil &&
		p.nextToken != nil &&
		*prevToken == *p.nextToken {
		p.nextToken = nil
	}

	return result, nil
}

// ListGiMinorVersionsAPIClient is a client that implements the
// ListGiMinorVersions operation.
type ListGiMinorVersionsAPIClient interface {
	ListGiMinorVersions(context.Context, *ListGiMinorVersionsInput, ...func(*Options)) (*ListGiMinorVersionsOutput, error)
}

var _ ListGiMinorVersionsAPIClient = (*Client)(nil)
