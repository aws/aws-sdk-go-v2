// Code generated by smithy-go-codegen DO NOT EDIT.

package support

import (
	"context"
	"github.com/aws/aws-sdk-go-v2/service/support/types"
	"github.com/aws/smithy-go/middleware"
)

// Returns one or more presigned upload URLs for uploading a large file attachment
// to a support case by using a multipart upload workflow. The maximum file size
// that you can upload with this workflow is 150 MB, and parts can be up to 100 MB
// each. Initiate a new upload by providing fileName and fileSizeBytes ; the
// response returns a unique uploadId , the part size, the total number of parts,
// and a list of presigned upload URLs for the requested range of parts. A maximum
// of 10 upload URLs are returned per call. To retrieve more upload URLs for an
// upload that's already in progress, call GetAttachmentUploadLinks again with the
// existing uploadId and a new uploadRange .
//
// Upload each part to its presigned URL by using HTTP PUT and capture the ETag
// from the response. After you upload all parts, call CompleteAttachmentUploadwith the uploadId and the
// list of part indexes and ETags to finalize the upload. You can then attach the
// upload to a case by passing the uploadId in the uploadIds parameter of CreateCase or AddCommunicationToCase. To
// monitor progress before completion, call DescribeAttachmentUploadStatus.
//
//   - You must have an Amazon Web Services Business Support+, Amazon Web Services
//     Enterprise Support, or Amazon Web Services Unified Operations plan to use the
//     Amazon Web Services Support API. If you're in an Amazon Web Services Region that
//     doesn't offer one of these Amazon Web Services Support plans, or if you haven't
//     transitioned to one of these plans, you can use the Amazon Web Services Support
//     API with a Business, Enterprise On-Ramp, or Enterprise Support plan.
//
//   - If you call the Amazon Web Services Support API from an account that
//     doesn't have an Amazon Web Services Business Support+, Amazon Web Services
//     Enterprise Support, or Amazon Web Services Unified Operations plan, the
//     SubscriptionRequiredException error message appears. For information about
//     changing your support plan, see [Amazon Web Services Support].
//
// [Amazon Web Services Support]: http://aws.amazon.com/premiumsupport/
func (c *Client) GetAttachmentUploadLinks(ctx context.Context, params *GetAttachmentUploadLinksInput, optFns ...func(*Options)) (*GetAttachmentUploadLinksOutput, error) {
	if params == nil {
		params = &GetAttachmentUploadLinksInput{}
	}

	result, metadata, err := c.invokeOperation(ctx, "GetAttachmentUploadLinks", params, optFns, c.addOperationGetAttachmentUploadLinksMiddlewares)
	if err != nil {
		return nil, err
	}

	out := result.(*GetAttachmentUploadLinksOutput)
	out.ResultMetadata = metadata
	return out, nil
}

type GetAttachmentUploadLinksInput struct {

	// The name of the file to upload, including the file extension. This value is
	// required when you initiate a new upload.
	//
	// This member is required.
	FileName *string

	// Specifies whether to validate the request without actually generating upload
	// URLs. When set to true , the request is validated but no URLs are returned, and
	// the operation returns a DryRunOperationException . When omitted or set to false
	// , the request runs normally.
	DryRun *bool

	// The total size of the file in bytes. The service uses this value to calculate
	// the total number of parts and the size of each part. Required when you initiate
	// a new upload (when uploadId isn't provided). Valid range: 1 to 157,286,400
	// bytes (approximately 150 MB).
	FileSizeBytes *int64

	// The unique identifier of an in-progress multipart upload, returned by a
	// previous call to GetAttachmentUploadLinks . Specify uploadId to retrieve
	// additional presigned upload URLs for an upload that has already been initiated.
	// Required when fileSizeBytes isn't provided. Length: 1 to 2,048 characters.
	UploadId *string

	// The range of part indexes for which to return presigned upload URLs. Use this
	// parameter to page through the upload URLs for a large file across multiple
	// calls. If you omit this parameter, the service determines the range to return.
	UploadRange *types.UploadRange

	noSmithyDocumentSerde
}

type GetAttachmentUploadLinksOutput struct {

	// The size, in bytes, of each part. Split the file into parts of this size before
	// you upload them to the presigned URLs. For an upload with n total parts, parts
	// 1 through n - 1 are exactly this size; the last part may be smaller. Maximum:
	// 104,857,600 bytes (approximately 100 MB).
	//
	// This member is required.
	PartSizeBytes *int64

	// The total number of parts that the file is split into. Upload one part to each
	// presigned URL.
	//
	// This member is required.
	TotalParts int32

	// The unique identifier for the multipart upload. Use this value in subsequent
	// calls to GetAttachmentUploadLinks , DescribeAttachmentUploadStatus, and CompleteAttachmentUpload, and to attach the upload to a case
	// through the uploadIds parameter on CreateCase or AddCommunicationToCase.
	//
	// This member is required.
	UploadId *string

	// The list of presigned upload URLs for the requested range of parts. The list
	// contains at most 10 URLs per call. Upload each part to its corresponding URL by
	// using HTTP PUT before the URL expires.
	//
	// This member is required.
	UploadUrls []types.UploadUrl

	// The next part index to request presigned URLs for. If all upload URLs for the
	// file have been returned, this field is null . Use this value as the startIndex
	// in uploadRange on a subsequent call to GetAttachmentUploadLinks to retrieve the
	// next batch of upload URLs.
	NextIndex int32

	// Metadata pertaining to the operation's result.
	ResultMetadata middleware.Metadata

	noSmithyDocumentSerde
}

func (c *Client) addOperationGetAttachmentUploadLinksMiddlewares(stack *middleware.Stack, options Options) (err error) {
	err = stack.Serialize.Add(&awsAwsjson11_serializeOpGetAttachmentUploadLinks{}, middleware.After)
	if err != nil {
		return err
	}
	err = stack.Deserialize.Add(&awsAwsjson11_deserializeOpGetAttachmentUploadLinks{}, middleware.After)
	if err != nil {
		return err
	}

	if err = addResolveEndpointMiddleware(stack, options); err != nil {
		return err
	}
	if err = addComputePayloadSHA256(stack); err != nil {
		return err
	}
	if err = addRecordResponseTiming(stack, options); err != nil {
		return err
	}
	if err = addCredentialSource(stack, options); err != nil {
		return err
	}
	if err = addOpGetAttachmentUploadLinksValidationMiddleware(stack); err != nil {
		return err
	}
	if err = addRequestIDRetrieverMiddleware(stack); err != nil {
		return err
	}
	if err = addResponseErrorMiddleware(stack); err != nil {
		return err
	}
	if err = addRequestResponseLogging(stack, options); err != nil {
		return err
	}
	if err = addDisableHTTPSMiddleware(stack, options); err != nil {
		return err
	}
	if err = addInterceptors(stack, options); err != nil {
		return err
	}
	return nil
}
