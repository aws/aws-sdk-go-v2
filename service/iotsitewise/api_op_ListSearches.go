// Code generated by smithy-go-codegen DO NOT EDIT.

package iotsitewise

import (
	"context"
	"fmt"
	"github.com/aws/aws-sdk-go-v2/service/iotsitewise/types"
	"github.com/aws/smithy-go/middleware"
	smithyhttp "github.com/aws/smithy-go/transport/http"
)

// Lists the searches in a workspace, most recently started first. Results can be
// narrowed with optional filters (status, search type, group, and started-at time
// range) and are paginated: when nextToken is present, pass it on a subsequent
// call to retrieve the next page.
func (c *Client) ListSearches(ctx context.Context, params *ListSearchesInput, optFns ...func(*Options)) (*ListSearchesOutput, error) {
	if params == nil {
		params = &ListSearchesInput{}
	}

	result, metadata, err := c.invokeOperation(ctx, "ListSearches", params, optFns, c.addOperationListSearchesMiddlewares)
	if err != nil {
		return nil, err
	}

	out := result.(*ListSearchesOutput)
	out.ResultMetadata = metadata
	return out, nil
}

// Input for the ListSearches operation.
type ListSearchesInput struct {

	// The name of the workspace whose searches are listed.
	//
	// This member is required.
	WorkspaceName *string

	// Optional filters that restrict which searches are returned.
	ListSearchesFilters *types.ListSearchesFilters

	// The maximum number of searches to return in a single page. Valid range is 1 to
	// 1,000; if omitted, a service-defined default is used.
	MaxResults *int32

	// The pagination token returned by a previous ListSearches call. Provide it to
	// retrieve the next page; omit it to retrieve the first page.
	NextToken *string

	noSmithyDocumentSerde
}

// Output of the ListSearches operation.
type ListSearchesOutput struct {

	// A page of search summaries, most recently started first.
	//
	// This member is required.
	SearchSummaries []types.SearchSummary

	// The pagination token to use in a subsequent ListSearches call to retrieve the
	// next page. Absent when there are no more searches.
	NextToken *string

	// Metadata pertaining to the operation's result.
	ResultMetadata middleware.Metadata

	noSmithyDocumentSerde
}

func (c *Client) addOperationListSearchesMiddlewares(stack *middleware.Stack, options Options) (err error) {
	err = stack.Serialize.Add(&awsRestjson1_serializeOpListSearches{}, middleware.After)
	if err != nil {
		return err
	}
	err = stack.Deserialize.Add(&awsRestjson1_deserializeOpListSearches{}, middleware.After)
	if err != nil {
		return err
	}

	if err = addComputeContentLength(stack); err != nil {
		return err
	}
	if err = addResolveEndpointMiddleware(stack, options); err != nil {
		return err
	}
	if err = addComputePayloadSHA256(stack); err != nil {
		return err
	}
	if err = addRecordResponseTiming(stack, options); err != nil {
		return err
	}
	if err = addCredentialSource(stack, options); err != nil {
		return err
	}
	if err = addEndpointPrefix_opListSearchesMiddleware(stack); err != nil {
		return err
	}
	if err = addOpListSearchesValidationMiddleware(stack); err != nil {
		return err
	}
	if err = addRequestIDRetrieverMiddleware(stack); err != nil {
		return err
	}
	if err = addResponseErrorMiddleware(stack); err != nil {
		return err
	}
	if err = addRequestResponseLogging(stack, options); err != nil {
		return err
	}
	if err = addDisableHTTPSMiddleware(stack, options); err != nil {
		return err
	}
	if err = addInterceptors(stack, options); err != nil {
		return err
	}
	return nil
}

// ListSearchesPaginatorOptions is the paginator options for ListSearches
type ListSearchesPaginatorOptions struct {
	// The maximum number of searches to return in a single page. Valid range is 1 to
	// 1,000; if omitted, a service-defined default is used.
	Limit int32

	// Set to true if pagination should stop if the service returns a pagination token
	// that matches the most recent token provided to the service.
	StopOnDuplicateToken bool
}

// ListSearchesPaginator is a paginator for ListSearches
type ListSearchesPaginator struct {
	options   ListSearchesPaginatorOptions
	client    ListSearchesAPIClient
	params    *ListSearchesInput
	nextToken *string
	firstPage bool
}

// NewListSearchesPaginator returns a new ListSearchesPaginator
func NewListSearchesPaginator(client ListSearchesAPIClient, params *ListSearchesInput, optFns ...func(*ListSearchesPaginatorOptions)) *ListSearchesPaginator {
	if params == nil {
		params = &ListSearchesInput{}
	}

	options := ListSearchesPaginatorOptions{}
	if params.MaxResults != nil {
		options.Limit = *params.MaxResults
	}

	for _, fn := range optFns {
		fn(&options)
	}

	return &ListSearchesPaginator{
		options:   options,
		client:    client,
		params:    params,
		firstPage: true,
		nextToken: params.NextToken,
	}
}

// HasMorePages returns a boolean indicating whether more pages are available
func (p *ListSearchesPaginator) HasMorePages() bool {
	return p.firstPage || (p.nextToken != nil && len(*p.nextToken) != 0)
}

// NextPage retrieves the next ListSearches page.
func (p *ListSearchesPaginator) NextPage(ctx context.Context, optFns ...func(*Options)) (*ListSearchesOutput, error) {
	if !p.HasMorePages() {
		return nil, fmt.Errorf("no more pages available")
	}

	params := *p.params
	params.NextToken = p.nextToken

	var limit *int32
	if p.options.Limit > 0 {
		limit = &p.options.Limit
	}
	params.MaxResults = limit

	optFns = append([]func(*Options){
		addIsPaginatorUserAgent,
	}, optFns...)
	result, err := p.client.ListSearches(ctx, &params, optFns...)
	if err != nil {
		return nil, err
	}
	p.firstPage = false

	prevToken := p.nextToken
	p.nextToken = result.NextToken

	if p.options.StopOnDuplicateToken &&
		prevToken != nil &&
		p.nextToken != nil &&
		*prevToken == *p.nextToken {
		p.nextToken = nil
	}

	return result, nil
}

type endpointPrefix_opListSearchesMiddleware struct {
}

func (*endpointPrefix_opListSearchesMiddleware) ID() string {
	return "EndpointHostPrefix"
}

func (m *endpointPrefix_opListSearchesMiddleware) HandleFinalize(ctx context.Context, in middleware.FinalizeInput, next middleware.FinalizeHandler) (
	out middleware.FinalizeOutput, metadata middleware.Metadata, err error,
) {
	if smithyhttp.GetHostnameImmutable(ctx) || smithyhttp.IsEndpointHostPrefixDisabled(ctx) {
		return next.HandleFinalize(ctx, in)
	}

	req, ok := in.Request.(*smithyhttp.Request)
	if !ok {
		return out, metadata, fmt.Errorf("unknown transport type %T", in.Request)
	}

	req.URL.Host = "data." + req.URL.Host

	return next.HandleFinalize(ctx, in)
}
func addEndpointPrefix_opListSearchesMiddleware(stack *middleware.Stack) error {
	return stack.Finalize.Insert(&endpointPrefix_opListSearchesMiddleware{}, "ResolveEndpointV2", middleware.After)
}

// ListSearchesAPIClient is a client that implements the ListSearches operation.
type ListSearchesAPIClient interface {
	ListSearches(context.Context, *ListSearchesInput, ...func(*Options)) (*ListSearchesOutput, error)
}

var _ ListSearchesAPIClient = (*Client)(nil)
