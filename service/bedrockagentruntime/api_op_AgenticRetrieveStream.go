// Code generated by smithy-go-codegen DO NOT EDIT.

package bedrockagentruntime

import (
	"context"
	"fmt"
	"github.com/aws/aws-sdk-go-v2/service/bedrockagentruntime/types"
	"github.com/aws/smithy-go/middleware"
	smithysync "github.com/aws/smithy-go/sync"
	"sync"
)

// Retrieves information from one or more knowledge bases using an agentic
// approach. Agentic retrieval uses a foundation model to intelligently decompose
// complex queries into sub-queries and iteratively retrieve relevant information
// from your knowledge bases. This approach improves retrieval accuracy for
// complex, multi-step questions that a single retrieval pass might not fully
// address.
//
// The operation returns results through a stream that includes retrieval results,
// trace events for visibility into the process, and a generated response
// synthesized from the results by default, which can be turned off.
func (c *Client) AgenticRetrieveStream(ctx context.Context, params *AgenticRetrieveStreamInput, optFns ...func(*Options)) (*AgenticRetrieveStreamOutput, error) {
	if params == nil {
		params = &AgenticRetrieveStreamInput{}
	}

	result, metadata, err := c.invokeEventStreamOperation(ctx, "AgenticRetrieveStream", params, optFns, c.addOperationAgenticRetrieveStreamMiddlewares)
	if err != nil {
		return nil, err
	}

	out := result.(*AgenticRetrieveStreamOutput)
	out.ResultMetadata = metadata
	return out, nil
}

// Request structure for the agentic retrieve stream operation.
type AgenticRetrieveStreamInput struct {

	// Configuration settings for the agentic retrieval operation.
	//
	// This member is required.
	AgenticRetrieveConfiguration *types.AgenticRetrieveConfiguration

	// The list of messages for the agentic retrieval conversation.
	//
	// This member is required.
	Messages []types.AgenticRetrieveMessage

	// The list of retrievers to use for agentic retrieval.
	//
	// This member is required.
	Retrievers []types.AgenticRetriever

	// Whether to generate a response based on the retrieved results.
	GenerateResponse *bool

	// Opaque continuation token for paginated results.
	NextToken *string

	// Policy configuration for guardrails and content filtering.
	PolicyConfiguration *types.AgenticRetrievePolicyConfiguration

	// Contains information about the user making the request. This is used for access
	// control filtering to ensure that retrieval results only include documents the
	// user is authorized to access.
	UserContext *types.UserContext

	noSmithyDocumentSerde
}

// Response structure for the agentic retrieve stream operation.
type AgenticRetrieveStreamOutput struct {
	eventStream *AgenticRetrieveStreamEventStream

	initialReply chan AgenticRetrieveStreamInitialReply
	// Metadata pertaining to the operation's result.
	ResultMetadata middleware.Metadata

	noSmithyDocumentSerde
}

// GetStream returns the type to interact with the event stream.
func (o *AgenticRetrieveStreamOutput) GetStream() *AgenticRetrieveStreamEventStream {
	return o.eventStream
}

// Response structure for the agentic retrieve stream operation.
type AgenticRetrieveStreamInitialReply struct {
	// Metadata pertaining to the operation's result.
	ResultMetadata middleware.Metadata

	noSmithyDocumentSerde
}

func (o *AgenticRetrieveStreamOutput) GetInitialReply() <-chan AgenticRetrieveStreamInitialReply {
	return o.initialReply
}

func (c *Client) addOperationAgenticRetrieveStreamMiddlewares(stack *middleware.Stack, options Options) (err error) {
	err = stack.Serialize.Add(&awsRestjson1_serializeOpAgenticRetrieveStream{}, middleware.After)
	if err != nil {
		return err
	}
	err = stack.Deserialize.Add(&awsRestjson1_deserializeOpAgenticRetrieveStream{}, middleware.After)
	if err != nil {
		return err
	}

	if err = addlegacyEndpointContextSetter(stack, options); err != nil {
		return err
	}
	if err = addEventStreamAgenticRetrieveStreamMiddleware(stack, options); err != nil {
		return err
	}
	if err = addEventStreamBuild_opAgenticRetrieveStreamMiddleware(stack); err != nil {
		return err
	}
	if err = addComputeContentLength(stack); err != nil {
		return err
	}
	if err = addResolveEndpointMiddleware(stack, options); err != nil {
		return err
	}
	if err = addComputePayloadSHA256(stack); err != nil {
		return err
	}
	if err = addRecordResponseTiming(stack, options); err != nil {
		return err
	}
	if err = addCredentialSource(stack, options); err != nil {
		return err
	}
	if err = addOpAgenticRetrieveStreamValidationMiddleware(stack); err != nil {
		return err
	}
	if err = stack.Initialize.Add(newServiceMetadataMiddleware(options.Region, "AgenticRetrieveStream"), middleware.Before); err != nil {
		return err
	}
	if err = addRequestIDRetrieverMiddleware(stack); err != nil {
		return err
	}
	if err = addResponseErrorMiddleware(stack); err != nil {
		return err
	}
	if err = addRequestResponseLogging(stack, options); err != nil {
		return err
	}
	if err = addDisableHTTPSMiddleware(stack, options); err != nil {
		return err
	}
	if err = addInterceptors(stack, options); err != nil {
		return err
	}
	return nil
}

type eventStreamBuild_opAgenticRetrieveStreamMiddleware struct {
}

func (*eventStreamBuild_opAgenticRetrieveStreamMiddleware) ID() string {
	return "EventStreamBuildMiddleware"
}

func (m *eventStreamBuild_opAgenticRetrieveStreamMiddleware) HandleBuild(ctx context.Context, in middleware.BuildInput, next middleware.BuildHandler) (
	out middleware.BuildOutput, metadata middleware.Metadata, err error,
) {
	out, metadata, err = next.HandleBuild(ctx, in)
	res, ok := middleware.GetEventStreamOutputToMetadata[AgenticRetrieveStreamOutput](&metadata)
	if !ok {
		return out, metadata, fmt.Errorf("expected to find an object of type AgenticRetrieveStreamOutput on metadata, none was found. Metadata %v. Additionally %w", metadata, err)
	}
	if err != nil {
		// fail the event stream because the middleware failed
		res.eventStream.err.SetError(err)
		res.GetStream().Close()
	}
	initialReply, ok := out.Result.(*AgenticRetrieveStreamInitialReply)
	_ = initialReply
	if !ok {
		// set an initial reply with just the metadata. Error was set above
		response := AgenticRetrieveStreamInitialReply{
			ResultMetadata: metadata,
		}
		res.initialReply <- response
		return out, metadata, fmt.Errorf("unexpected type of result. expected AgenticRetrieveStreamInitialReply, got %T. Additionally %w", out.Result, err)
	}
	response := AgenticRetrieveStreamInitialReply{
		ResultMetadata: metadata,
	}
	res.initialReply <- response
	return out, metadata, err
}
func addEventStreamBuild_opAgenticRetrieveStreamMiddleware(stack *middleware.Stack) error {
	return stack.Build.Add(&eventStreamBuild_opAgenticRetrieveStreamMiddleware{}, middleware.Before)
}

// AgenticRetrieveStreamEventStream provides the event stream handling for the AgenticRetrieveStream operation.
//
// For testing and mocking the event stream this type should be initialized via
// the NewAgenticRetrieveStreamEventStream constructor function. Using the functional options
// to pass in nested mock behavior.
type AgenticRetrieveStreamEventStream struct {
	// AgenticRetrieveStreamResponseOutputReader is the EventStream reader for the
	// AgenticRetrieveStreamResponseOutput events. This value is automatically set by
	// the SDK when the API call is made Use this member when unit testing your code
	// with the SDK to mock out the EventStream Reader.
	//
	// Must not be nil.
	Reader AgenticRetrieveStreamResponseOutputReader

	done      chan struct{}
	closeOnce sync.Once
	err       *smithysync.OnceErr
}

// NewAgenticRetrieveStreamEventStream initializes an AgenticRetrieveStreamEventStream.
// This function should only be used for testing and mocking the AgenticRetrieveStreamEventStream
// stream within your application.
//
// The Reader member must be set before reading events from the stream.
func NewAgenticRetrieveStreamEventStream(optFns ...func(*AgenticRetrieveStreamEventStream)) *AgenticRetrieveStreamEventStream {
	es := &AgenticRetrieveStreamEventStream{
		done: make(chan struct{}),
		err:  smithysync.NewOnceErr(),
	}
	for _, fn := range optFns {
		fn(es)
	}
	return es
}

// Events returns a channel to read events from.
func (es *AgenticRetrieveStreamEventStream) Events() <-chan types.AgenticRetrieveStreamResponseOutput {
	return es.Reader.Events()
}

// Close closes the stream. This will also cause the stream to be closed.
// Close must be called when done using the stream API. Not calling Close
// may result in resource leaks.
//
// Will close the underlying EventStream writer and reader, and no more events can be
// sent or received.
func (es *AgenticRetrieveStreamEventStream) Close() error {
	es.closeOnce.Do(es.safeClose)
	return es.Err()
}

func (es *AgenticRetrieveStreamEventStream) safeClose() {
	close(es.done)

	es.Reader.Close()
}

// Err returns any error that occurred while reading or writing EventStream Events
// from the service API's response. Returns nil if there were no errors.
func (es *AgenticRetrieveStreamEventStream) Err() error {
	if err := es.err.Err(); err != nil {
		return err
	}

	if err := es.Reader.Err(); err != nil {
		return err
	}

	return nil
}

func (es *AgenticRetrieveStreamEventStream) waitStreamClose() {
	type errorSet interface {
		ErrorSet() <-chan struct{}
	}

	var outputErrCh <-chan struct{}
	if v, ok := es.Reader.(errorSet); ok {
		outputErrCh = v.ErrorSet()
	}
	var outputClosedCh <-chan struct{}
	if v, ok := es.Reader.(interface{ Closed() <-chan struct{} }); ok {
		outputClosedCh = v.Closed()
	}

	select {
	case <-es.done:
	case <-outputErrCh:
		es.err.SetError(es.Reader.Err())
		es.Close()

	case <-outputClosedCh:
		if err := es.Reader.Err(); err != nil {
			es.err.SetError(es.Reader.Err())
		}
		es.Close()

	}
}
