// Code generated by smithy-go-codegen DO NOT EDIT.

package wellarchitected

import (
	"context"
	"fmt"
	"github.com/aws/aws-sdk-go-v2/service/wellarchitected/types"
	"github.com/aws/smithy-go/middleware"
)

// Lists optimization profiles in your account. Profiles define the scope and
// configuration for generating optimization recommendations.
func (c *Client) ListAgentProfiles(ctx context.Context, params *ListAgentProfilesInput, optFns ...func(*Options)) (*ListAgentProfilesOutput, error) {
	if params == nil {
		params = &ListAgentProfilesInput{}
	}

	result, metadata, err := c.invokeOperation(ctx, "ListAgentProfiles", params, optFns, c.addOperationListAgentProfilesMiddlewares)
	if err != nil {
		return nil, err
	}

	out := result.(*ListAgentProfilesOutput)
	out.ResultMetadata = metadata
	return out, nil
}

type ListAgentProfilesInput struct {

	// The maximum number of profiles to return in a single call. Default is 100.
	MaxResults *int32

	// A pagination token returned from a previous call to continue retrieving results.
	NextToken *string

	noSmithyDocumentSerde
}

type ListAgentProfilesOutput struct {

	// A list of profile summaries.
	//
	// This member is required.
	Items []types.AgentProfileSummary

	// A pagination token to retrieve the next set of results, if available.
	NextToken *string

	// Metadata pertaining to the operation's result.
	ResultMetadata middleware.Metadata

	noSmithyDocumentSerde
}

func (c *Client) addOperationListAgentProfilesMiddlewares(stack *middleware.Stack, options Options) (err error) {
	err = stack.Serialize.Add(&awsRestjson1_serializeOpListAgentProfiles{}, middleware.After)
	if err != nil {
		return err
	}
	err = stack.Deserialize.Add(&awsRestjson1_deserializeOpListAgentProfiles{}, middleware.After)
	if err != nil {
		return err
	}

	if err = addResolveEndpointMiddleware(stack, options); err != nil {
		return err
	}
	if err = addComputePayloadSHA256(stack); err != nil {
		return err
	}
	if err = addRecordResponseTiming(stack, options); err != nil {
		return err
	}
	if err = addCredentialSource(stack, options); err != nil {
		return err
	}
	if err = addRequestIDRetrieverMiddleware(stack); err != nil {
		return err
	}
	if err = addResponseErrorMiddleware(stack); err != nil {
		return err
	}
	if err = addRequestResponseLogging(stack, options); err != nil {
		return err
	}
	if err = addDisableHTTPSMiddleware(stack, options); err != nil {
		return err
	}
	if err = addInterceptors(stack, options); err != nil {
		return err
	}
	return nil
}

// ListAgentProfilesPaginatorOptions is the paginator options for ListAgentProfiles
type ListAgentProfilesPaginatorOptions struct {
	// The maximum number of profiles to return in a single call. Default is 100.
	Limit int32

	// Set to true if pagination should stop if the service returns a pagination token
	// that matches the most recent token provided to the service.
	StopOnDuplicateToken bool
}

// ListAgentProfilesPaginator is a paginator for ListAgentProfiles
type ListAgentProfilesPaginator struct {
	options   ListAgentProfilesPaginatorOptions
	client    ListAgentProfilesAPIClient
	params    *ListAgentProfilesInput
	nextToken *string
	firstPage bool
}

// NewListAgentProfilesPaginator returns a new ListAgentProfilesPaginator
func NewListAgentProfilesPaginator(client ListAgentProfilesAPIClient, params *ListAgentProfilesInput, optFns ...func(*ListAgentProfilesPaginatorOptions)) *ListAgentProfilesPaginator {
	if params == nil {
		params = &ListAgentProfilesInput{}
	}

	options := ListAgentProfilesPaginatorOptions{}
	if params.MaxResults != nil {
		options.Limit = *params.MaxResults
	}

	for _, fn := range optFns {
		fn(&options)
	}

	return &ListAgentProfilesPaginator{
		options:   options,
		client:    client,
		params:    params,
		firstPage: true,
		nextToken: params.NextToken,
	}
}

// HasMorePages returns a boolean indicating whether more pages are available
func (p *ListAgentProfilesPaginator) HasMorePages() bool {
	return p.firstPage || (p.nextToken != nil && len(*p.nextToken) != 0)
}

// NextPage retrieves the next ListAgentProfiles page.
func (p *ListAgentProfilesPaginator) NextPage(ctx context.Context, optFns ...func(*Options)) (*ListAgentProfilesOutput, error) {
	if !p.HasMorePages() {
		return nil, fmt.Errorf("no more pages available")
	}

	params := *p.params
	params.NextToken = p.nextToken

	var limit *int32
	if p.options.Limit > 0 {
		limit = &p.options.Limit
	}
	params.MaxResults = limit

	optFns = append([]func(*Options){
		addIsPaginatorUserAgent,
	}, optFns...)
	result, err := p.client.ListAgentProfiles(ctx, &params, optFns...)
	if err != nil {
		return nil, err
	}
	p.firstPage = false

	prevToken := p.nextToken
	p.nextToken = result.NextToken

	if p.options.StopOnDuplicateToken &&
		prevToken != nil &&
		p.nextToken != nil &&
		*prevToken == *p.nextToken {
		p.nextToken = nil
	}

	return result, nil
}

// ListAgentProfilesAPIClient is a client that implements the ListAgentProfiles
// operation.
type ListAgentProfilesAPIClient interface {
	ListAgentProfiles(context.Context, *ListAgentProfilesInput, ...func(*Options)) (*ListAgentProfilesOutput, error)
}

var _ ListAgentProfilesAPIClient = (*Client)(nil)
