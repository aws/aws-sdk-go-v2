// Code generated by smithy-go-codegen DO NOT EDIT.

package wellarchitected

import (
	"context"
	"fmt"
	"github.com/aws/aws-sdk-go-v2/service/wellarchitected/types"
	"github.com/aws/smithy-go/middleware"
)

// Lists contexts associated with a profile.
func (c *Client) ListAgentContexts(ctx context.Context, params *ListAgentContextsInput, optFns ...func(*Options)) (*ListAgentContextsOutput, error) {
	if params == nil {
		params = &ListAgentContextsInput{}
	}

	result, metadata, err := c.invokeOperation(ctx, "ListAgentContexts", params, optFns, c.addOperationListAgentContextsMiddlewares)
	if err != nil {
		return nil, err
	}

	out := result.(*ListAgentContextsOutput)
	out.ResultMetadata = metadata
	return out, nil
}

type ListAgentContextsInput struct {

	// The Amazon Resource Name (ARN) of the profile to list contexts for.
	//
	// This member is required.
	ProfileArn *string

	// The maximum number of results to return for this request.
	MaxResults *int32

	// The token to use to retrieve the next set of results.
	NextToken *string

	noSmithyDocumentSerde
}

type ListAgentContextsOutput struct {

	// A list of context summaries associated with the profile.
	//
	// This member is required.
	Items []types.ContextSummary

	// The token to use to retrieve the next set of results.
	NextToken *string

	// Metadata pertaining to the operation's result.
	ResultMetadata middleware.Metadata

	noSmithyDocumentSerde
}

func (c *Client) addOperationListAgentContextsMiddlewares(stack *middleware.Stack, options Options) (err error) {
	err = stack.Serialize.Add(&awsRestjson1_serializeOpListAgentContexts{}, middleware.After)
	if err != nil {
		return err
	}
	err = stack.Deserialize.Add(&awsRestjson1_deserializeOpListAgentContexts{}, middleware.After)
	if err != nil {
		return err
	}

	if err = addComputeContentLength(stack); err != nil {
		return err
	}
	if err = addResolveEndpointMiddleware(stack, options); err != nil {
		return err
	}
	if err = addComputePayloadSHA256(stack); err != nil {
		return err
	}
	if err = addRecordResponseTiming(stack, options); err != nil {
		return err
	}
	if err = addCredentialSource(stack, options); err != nil {
		return err
	}
	if err = addOpListAgentContextsValidationMiddleware(stack); err != nil {
		return err
	}
	if err = addRequestIDRetrieverMiddleware(stack); err != nil {
		return err
	}
	if err = addResponseErrorMiddleware(stack); err != nil {
		return err
	}
	if err = addRequestResponseLogging(stack, options); err != nil {
		return err
	}
	if err = addDisableHTTPSMiddleware(stack, options); err != nil {
		return err
	}
	if err = addInterceptors(stack, options); err != nil {
		return err
	}
	return nil
}

// ListAgentContextsPaginatorOptions is the paginator options for ListAgentContexts
type ListAgentContextsPaginatorOptions struct {
	// The maximum number of results to return for this request.
	Limit int32

	// Set to true if pagination should stop if the service returns a pagination token
	// that matches the most recent token provided to the service.
	StopOnDuplicateToken bool
}

// ListAgentContextsPaginator is a paginator for ListAgentContexts
type ListAgentContextsPaginator struct {
	options   ListAgentContextsPaginatorOptions
	client    ListAgentContextsAPIClient
	params    *ListAgentContextsInput
	nextToken *string
	firstPage bool
}

// NewListAgentContextsPaginator returns a new ListAgentContextsPaginator
func NewListAgentContextsPaginator(client ListAgentContextsAPIClient, params *ListAgentContextsInput, optFns ...func(*ListAgentContextsPaginatorOptions)) *ListAgentContextsPaginator {
	if params == nil {
		params = &ListAgentContextsInput{}
	}

	options := ListAgentContextsPaginatorOptions{}
	if params.MaxResults != nil {
		options.Limit = *params.MaxResults
	}

	for _, fn := range optFns {
		fn(&options)
	}

	return &ListAgentContextsPaginator{
		options:   options,
		client:    client,
		params:    params,
		firstPage: true,
		nextToken: params.NextToken,
	}
}

// HasMorePages returns a boolean indicating whether more pages are available
func (p *ListAgentContextsPaginator) HasMorePages() bool {
	return p.firstPage || (p.nextToken != nil && len(*p.nextToken) != 0)
}

// NextPage retrieves the next ListAgentContexts page.
func (p *ListAgentContextsPaginator) NextPage(ctx context.Context, optFns ...func(*Options)) (*ListAgentContextsOutput, error) {
	if !p.HasMorePages() {
		return nil, fmt.Errorf("no more pages available")
	}

	params := *p.params
	params.NextToken = p.nextToken

	var limit *int32
	if p.options.Limit > 0 {
		limit = &p.options.Limit
	}
	params.MaxResults = limit

	optFns = append([]func(*Options){
		addIsPaginatorUserAgent,
	}, optFns...)
	result, err := p.client.ListAgentContexts(ctx, &params, optFns...)
	if err != nil {
		return nil, err
	}
	p.firstPage = false

	prevToken := p.nextToken
	p.nextToken = result.NextToken

	if p.options.StopOnDuplicateToken &&
		prevToken != nil &&
		p.nextToken != nil &&
		*prevToken == *p.nextToken {
		p.nextToken = nil
	}

	return result, nil
}

// ListAgentContextsAPIClient is a client that implements the ListAgentContexts
// operation.
type ListAgentContextsAPIClient interface {
	ListAgentContexts(context.Context, *ListAgentContextsInput, ...func(*Options)) (*ListAgentContextsOutput, error)
}

var _ ListAgentContextsAPIClient = (*Client)(nil)
