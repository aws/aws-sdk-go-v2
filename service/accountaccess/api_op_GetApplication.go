// Code generated by smithy-go-codegen DO NOT EDIT.

package accountaccess

import (
	"context"
	"fmt"
	"github.com/aws/aws-sdk-go-v2/service/accountaccess/schemas"
	"github.com/aws/aws-sdk-go-v2/service/accountaccess/types"
	smithy "github.com/aws/smithy-go"
	"github.com/aws/smithy-go/middleware"
	smithytime "github.com/aws/smithy-go/time"
	smithywaiter "github.com/aws/smithy-go/waiter"
	"time"
)

// Retrieves details about an account access manager application, including its
// status, identity source, and tags.
func (c *Client) GetApplication(ctx context.Context, params *GetApplicationInput, optFns ...func(*Options)) (*GetApplicationOutput, error) {
	if params == nil {
		params = &GetApplicationInput{}
	}

	result, metadata, err := c.invokeOperation(ctx, "GetApplication", params, optFns, c.addOperationGetApplicationMiddlewares)
	if err != nil {
		return nil, err
	}

	out := result.(*GetApplicationOutput)
	out.ResultMetadata = metadata
	return out, nil
}

type GetApplicationInput struct {

	// Specifies the ARN of the application to retrieve.
	//
	// This member is required.
	ApplicationArn *string

	noSmithyDocumentSerde
}

func (v *GetApplicationInput) Serialize(s smithy.ShapeSerializer) {
	s.WriteStruct(schemas.GetApplicationRequest)
	v.SerializeMembers(s)
	s.CloseStruct()
}

func (v *GetApplicationInput) SerializeMembers(s smithy.ShapeSerializer) {
	if v.ApplicationArn != nil {
		s.WriteString(schemas.GetApplicationRequest_applicationArn, *v.ApplicationArn)
	}
}

type GetApplicationOutput struct {

	// The date and time when the application was created.
	//
	// This member is required.
	CreatedAt *time.Time

	// The identity source details for the application, including the IAM Identity
	// Center instance configuration.
	//
	// This member is required.
	IdentitySource types.IdentitySourceDetails

	// The current status of the application.
	//
	// This member is required.
	Status types.Status

	// The date and time when the application was last updated.
	//
	// This member is required.
	UpdatedAt *time.Time

	// The error details if the application is in a failed state.
	Error *types.ErrorDetails

	// The tags associated with the application.
	Tags map[string]string

	// The tenant identifier associated with the application.
	TenantId *string

	// Metadata pertaining to the operation's result.
	ResultMetadata middleware.Metadata

	noSmithyDocumentSerde
}

func (v *GetApplicationOutput) Serialize(s smithy.ShapeSerializer) {
	s.WriteStruct(schemas.GetApplicationResponse)
	v.SerializeMembers(s)
	s.CloseStruct()
}

func (v *GetApplicationOutput) SerializeMembers(s smithy.ShapeSerializer) {
	if v.CreatedAt != nil {
		s.WriteTime(schemas.GetApplicationResponse_createdAt, *v.CreatedAt)
	}
	if v.Error != nil {
		s.WriteStruct(schemas.GetApplicationResponse_error)
		v.Error.SerializeMembers(s)
		s.CloseStruct()
	}
	serializeIdentitySourceDetails(s, schemas.GetApplicationResponse_identitySource, v.IdentitySource)
	if v.Status != "" {
		s.WriteString(schemas.GetApplicationResponse_status, string(v.Status))
	}
	serializeTagsMap(s, schemas.GetApplicationResponse_tags, v.Tags)
	if v.TenantId != nil {
		s.WriteString(schemas.GetApplicationResponse_tenantId, *v.TenantId)
	}
	if v.UpdatedAt != nil {
		s.WriteTime(schemas.GetApplicationResponse_updatedAt, *v.UpdatedAt)
	}
}
func (v *GetApplicationOutput) Deserialize(d smithy.ShapeDeserializer) error {
	return smithy.ReadStruct(d, schemas.GetApplicationResponse, func(s *smithy.Schema) error {
		switch s {
		case schemas.GetApplicationResponse_createdAt:
			v.CreatedAt = new(time.Time)
			return d.ReadTime(schemas.GetApplicationResponse_createdAt, v.CreatedAt)
		case schemas.GetApplicationResponse_error:
			v.Error = &types.ErrorDetails{}
			return v.Error.Deserialize(d)
		case schemas.GetApplicationResponse_identitySource:
			return deserializeIdentitySourceDetails(d, schemas.GetApplicationResponse_identitySource, &v.IdentitySource)
		case schemas.GetApplicationResponse_status:
			var ev string
			if err := d.ReadString(schemas.GetApplicationResponse_status, &ev); err != nil {
				return err
			}
			v.Status = types.Status(ev)
			return nil
		case schemas.GetApplicationResponse_tags:
			return deserializeTagsMap(d, schemas.GetApplicationResponse_tags, &v.Tags)
		case schemas.GetApplicationResponse_tenantId:
			v.TenantId = new(string)
			return d.ReadString(schemas.GetApplicationResponse_tenantId, v.TenantId)
		case schemas.GetApplicationResponse_updatedAt:
			v.UpdatedAt = new(time.Time)
			return d.ReadTime(schemas.GetApplicationResponse_updatedAt, v.UpdatedAt)
		}
		return nil
	})
}
func (c *Client) addOperationGetApplicationMiddlewares(stack *middleware.Stack, options Options) (err error) {
	if err := stack.Serialize.Add(&serializeRequestMiddleware{options: &options, operationSchema: smithy.NewOperationSchema(schemas.GetApplication, schemas.GetApplicationRequest, schemas.GetApplicationResponse)}, middleware.After); err != nil {
		return err
	}
	if err := stack.Deserialize.Add(&deserializeResponseMiddleware{options: &options, operationSchema: smithy.NewOperationSchema(schemas.GetApplication, schemas.GetApplicationRequest, schemas.GetApplicationResponse), output: &GetApplicationOutput{}}, middleware.After); err != nil {
		return err
	}

	if err = addResolveEndpointMiddleware(stack, options); err != nil {
		return err
	}
	if err = addComputePayloadSHA256(stack); err != nil {
		return err
	}
	if err = addRecordResponseTiming(stack, options); err != nil {
		return err
	}
	if err = addCredentialSource(stack, options); err != nil {
		return err
	}
	if err = addOpGetApplicationValidationMiddleware(stack); err != nil {
		return err
	}
	if err = addRequestIDRetrieverMiddleware(stack); err != nil {
		return err
	}
	if err = addResponseErrorMiddleware(stack); err != nil {
		return err
	}
	if err = addRequestResponseLogging(stack, options); err != nil {
		return err
	}
	if err = addDisableHTTPSMiddleware(stack, options); err != nil {
		return err
	}
	if err = addInterceptors(stack, options); err != nil {
		return err
	}
	return nil
}

// ApplicationActiveWaiterOptions are waiter options for ApplicationActiveWaiter
type ApplicationActiveWaiterOptions struct {

	// Set of options to modify how an operation is invoked. These apply to all
	// operations invoked for this client. Use functional options on operation call to
	// modify this list for per operation behavior.
	//
	// Passing options here is functionally equivalent to passing values to this
	// config's ClientOptions field that extend the inner client's APIOptions directly.
	APIOptions []func(*middleware.Stack) error

	// Functional options to be passed to all operations invoked by this client.
	//
	// Function values that modify the inner APIOptions are applied after the waiter
	// config's own APIOptions modifiers.
	ClientOptions []func(*Options)

	// MinDelay is the minimum amount of time to delay between retries. If unset,
	// ApplicationActiveWaiter will use default minimum delay of 5 seconds. Note that
	// MinDelay must resolve to a value lesser than or equal to the MaxDelay.
	MinDelay time.Duration

	// MaxDelay is the maximum amount of time to delay between retries. If unset or
	// set to zero, ApplicationActiveWaiter will use default max delay of 120 seconds.
	// Note that MaxDelay must resolve to value greater than or equal to the MinDelay.
	MaxDelay time.Duration

	// LogWaitAttempts is used to enable logging for waiter retry attempts
	LogWaitAttempts bool

	// Retryable is function that can be used to override the service defined
	// waiter-behavior based on operation output, or returned error. This function is
	// used by the waiter to decide if a state is retryable or a terminal state.
	//
	// By default service-modeled logic will populate this option. This option can
	// thus be used to define a custom waiter state with fall-back to service-modeled
	// waiter state mutators.The function returns an error in case of a failure state.
	// In case of retry state, this function returns a bool value of true and nil
	// error, while in case of success it returns a bool value of false and nil error.
	Retryable func(context.Context, *GetApplicationInput, *GetApplicationOutput, error) (bool, error)
}

// ApplicationActiveWaiter defines the waiters for ApplicationActive
type ApplicationActiveWaiter struct {
	client GetApplicationAPIClient

	options ApplicationActiveWaiterOptions
}

// NewApplicationActiveWaiter constructs a ApplicationActiveWaiter.
func NewApplicationActiveWaiter(client GetApplicationAPIClient, optFns ...func(*ApplicationActiveWaiterOptions)) *ApplicationActiveWaiter {
	options := ApplicationActiveWaiterOptions{}
	options.MinDelay = 5 * time.Second
	options.MaxDelay = 120 * time.Second
	options.Retryable = applicationActiveStateRetryable

	for _, fn := range optFns {
		fn(&options)
	}
	return &ApplicationActiveWaiter{
		client:  client,
		options: options,
	}
}

// Wait calls the waiter function for ApplicationActive waiter. The maxWaitDur is
// the maximum wait duration the waiter will wait. The maxWaitDur is required and
// must be greater than zero.
func (w *ApplicationActiveWaiter) Wait(ctx context.Context, params *GetApplicationInput, maxWaitDur time.Duration, optFns ...func(*ApplicationActiveWaiterOptions)) error {
	_, err := w.WaitForOutput(ctx, params, maxWaitDur, optFns...)
	return err
}

// WaitForOutput calls the waiter function for ApplicationActive waiter and
// returns the output of the successful operation. The maxWaitDur is the maximum
// wait duration the waiter will wait. The maxWaitDur is required and must be
// greater than zero.
func (w *ApplicationActiveWaiter) WaitForOutput(ctx context.Context, params *GetApplicationInput, maxWaitDur time.Duration, optFns ...func(*ApplicationActiveWaiterOptions)) (*GetApplicationOutput, error) {
	if maxWaitDur <= 0 {
		return nil, fmt.Errorf("maximum wait time for waiter must be greater than zero")
	}

	options := w.options
	for _, fn := range optFns {
		fn(&options)
	}

	if options.MaxDelay <= 0 {
		options.MaxDelay = 120 * time.Second
	}

	if options.MinDelay > options.MaxDelay {
		return nil, fmt.Errorf("minimum waiter delay %v must be lesser than or equal to maximum waiter delay of %v.", options.MinDelay, options.MaxDelay)
	}

	ctx, cancelFn := context.WithTimeout(ctx, maxWaitDur)
	defer cancelFn()

	logger := smithywaiter.Logger{}
	remainingTime := maxWaitDur

	var attempt int64
	for {

		attempt++
		apiOptions := options.APIOptions
		start := time.Now()

		if options.LogWaitAttempts {
			logger.Attempt = attempt
			apiOptions = append([]func(*middleware.Stack) error{}, options.APIOptions...)
			apiOptions = append(apiOptions, logger.AddLogger)
		}

		out, err := w.client.GetApplication(ctx, params, func(o *Options) {
			baseOpts := []func(*Options){
				addIsWaiterUserAgent,
			}
			o.APIOptions = append(o.APIOptions, apiOptions...)
			for _, opt := range baseOpts {
				opt(o)
			}
			for _, opt := range options.ClientOptions {
				opt(o)
			}
		})

		retryable, err := options.Retryable(ctx, params, out, err)
		if err != nil {
			return nil, err
		}
		if !retryable {
			return out, nil
		}

		remainingTime -= time.Since(start)
		if remainingTime < options.MinDelay || remainingTime <= 0 {
			break
		}

		// compute exponential backoff between waiter retries
		delay, err := smithywaiter.ComputeDelay(
			attempt, options.MinDelay, options.MaxDelay, remainingTime,
		)
		if err != nil {
			return nil, fmt.Errorf("error computing waiter delay, %w", err)
		}

		remainingTime -= delay
		// sleep for the delay amount before invoking a request
		if err := smithytime.SleepWithContext(ctx, delay); err != nil {
			return nil, fmt.Errorf("request cancelled while waiting, %w", err)
		}
	}
	return nil, fmt.Errorf("exceeded max wait time for ApplicationActive waiter")
}

func applicationActiveStateRetryable(ctx context.Context, input *GetApplicationInput, output *GetApplicationOutput, err error) (bool, error) {

	if err == nil {
		v1 := output.Status
		expectedValue := "ACTIVE"
		var pathValue string
		pathValue = string(v1)
		if pathValue == expectedValue {
			return false, nil
		}
	}

	if err == nil {
		v1 := output.Status
		expectedValue := "CREATE_FAILED"
		var pathValue string
		pathValue = string(v1)
		if pathValue == expectedValue {
			return false, fmt.Errorf("waiter state transitioned to Failure")
		}
	}

	if err == nil {
		v1 := output.Status
		expectedValue := "DELETE_FAILED"
		var pathValue string
		pathValue = string(v1)
		if pathValue == expectedValue {
			return false, fmt.Errorf("waiter state transitioned to Failure")
		}
	}

	if err != nil {
		return false, err
	}
	return true, nil
}

// GetApplicationAPIClient is a client that implements the GetApplication
// operation.
type GetApplicationAPIClient interface {
	GetApplication(context.Context, *GetApplicationInput, ...func(*Options)) (*GetApplicationOutput, error)
}

var _ GetApplicationAPIClient = (*Client)(nil)
