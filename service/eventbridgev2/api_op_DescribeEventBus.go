// Code generated by smithy-go-codegen DO NOT EDIT.

package eventbridgev2

import (
	"context"
	"errors"
	"fmt"
	"github.com/aws/aws-sdk-go-v2/service/eventbridgev2/schemas"
	"github.com/aws/aws-sdk-go-v2/service/eventbridgev2/types"
	smithy "github.com/aws/smithy-go"
	"github.com/aws/smithy-go/middleware"
	smithytime "github.com/aws/smithy-go/time"
	smithywaiter "github.com/aws/smithy-go/waiter"
	"time"
)

// Returns the full configuration and lifecycle state of an event bus.
func (c *Client) DescribeEventBus(ctx context.Context, params *DescribeEventBusInput, optFns ...func(*Options)) (*DescribeEventBusOutput, error) {
	if params == nil {
		params = &DescribeEventBusInput{}
	}

	result, metadata, err := c.invokeOperation(ctx, "DescribeEventBus", params, optFns, c.addOperationDescribeEventBusMiddlewares)
	if err != nil {
		return nil, err
	}

	out := result.(*DescribeEventBusOutput)
	out.ResultMetadata = metadata
	return out, nil
}

type DescribeEventBusInput struct {

	// The Amazon Resource Name (ARN) that uniquely identifies an event bus.
	//
	// This member is required.
	EventBusArn *string

	noSmithyDocumentSerde
}

func (v *DescribeEventBusInput) Serialize(s smithy.ShapeSerializer) {
	s.WriteStruct(schemas.DescribeEventBusRequest)
	v.SerializeMembers(s)
	s.CloseStruct()
}

func (v *DescribeEventBusInput) SerializeMembers(s smithy.ShapeSerializer) {
	if v.EventBusArn != nil {
		s.WriteString(schemas.DescribeEventBusRequest_EventBusArn, *v.EventBusArn)
	}
}
func (in *DescribeEventBusInput) bindEndpointParams(p *EndpointParameters) {

	p.EventBusArn = in.EventBusArn

}

type DescribeEventBusOutput struct {

	// The time the event bus was created.
	CreationTime *time.Time

	// Free-text description. Used by every resource type that has one: event buses,
	// subscribers, and event sources.
	Description *string

	// Encryption configuration for an event bus.
	EncryptionConfiguration *types.EncryptionConfiguration

	// The Amazon Resource Name (ARN) that uniquely identifies an event bus.
	EventBusArn *string

	// The time the event bus was last modified.
	LastModifiedTime *time.Time

	// Name of an event bus. The first character must be alphanumeric; the remaining
	// characters may also include '.', '-', and '_'. The grammar matches the name
	// segment of EventBusArn (event-busv2//), so every valid name can be represented
	// in the bus's ARN. The same type is used everywhere a bus name appears.
	Name *string

	// Lifecycle state of an event bus.
	//
	//   - CREATING: Bus creation is in progress.
	//   - CREATE_FAILED: Bus creation failed; see StateReason. The bus cannot be
	//   recovered or recreated in place: delete it with DeleteEventBus, then create a
	//   new bus.
	//   - ACTIVE: Bus is fully operational and can accept events.
	//   - UPDATING: Bus update is in progress.
	//   - UPDATE_FAILED: Bus update failed; the bus remains operational with its
	//   previous configuration.
	//   - DELETING: Bus deletion is actively in progress.
	//   - DELETE_FAILED: Bus deletion failed; see StateReason. Retry DeleteEventBus
	//   after removing subscribers and event sources.
	State types.BusState

	// Human-readable explanation of why an event bus is in its current State. Omitted
	// when the bus is in a normal operational state (ACTIVE). It stands in for the
	// error response an asynchronous failure cannot return, so it applies only to
	// resources with an asynchronous lifecycle: event buses. EventSources and
	// subscribers are provisioned synchronously and report failures directly on the
	// request.
	StateReason *string

	// Event storage configuration of an event bus, as returned on reads.
	StorageConfiguration *types.StorageConfigurationOutput

	// Metadata pertaining to the operation's result.
	ResultMetadata middleware.Metadata

	noSmithyDocumentSerde
}

func (v *DescribeEventBusOutput) Serialize(s smithy.ShapeSerializer) {
	s.WriteStruct(schemas.DescribeEventBusResponse)
	v.SerializeMembers(s)
	s.CloseStruct()
}

func (v *DescribeEventBusOutput) SerializeMembers(s smithy.ShapeSerializer) {
	if v.CreationTime != nil {
		s.WriteTime(schemas.DescribeEventBusResponse_CreationTime, *v.CreationTime)
	}
	if v.Description != nil {
		s.WriteString(schemas.DescribeEventBusResponse_Description, *v.Description)
	}
	if v.EncryptionConfiguration != nil {
		s.WriteStruct(schemas.DescribeEventBusResponse_EncryptionConfiguration)
		v.EncryptionConfiguration.SerializeMembers(s)
		s.CloseStruct()
	}
	if v.EventBusArn != nil {
		s.WriteString(schemas.DescribeEventBusResponse_EventBusArn, *v.EventBusArn)
	}
	if v.LastModifiedTime != nil {
		s.WriteTime(schemas.DescribeEventBusResponse_LastModifiedTime, *v.LastModifiedTime)
	}
	if v.Name != nil {
		s.WriteString(schemas.DescribeEventBusResponse_Name, *v.Name)
	}
	if v.State != "" {
		s.WriteString(schemas.DescribeEventBusResponse_State, string(v.State))
	}
	if v.StateReason != nil {
		s.WriteString(schemas.DescribeEventBusResponse_StateReason, *v.StateReason)
	}
	if v.StorageConfiguration != nil {
		s.WriteStruct(schemas.DescribeEventBusResponse_StorageConfiguration)
		v.StorageConfiguration.SerializeMembers(s)
		s.CloseStruct()
	}
}
func (v *DescribeEventBusOutput) Deserialize(d smithy.ShapeDeserializer) error {
	return smithy.ReadStruct(d, schemas.DescribeEventBusResponse, func(s *smithy.Schema) error {
		switch s {
		case schemas.DescribeEventBusResponse_CreationTime:
			v.CreationTime = new(time.Time)
			return d.ReadTime(schemas.DescribeEventBusResponse_CreationTime, v.CreationTime)
		case schemas.DescribeEventBusResponse_Description:
			v.Description = new(string)
			return d.ReadString(schemas.DescribeEventBusResponse_Description, v.Description)
		case schemas.DescribeEventBusResponse_EncryptionConfiguration:
			v.EncryptionConfiguration = &types.EncryptionConfiguration{}
			return v.EncryptionConfiguration.Deserialize(d)
		case schemas.DescribeEventBusResponse_EventBusArn:
			v.EventBusArn = new(string)
			return d.ReadString(schemas.DescribeEventBusResponse_EventBusArn, v.EventBusArn)
		case schemas.DescribeEventBusResponse_LastModifiedTime:
			v.LastModifiedTime = new(time.Time)
			return d.ReadTime(schemas.DescribeEventBusResponse_LastModifiedTime, v.LastModifiedTime)
		case schemas.DescribeEventBusResponse_Name:
			v.Name = new(string)
			return d.ReadString(schemas.DescribeEventBusResponse_Name, v.Name)
		case schemas.DescribeEventBusResponse_State:
			var ev string
			if err := d.ReadString(schemas.DescribeEventBusResponse_State, &ev); err != nil {
				return err
			}
			v.State = types.BusState(ev)
			return nil
		case schemas.DescribeEventBusResponse_StateReason:
			v.StateReason = new(string)
			return d.ReadString(schemas.DescribeEventBusResponse_StateReason, v.StateReason)
		case schemas.DescribeEventBusResponse_StorageConfiguration:
			v.StorageConfiguration = &types.StorageConfigurationOutput{}
			return v.StorageConfiguration.Deserialize(d)
		}
		return nil
	})
}
func (c *Client) addOperationDescribeEventBusMiddlewares(stack *middleware.Stack, options Options) (err error) {
	if err := stack.Serialize.Add(&serializeRequestMiddleware{options: &options, operationSchema: smithy.NewOperationSchema(schemas.DescribeEventBus, schemas.DescribeEventBusRequest, schemas.DescribeEventBusResponse)}, middleware.After); err != nil {
		return err
	}
	if err := stack.Deserialize.Add(&deserializeResponseMiddleware{options: &options, operationSchema: smithy.NewOperationSchema(schemas.DescribeEventBus, schemas.DescribeEventBusRequest, schemas.DescribeEventBusResponse), output: &DescribeEventBusOutput{}}, middleware.After); err != nil {
		return err
	}

	if err = addResolveEndpointMiddleware(stack, options); err != nil {
		return err
	}
	if err = addComputePayloadSHA256(stack); err != nil {
		return err
	}
	if err = addRecordResponseTiming(stack, options); err != nil {
		return err
	}
	if err = addUserAgentFeatureProtocolRPCV2CBOR(stack, options); err != nil {
		return err
	}
	if err = addUserAgentAccountIDEndpointMode(stack, options); err != nil {
		return err
	}
	if err = addCredentialSource(stack, options); err != nil {
		return err
	}
	if err = addOpDescribeEventBusValidationMiddleware(stack); err != nil {
		return err
	}
	if err = addRequestIDRetrieverMiddleware(stack); err != nil {
		return err
	}
	if err = addResponseErrorMiddleware(stack); err != nil {
		return err
	}
	if err = addRequestResponseLogging(stack, options); err != nil {
		return err
	}
	if err = addDisableHTTPSMiddleware(stack, options); err != nil {
		return err
	}
	if err = addInterceptors(stack, options); err != nil {
		return err
	}
	return nil
}

// EventBusActiveWaiterOptions are waiter options for EventBusActiveWaiter
type EventBusActiveWaiterOptions struct {

	// Set of options to modify how an operation is invoked. These apply to all
	// operations invoked for this client. Use functional options on operation call to
	// modify this list for per operation behavior.
	//
	// Passing options here is functionally equivalent to passing values to this
	// config's ClientOptions field that extend the inner client's APIOptions directly.
	APIOptions []func(*middleware.Stack) error

	// Functional options to be passed to all operations invoked by this client.
	//
	// Function values that modify the inner APIOptions are applied after the waiter
	// config's own APIOptions modifiers.
	ClientOptions []func(*Options)

	// MinDelay is the minimum amount of time to delay between retries. If unset,
	// EventBusActiveWaiter will use default minimum delay of 2 seconds. Note that
	// MinDelay must resolve to a value lesser than or equal to the MaxDelay.
	MinDelay time.Duration

	// MaxDelay is the maximum amount of time to delay between retries. If unset or
	// set to zero, EventBusActiveWaiter will use default max delay of 120 seconds.
	// Note that MaxDelay must resolve to value greater than or equal to the MinDelay.
	MaxDelay time.Duration

	// LogWaitAttempts is used to enable logging for waiter retry attempts
	LogWaitAttempts bool

	// Retryable is function that can be used to override the service defined
	// waiter-behavior based on operation output, or returned error. This function is
	// used by the waiter to decide if a state is retryable or a terminal state.
	//
	// By default service-modeled logic will populate this option. This option can
	// thus be used to define a custom waiter state with fall-back to service-modeled
	// waiter state mutators.The function returns an error in case of a failure state.
	// In case of retry state, this function returns a bool value of true and nil
	// error, while in case of success it returns a bool value of false and nil error.
	Retryable func(context.Context, *DescribeEventBusInput, *DescribeEventBusOutput, error) (bool, error)
}

// EventBusActiveWaiter defines the waiters for EventBusActive
type EventBusActiveWaiter struct {
	client DescribeEventBusAPIClient

	options EventBusActiveWaiterOptions
}

// NewEventBusActiveWaiter constructs a EventBusActiveWaiter.
func NewEventBusActiveWaiter(client DescribeEventBusAPIClient, optFns ...func(*EventBusActiveWaiterOptions)) *EventBusActiveWaiter {
	options := EventBusActiveWaiterOptions{}
	options.MinDelay = 2 * time.Second
	options.MaxDelay = 120 * time.Second
	options.Retryable = eventBusActiveStateRetryable

	for _, fn := range optFns {
		fn(&options)
	}
	return &EventBusActiveWaiter{
		client:  client,
		options: options,
	}
}

// Wait calls the waiter function for EventBusActive waiter. The maxWaitDur is the
// maximum wait duration the waiter will wait. The maxWaitDur is required and must
// be greater than zero.
func (w *EventBusActiveWaiter) Wait(ctx context.Context, params *DescribeEventBusInput, maxWaitDur time.Duration, optFns ...func(*EventBusActiveWaiterOptions)) error {
	_, err := w.WaitForOutput(ctx, params, maxWaitDur, optFns...)
	return err
}

// WaitForOutput calls the waiter function for EventBusActive waiter and returns
// the output of the successful operation. The maxWaitDur is the maximum wait
// duration the waiter will wait. The maxWaitDur is required and must be greater
// than zero.
func (w *EventBusActiveWaiter) WaitForOutput(ctx context.Context, params *DescribeEventBusInput, maxWaitDur time.Duration, optFns ...func(*EventBusActiveWaiterOptions)) (*DescribeEventBusOutput, error) {
	if maxWaitDur <= 0 {
		return nil, fmt.Errorf("maximum wait time for waiter must be greater than zero")
	}

	options := w.options
	for _, fn := range optFns {
		fn(&options)
	}

	if options.MaxDelay <= 0 {
		options.MaxDelay = 120 * time.Second
	}

	if options.MinDelay > options.MaxDelay {
		return nil, fmt.Errorf("minimum waiter delay %v must be lesser than or equal to maximum waiter delay of %v.", options.MinDelay, options.MaxDelay)
	}

	ctx, cancelFn := context.WithTimeout(ctx, maxWaitDur)
	defer cancelFn()

	logger := smithywaiter.Logger{}
	remainingTime := maxWaitDur

	var attempt int64
	for {

		attempt++
		apiOptions := options.APIOptions
		start := time.Now()

		if options.LogWaitAttempts {
			logger.Attempt = attempt
			apiOptions = append([]func(*middleware.Stack) error{}, options.APIOptions...)
			apiOptions = append(apiOptions, logger.AddLogger)
		}

		out, err := w.client.DescribeEventBus(ctx, params, func(o *Options) {
			baseOpts := []func(*Options){
				addIsWaiterUserAgent,
			}
			o.APIOptions = append(o.APIOptions, apiOptions...)
			for _, opt := range baseOpts {
				opt(o)
			}
			for _, opt := range options.ClientOptions {
				opt(o)
			}
		})

		retryable, err := options.Retryable(ctx, params, out, err)
		if err != nil {
			return nil, err
		}
		if !retryable {
			return out, nil
		}

		remainingTime -= time.Since(start)
		if remainingTime < options.MinDelay || remainingTime <= 0 {
			break
		}

		// compute exponential backoff between waiter retries
		delay, err := smithywaiter.ComputeDelay(
			attempt, options.MinDelay, options.MaxDelay, remainingTime,
		)
		if err != nil {
			return nil, fmt.Errorf("error computing waiter delay, %w", err)
		}

		remainingTime -= delay
		// sleep for the delay amount before invoking a request
		if err := smithytime.SleepWithContext(ctx, delay); err != nil {
			return nil, fmt.Errorf("request cancelled while waiting, %w", err)
		}
	}
	return nil, fmt.Errorf("exceeded max wait time for EventBusActive waiter")
}

func eventBusActiveStateRetryable(ctx context.Context, input *DescribeEventBusInput, output *DescribeEventBusOutput, err error) (bool, error) {

	if err == nil {
		v1 := output.State
		expectedValue := "CREATE_FAILED"
		var pathValue string
		pathValue = string(v1)
		if pathValue == expectedValue {
			return false, fmt.Errorf("waiter state transitioned to Failure")
		}
	}

	if err == nil {
		v1 := output.State
		expectedValue := "UPDATE_FAILED"
		var pathValue string
		pathValue = string(v1)
		if pathValue == expectedValue {
			return false, fmt.Errorf("waiter state transitioned to Failure")
		}
	}

	if err == nil {
		v1 := output.State
		expectedValue := "DELETE_FAILED"
		var pathValue string
		pathValue = string(v1)
		if pathValue == expectedValue {
			return false, fmt.Errorf("waiter state transitioned to Failure")
		}
	}

	if err == nil {
		v1 := output.State
		expectedValue := "ACTIVE"
		var pathValue string
		pathValue = string(v1)
		if pathValue == expectedValue {
			return false, nil
		}
	}

	if err != nil {
		return false, err
	}
	return true, nil
}

// EventBusDeletedWaiterOptions are waiter options for EventBusDeletedWaiter
type EventBusDeletedWaiterOptions struct {

	// Set of options to modify how an operation is invoked. These apply to all
	// operations invoked for this client. Use functional options on operation call to
	// modify this list for per operation behavior.
	//
	// Passing options here is functionally equivalent to passing values to this
	// config's ClientOptions field that extend the inner client's APIOptions directly.
	APIOptions []func(*middleware.Stack) error

	// Functional options to be passed to all operations invoked by this client.
	//
	// Function values that modify the inner APIOptions are applied after the waiter
	// config's own APIOptions modifiers.
	ClientOptions []func(*Options)

	// MinDelay is the minimum amount of time to delay between retries. If unset,
	// EventBusDeletedWaiter will use default minimum delay of 2 seconds. Note that
	// MinDelay must resolve to a value lesser than or equal to the MaxDelay.
	MinDelay time.Duration

	// MaxDelay is the maximum amount of time to delay between retries. If unset or
	// set to zero, EventBusDeletedWaiter will use default max delay of 120 seconds.
	// Note that MaxDelay must resolve to value greater than or equal to the MinDelay.
	MaxDelay time.Duration

	// LogWaitAttempts is used to enable logging for waiter retry attempts
	LogWaitAttempts bool

	// Retryable is function that can be used to override the service defined
	// waiter-behavior based on operation output, or returned error. This function is
	// used by the waiter to decide if a state is retryable or a terminal state.
	//
	// By default service-modeled logic will populate this option. This option can
	// thus be used to define a custom waiter state with fall-back to service-modeled
	// waiter state mutators.The function returns an error in case of a failure state.
	// In case of retry state, this function returns a bool value of true and nil
	// error, while in case of success it returns a bool value of false and nil error.
	Retryable func(context.Context, *DescribeEventBusInput, *DescribeEventBusOutput, error) (bool, error)
}

// EventBusDeletedWaiter defines the waiters for EventBusDeleted
type EventBusDeletedWaiter struct {
	client DescribeEventBusAPIClient

	options EventBusDeletedWaiterOptions
}

// NewEventBusDeletedWaiter constructs a EventBusDeletedWaiter.
func NewEventBusDeletedWaiter(client DescribeEventBusAPIClient, optFns ...func(*EventBusDeletedWaiterOptions)) *EventBusDeletedWaiter {
	options := EventBusDeletedWaiterOptions{}
	options.MinDelay = 2 * time.Second
	options.MaxDelay = 120 * time.Second
	options.Retryable = eventBusDeletedStateRetryable

	for _, fn := range optFns {
		fn(&options)
	}
	return &EventBusDeletedWaiter{
		client:  client,
		options: options,
	}
}

// Wait calls the waiter function for EventBusDeleted waiter. The maxWaitDur is
// the maximum wait duration the waiter will wait. The maxWaitDur is required and
// must be greater than zero.
func (w *EventBusDeletedWaiter) Wait(ctx context.Context, params *DescribeEventBusInput, maxWaitDur time.Duration, optFns ...func(*EventBusDeletedWaiterOptions)) error {
	_, err := w.WaitForOutput(ctx, params, maxWaitDur, optFns...)
	return err
}

// WaitForOutput calls the waiter function for EventBusDeleted waiter and returns
// the output of the successful operation. The maxWaitDur is the maximum wait
// duration the waiter will wait. The maxWaitDur is required and must be greater
// than zero.
func (w *EventBusDeletedWaiter) WaitForOutput(ctx context.Context, params *DescribeEventBusInput, maxWaitDur time.Duration, optFns ...func(*EventBusDeletedWaiterOptions)) (*DescribeEventBusOutput, error) {
	if maxWaitDur <= 0 {
		return nil, fmt.Errorf("maximum wait time for waiter must be greater than zero")
	}

	options := w.options
	for _, fn := range optFns {
		fn(&options)
	}

	if options.MaxDelay <= 0 {
		options.MaxDelay = 120 * time.Second
	}

	if options.MinDelay > options.MaxDelay {
		return nil, fmt.Errorf("minimum waiter delay %v must be lesser than or equal to maximum waiter delay of %v.", options.MinDelay, options.MaxDelay)
	}

	ctx, cancelFn := context.WithTimeout(ctx, maxWaitDur)
	defer cancelFn()

	logger := smithywaiter.Logger{}
	remainingTime := maxWaitDur

	var attempt int64
	for {

		attempt++
		apiOptions := options.APIOptions
		start := time.Now()

		if options.LogWaitAttempts {
			logger.Attempt = attempt
			apiOptions = append([]func(*middleware.Stack) error{}, options.APIOptions...)
			apiOptions = append(apiOptions, logger.AddLogger)
		}

		out, err := w.client.DescribeEventBus(ctx, params, func(o *Options) {
			baseOpts := []func(*Options){
				addIsWaiterUserAgent,
			}
			o.APIOptions = append(o.APIOptions, apiOptions...)
			for _, opt := range baseOpts {
				opt(o)
			}
			for _, opt := range options.ClientOptions {
				opt(o)
			}
		})

		retryable, err := options.Retryable(ctx, params, out, err)
		if err != nil {
			return nil, err
		}
		if !retryable {
			return out, nil
		}

		remainingTime -= time.Since(start)
		if remainingTime < options.MinDelay || remainingTime <= 0 {
			break
		}

		// compute exponential backoff between waiter retries
		delay, err := smithywaiter.ComputeDelay(
			attempt, options.MinDelay, options.MaxDelay, remainingTime,
		)
		if err != nil {
			return nil, fmt.Errorf("error computing waiter delay, %w", err)
		}

		remainingTime -= delay
		// sleep for the delay amount before invoking a request
		if err := smithytime.SleepWithContext(ctx, delay); err != nil {
			return nil, fmt.Errorf("request cancelled while waiting, %w", err)
		}
	}
	return nil, fmt.Errorf("exceeded max wait time for EventBusDeleted waiter")
}

func eventBusDeletedStateRetryable(ctx context.Context, input *DescribeEventBusInput, output *DescribeEventBusOutput, err error) (bool, error) {

	if err == nil {
		v1 := output.State
		expectedValue := "DELETE_FAILED"
		var pathValue string
		pathValue = string(v1)
		if pathValue == expectedValue {
			return false, fmt.Errorf("waiter state transitioned to Failure")
		}
	}

	if err != nil {
		var errorType *types.ResourceNotFoundException
		if errors.As(err, &errorType) {
			return false, nil
		}
	}

	if err != nil {
		return false, err
	}
	return true, nil
}

// DescribeEventBusAPIClient is a client that implements the DescribeEventBus
// operation.
type DescribeEventBusAPIClient interface {
	DescribeEventBus(context.Context, *DescribeEventBusInput, ...func(*Options)) (*DescribeEventBusOutput, error)
}

var _ DescribeEventBusAPIClient = (*Client)(nil)
