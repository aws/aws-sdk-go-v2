// Code generated by smithy-go-codegen DO NOT EDIT.

package securityagent

import (
	"context"
	"fmt"
	"github.com/aws/aws-sdk-go-v2/service/securityagent/types"
	"github.com/aws/smithy-go/middleware"
	smithyhttp "github.com/aws/smithy-go/transport/http"
)

// Lists all security requirement packs in the caller's account.
func (c *Client) ListSecurityRequirementPacks(ctx context.Context, params *ListSecurityRequirementPacksInput, optFns ...func(*Options)) (*ListSecurityRequirementPacksOutput, error) {
	if params == nil {
		params = &ListSecurityRequirementPacksInput{}
	}

	result, metadata, err := c.invokeOperation(ctx, "ListSecurityRequirementPacks", params, optFns, c.addOperationListSecurityRequirementPacksMiddlewares)
	if err != nil {
		return nil, err
	}

	out := result.(*ListSecurityRequirementPacksOutput)
	out.ResultMetadata = metadata
	return out, nil
}

type ListSecurityRequirementPacksInput struct {

	// The filter criteria for listing security requirement packs.
	Filter *types.ListSecurityRequirementPackFilter

	// The maximum number of results to return in a single request.
	MaxResults *int32

	// The pagination token from a previous request to retrieve the next page of
	// results.
	NextToken *string

	noSmithyDocumentSerde
}

type ListSecurityRequirementPacksOutput struct {

	// The list of security requirement pack summaries.
	//
	// This member is required.
	SecurityRequirementPackSummaries []types.SecurityRequirementPackSummary

	// The pagination token to use in a subsequent request to retrieve the next page
	// of results.
	NextToken *string

	// Metadata pertaining to the operation's result.
	ResultMetadata middleware.Metadata

	noSmithyDocumentSerde
}

func (c *Client) addOperationListSecurityRequirementPacksMiddlewares(stack *middleware.Stack, options Options) (err error) {
	err = stack.Serialize.Add(&awsRestjson1_serializeOpListSecurityRequirementPacks{}, middleware.After)
	if err != nil {
		return err
	}
	err = stack.Deserialize.Add(&awsRestjson1_deserializeOpListSecurityRequirementPacks{}, middleware.After)
	if err != nil {
		return err
	}

	if err = addlegacyEndpointContextSetter(stack, options); err != nil {
		return err
	}
	if err = addComputeContentLength(stack); err != nil {
		return err
	}
	if err = addResolveEndpointMiddleware(stack, options); err != nil {
		return err
	}
	if err = addComputePayloadSHA256(stack); err != nil {
		return err
	}
	if err = addRecordResponseTiming(stack); err != nil {
		return err
	}
	if err = smithyhttp.AddErrorCloseResponseBodyMiddleware(stack); err != nil {
		return err
	}
	if err = smithyhttp.AddCloseResponseBodyMiddleware(stack); err != nil {
		return err
	}
	if err = addCredentialSource(stack, options); err != nil {
		return err
	}
	if err = stack.Initialize.Add(newServiceMetadataMiddleware(options.Region, "ListSecurityRequirementPacks"), middleware.Before); err != nil {
		return err
	}
	if err = addRequestIDRetrieverMiddleware(stack); err != nil {
		return err
	}
	if err = addResponseErrorMiddleware(stack); err != nil {
		return err
	}
	if err = addRequestResponseLogging(stack, options); err != nil {
		return err
	}
	if err = addDisableHTTPSMiddleware(stack, options); err != nil {
		return err
	}
	if err = addInterceptors(stack, options); err != nil {
		return err
	}
	return nil
}

// ListSecurityRequirementPacksPaginatorOptions is the paginator options for
// ListSecurityRequirementPacks
type ListSecurityRequirementPacksPaginatorOptions struct {
	// The maximum number of results to return in a single request.
	Limit int32

	// Set to true if pagination should stop if the service returns a pagination token
	// that matches the most recent token provided to the service.
	StopOnDuplicateToken bool
}

// ListSecurityRequirementPacksPaginator is a paginator for
// ListSecurityRequirementPacks
type ListSecurityRequirementPacksPaginator struct {
	options   ListSecurityRequirementPacksPaginatorOptions
	client    ListSecurityRequirementPacksAPIClient
	params    *ListSecurityRequirementPacksInput
	nextToken *string
	firstPage bool
}

// NewListSecurityRequirementPacksPaginator returns a new
// ListSecurityRequirementPacksPaginator
func NewListSecurityRequirementPacksPaginator(client ListSecurityRequirementPacksAPIClient, params *ListSecurityRequirementPacksInput, optFns ...func(*ListSecurityRequirementPacksPaginatorOptions)) *ListSecurityRequirementPacksPaginator {
	if params == nil {
		params = &ListSecurityRequirementPacksInput{}
	}

	options := ListSecurityRequirementPacksPaginatorOptions{}
	if params.MaxResults != nil {
		options.Limit = *params.MaxResults
	}

	for _, fn := range optFns {
		fn(&options)
	}

	return &ListSecurityRequirementPacksPaginator{
		options:   options,
		client:    client,
		params:    params,
		firstPage: true,
		nextToken: params.NextToken,
	}
}

// HasMorePages returns a boolean indicating whether more pages are available
func (p *ListSecurityRequirementPacksPaginator) HasMorePages() bool {
	return p.firstPage || (p.nextToken != nil && len(*p.nextToken) != 0)
}

// NextPage retrieves the next ListSecurityRequirementPacks page.
func (p *ListSecurityRequirementPacksPaginator) NextPage(ctx context.Context, optFns ...func(*Options)) (*ListSecurityRequirementPacksOutput, error) {
	if !p.HasMorePages() {
		return nil, fmt.Errorf("no more pages available")
	}

	params := *p.params
	params.NextToken = p.nextToken

	var limit *int32
	if p.options.Limit > 0 {
		limit = &p.options.Limit
	}
	params.MaxResults = limit

	optFns = append([]func(*Options){
		addIsPaginatorUserAgent,
	}, optFns...)
	result, err := p.client.ListSecurityRequirementPacks(ctx, &params, optFns...)
	if err != nil {
		return nil, err
	}
	p.firstPage = false

	prevToken := p.nextToken
	p.nextToken = result.NextToken

	if p.options.StopOnDuplicateToken &&
		prevToken != nil &&
		p.nextToken != nil &&
		*prevToken == *p.nextToken {
		p.nextToken = nil
	}

	return result, nil
}

// ListSecurityRequirementPacksAPIClient is a client that implements the
// ListSecurityRequirementPacks operation.
type ListSecurityRequirementPacksAPIClient interface {
	ListSecurityRequirementPacks(context.Context, *ListSecurityRequirementPacksInput, ...func(*Options)) (*ListSecurityRequirementPacksOutput, error)
}

var _ ListSecurityRequirementPacksAPIClient = (*Client)(nil)
