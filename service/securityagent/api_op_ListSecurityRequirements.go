// Code generated by smithy-go-codegen DO NOT EDIT.

package securityagent

import (
	"context"
	"fmt"
	"github.com/aws/aws-sdk-go-v2/service/securityagent/types"
	"github.com/aws/smithy-go/middleware"
	smithyhttp "github.com/aws/smithy-go/transport/http"
)

// Lists security requirements within a pack.
func (c *Client) ListSecurityRequirements(ctx context.Context, params *ListSecurityRequirementsInput, optFns ...func(*Options)) (*ListSecurityRequirementsOutput, error) {
	if params == nil {
		params = &ListSecurityRequirementsInput{}
	}

	result, metadata, err := c.invokeOperation(ctx, "ListSecurityRequirements", params, optFns, c.addOperationListSecurityRequirementsMiddlewares)
	if err != nil {
		return nil, err
	}

	out := result.(*ListSecurityRequirementsOutput)
	out.ResultMetadata = metadata
	return out, nil
}

type ListSecurityRequirementsInput struct {

	// The unique identifier of the security requirement pack to list requirements for.
	//
	// This member is required.
	PackId *string

	// The maximum number of results to return in a single request.
	MaxResults *int32

	// The pagination token from a previous request to retrieve the next page of
	// results.
	NextToken *string

	noSmithyDocumentSerde
}

type ListSecurityRequirementsOutput struct {

	// The list of security requirement summaries.
	//
	// This member is required.
	SecurityRequirementSummaries []types.SecurityRequirementSummary

	// The pagination token to use in a subsequent request to retrieve the next page
	// of results.
	NextToken *string

	// Metadata pertaining to the operation's result.
	ResultMetadata middleware.Metadata

	noSmithyDocumentSerde
}

func (c *Client) addOperationListSecurityRequirementsMiddlewares(stack *middleware.Stack, options Options) (err error) {
	err = stack.Serialize.Add(&awsRestjson1_serializeOpListSecurityRequirements{}, middleware.After)
	if err != nil {
		return err
	}
	err = stack.Deserialize.Add(&awsRestjson1_deserializeOpListSecurityRequirements{}, middleware.After)
	if err != nil {
		return err
	}

	if err = addComputeContentLength(stack); err != nil {
		return err
	}
	if err = addResolveEndpointMiddleware(stack, options); err != nil {
		return err
	}
	if err = addComputePayloadSHA256(stack); err != nil {
		return err
	}
	if err = addRecordResponseTiming(stack); err != nil {
		return err
	}
	if err = smithyhttp.AddErrorCloseResponseBodyMiddleware(stack); err != nil {
		return err
	}
	if err = smithyhttp.AddCloseResponseBodyMiddleware(stack); err != nil {
		return err
	}
	if err = addCredentialSource(stack, options); err != nil {
		return err
	}
	if err = addOpListSecurityRequirementsValidationMiddleware(stack); err != nil {
		return err
	}
	if err = stack.Initialize.Add(newServiceMetadataMiddleware(options.Region, "ListSecurityRequirements", options.EndpointResolver != nil), middleware.Before); err != nil {
		return err
	}
	if err = addRequestIDRetrieverMiddleware(stack); err != nil {
		return err
	}
	if err = addResponseErrorMiddleware(stack); err != nil {
		return err
	}
	if err = addRequestResponseLogging(stack, options); err != nil {
		return err
	}
	if err = addDisableHTTPSMiddleware(stack, options); err != nil {
		return err
	}
	if err = addInterceptors(stack, options); err != nil {
		return err
	}
	return nil
}

// ListSecurityRequirementsPaginatorOptions is the paginator options for
// ListSecurityRequirements
type ListSecurityRequirementsPaginatorOptions struct {
	// The maximum number of results to return in a single request.
	Limit int32

	// Set to true if pagination should stop if the service returns a pagination token
	// that matches the most recent token provided to the service.
	StopOnDuplicateToken bool
}

// ListSecurityRequirementsPaginator is a paginator for ListSecurityRequirements
type ListSecurityRequirementsPaginator struct {
	options   ListSecurityRequirementsPaginatorOptions
	client    ListSecurityRequirementsAPIClient
	params    *ListSecurityRequirementsInput
	nextToken *string
	firstPage bool
}

// NewListSecurityRequirementsPaginator returns a new
// ListSecurityRequirementsPaginator
func NewListSecurityRequirementsPaginator(client ListSecurityRequirementsAPIClient, params *ListSecurityRequirementsInput, optFns ...func(*ListSecurityRequirementsPaginatorOptions)) *ListSecurityRequirementsPaginator {
	if params == nil {
		params = &ListSecurityRequirementsInput{}
	}

	options := ListSecurityRequirementsPaginatorOptions{}
	if params.MaxResults != nil {
		options.Limit = *params.MaxResults
	}

	for _, fn := range optFns {
		fn(&options)
	}

	return &ListSecurityRequirementsPaginator{
		options:   options,
		client:    client,
		params:    params,
		firstPage: true,
		nextToken: params.NextToken,
	}
}

// HasMorePages returns a boolean indicating whether more pages are available
func (p *ListSecurityRequirementsPaginator) HasMorePages() bool {
	return p.firstPage || (p.nextToken != nil && len(*p.nextToken) != 0)
}

// NextPage retrieves the next ListSecurityRequirements page.
func (p *ListSecurityRequirementsPaginator) NextPage(ctx context.Context, optFns ...func(*Options)) (*ListSecurityRequirementsOutput, error) {
	if !p.HasMorePages() {
		return nil, fmt.Errorf("no more pages available")
	}

	params := *p.params
	params.NextToken = p.nextToken

	var limit *int32
	if p.options.Limit > 0 {
		limit = &p.options.Limit
	}
	params.MaxResults = limit

	optFns = append([]func(*Options){
		addIsPaginatorUserAgent,
	}, optFns...)
	result, err := p.client.ListSecurityRequirements(ctx, &params, optFns...)
	if err != nil {
		return nil, err
	}
	p.firstPage = false

	prevToken := p.nextToken
	p.nextToken = result.NextToken

	if p.options.StopOnDuplicateToken &&
		prevToken != nil &&
		p.nextToken != nil &&
		*prevToken == *p.nextToken {
		p.nextToken = nil
	}

	return result, nil
}

// ListSecurityRequirementsAPIClient is a client that implements the
// ListSecurityRequirements operation.
type ListSecurityRequirementsAPIClient interface {
	ListSecurityRequirements(context.Context, *ListSecurityRequirementsInput, ...func(*Options)) (*ListSecurityRequirementsOutput, error)
}

var _ ListSecurityRequirementsAPIClient = (*Client)(nil)
