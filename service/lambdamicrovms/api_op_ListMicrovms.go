// Code generated by smithy-go-codegen DO NOT EDIT.

package lambdamicrovms

import (
	"context"
	"fmt"
	"github.com/aws/aws-sdk-go-v2/service/lambdamicrovms/types"
	"github.com/aws/smithy-go/middleware"
	smithyhttp "github.com/aws/smithy-go/transport/http"
)

// Lists MicroVMs in the account with optional filtering by image and version. We
// recommend using pagination to ensure that the operation returns quickly and
// successfully.
func (c *Client) ListMicrovms(ctx context.Context, params *ListMicrovmsInput, optFns ...func(*Options)) (*ListMicrovmsOutput, error) {
	if params == nil {
		params = &ListMicrovmsInput{}
	}

	result, metadata, err := c.invokeOperation(ctx, "ListMicrovms", params, optFns, c.addOperationListMicrovmsMiddlewares)
	if err != nil {
		return nil, err
	}

	out := result.(*ListMicrovmsOutput)
	out.ResultMetadata = metadata
	return out, nil
}

type ListMicrovmsInput struct {

	// Optional filter to list only MicroVMs running the specified image.
	ImageIdentifier *string

	// Optional filter to list only MicroVMs running the specified image version.
	ImageVersion *string

	// The maximum number of results to return in a single call.
	MaxResults *int32

	// The pagination token from a previous call. Use this token to retrieve the next
	// page of results.
	NextToken *string

	noSmithyDocumentSerde
}

type ListMicrovmsOutput struct {

	// The list of MicroVMs.
	//
	// This member is required.
	Items []types.MicrovmItem

	// The pagination token to use in a subsequent request to retrieve the next page
	// of results. This value is null when there are no more results to return.
	NextToken *string

	// Metadata pertaining to the operation's result.
	ResultMetadata middleware.Metadata

	noSmithyDocumentSerde
}

func (c *Client) addOperationListMicrovmsMiddlewares(stack *middleware.Stack, options Options) (err error) {
	err = stack.Serialize.Add(&awsRestjson1_serializeOpListMicrovms{}, middleware.After)
	if err != nil {
		return err
	}
	err = stack.Deserialize.Add(&awsRestjson1_deserializeOpListMicrovms{}, middleware.After)
	if err != nil {
		return err
	}

	if err = addComputeContentLength(stack); err != nil {
		return err
	}
	if err = addResolveEndpointMiddleware(stack, options); err != nil {
		return err
	}
	if err = addComputePayloadSHA256(stack); err != nil {
		return err
	}
	if err = addRecordResponseTiming(stack); err != nil {
		return err
	}
	if err = smithyhttp.AddErrorCloseResponseBodyMiddleware(stack); err != nil {
		return err
	}
	if err = smithyhttp.AddCloseResponseBodyMiddleware(stack); err != nil {
		return err
	}
	if err = addCredentialSource(stack, options); err != nil {
		return err
	}
	if err = stack.Initialize.Add(newServiceMetadataMiddleware(options.Region, "ListMicrovms", options.EndpointResolver != nil), middleware.Before); err != nil {
		return err
	}
	if err = addRequestIDRetrieverMiddleware(stack); err != nil {
		return err
	}
	if err = addResponseErrorMiddleware(stack); err != nil {
		return err
	}
	if err = addRequestResponseLogging(stack, options); err != nil {
		return err
	}
	if err = addDisableHTTPSMiddleware(stack, options); err != nil {
		return err
	}
	if err = addInterceptors(stack, options); err != nil {
		return err
	}
	return nil
}

// ListMicrovmsPaginatorOptions is the paginator options for ListMicrovms
type ListMicrovmsPaginatorOptions struct {
	// The maximum number of results to return in a single call.
	Limit int32

	// Set to true if pagination should stop if the service returns a pagination token
	// that matches the most recent token provided to the service.
	StopOnDuplicateToken bool
}

// ListMicrovmsPaginator is a paginator for ListMicrovms
type ListMicrovmsPaginator struct {
	options   ListMicrovmsPaginatorOptions
	client    ListMicrovmsAPIClient
	params    *ListMicrovmsInput
	nextToken *string
	firstPage bool
}

// NewListMicrovmsPaginator returns a new ListMicrovmsPaginator
func NewListMicrovmsPaginator(client ListMicrovmsAPIClient, params *ListMicrovmsInput, optFns ...func(*ListMicrovmsPaginatorOptions)) *ListMicrovmsPaginator {
	if params == nil {
		params = &ListMicrovmsInput{}
	}

	options := ListMicrovmsPaginatorOptions{}
	if params.MaxResults != nil {
		options.Limit = *params.MaxResults
	}

	for _, fn := range optFns {
		fn(&options)
	}

	return &ListMicrovmsPaginator{
		options:   options,
		client:    client,
		params:    params,
		firstPage: true,
		nextToken: params.NextToken,
	}
}

// HasMorePages returns a boolean indicating whether more pages are available
func (p *ListMicrovmsPaginator) HasMorePages() bool {
	return p.firstPage || (p.nextToken != nil && len(*p.nextToken) != 0)
}

// NextPage retrieves the next ListMicrovms page.
func (p *ListMicrovmsPaginator) NextPage(ctx context.Context, optFns ...func(*Options)) (*ListMicrovmsOutput, error) {
	if !p.HasMorePages() {
		return nil, fmt.Errorf("no more pages available")
	}

	params := *p.params
	params.NextToken = p.nextToken

	var limit *int32
	if p.options.Limit > 0 {
		limit = &p.options.Limit
	}
	params.MaxResults = limit

	optFns = append([]func(*Options){
		addIsPaginatorUserAgent,
	}, optFns...)
	result, err := p.client.ListMicrovms(ctx, &params, optFns...)
	if err != nil {
		return nil, err
	}
	p.firstPage = false

	prevToken := p.nextToken
	p.nextToken = result.NextToken

	if p.options.StopOnDuplicateToken &&
		prevToken != nil &&
		p.nextToken != nil &&
		*prevToken == *p.nextToken {
		p.nextToken = nil
	}

	return result, nil
}

// ListMicrovmsAPIClient is a client that implements the ListMicrovms operation.
type ListMicrovmsAPIClient interface {
	ListMicrovms(context.Context, *ListMicrovmsInput, ...func(*Options)) (*ListMicrovmsOutput, error)
}

var _ ListMicrovmsAPIClient = (*Client)(nil)
