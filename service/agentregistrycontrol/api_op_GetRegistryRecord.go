// Code generated by smithy-go-codegen DO NOT EDIT.

package agentregistrycontrol

import (
	"context"
	"fmt"
	"github.com/aws/aws-sdk-go-v2/service/agentregistrycontrol/schemas"
	"github.com/aws/aws-sdk-go-v2/service/agentregistrycontrol/types"
	smithy "github.com/aws/smithy-go"
	"github.com/aws/smithy-go/middleware"
	smithytime "github.com/aws/smithy-go/time"
	smithyhttp "github.com/aws/smithy-go/transport/http"
	smithywaiter "github.com/aws/smithy-go/waiter"
	"time"
)

// Retrieves the details of a registry record
func (c *Client) GetRegistryRecord(ctx context.Context, params *GetRegistryRecordInput, optFns ...func(*Options)) (*GetRegistryRecordOutput, error) {
	if params == nil {
		params = &GetRegistryRecordInput{}
	}

	result, metadata, err := c.invokeOperation(ctx, "GetRegistryRecord", params, optFns, c.addOperationGetRegistryRecordMiddlewares)
	if err != nil {
		return nil, err
	}

	out := result.(*GetRegistryRecordOutput)
	out.ResultMetadata = metadata
	return out, nil
}

type GetRegistryRecordInput struct {

	// The identifier of the registry record to retrieve (ARN or ID)
	//
	// This member is required.
	RecordId *string

	// The identifier of the registry containing the record (ARN or ID)
	//
	// This member is required.
	RegistryId *string

	noSmithyDocumentSerde
}

func (v *GetRegistryRecordInput) Serialize(s smithy.ShapeSerializer) {
	s.WriteStruct(schemas.GetRegistryRecordRequest)
	v.SerializeMembers(s)
	s.CloseStruct()
}

func (v *GetRegistryRecordInput) SerializeMembers(s smithy.ShapeSerializer) {
	if v.RecordId != nil {
		s.WriteString(schemas.GetRegistryRecordRequest_recordId, *v.RecordId)
	}
	if v.RegistryId != nil {
		s.WriteString(schemas.GetRegistryRecordRequest_registryId, *v.RegistryId)
	}
}

// Response structure for retrieving a registry record
type GetRegistryRecordOutput struct {

	// The timestamp when the registry record was created.
	//
	// This member is required.
	CreatedAt *time.Time

	// The name of the registry record. Names are unique within a registry.
	//
	// This member is required.
	Name *string

	// The &ARN; of the registry record.
	//
	// This member is required.
	RecordArn *string

	// The unique identifier of the registry record.
	//
	// This member is required.
	RecordId *string

	// The type of the registry record, such as MCP, AGENT, SKILL, or CUSTOM.
	//
	// This member is required.
	RecordType types.RecordType

	// The &ARN; of the parent registry that owns the record.
	//
	// This member is required.
	RegistryArn *string

	// The lifecycle status of the registry record.
	//
	// This member is required.
	Status types.RegistryRecordStatus

	// The timestamp when the registry record was last updated.
	//
	// This member is required.
	UpdatedAt *time.Time

	// A description of the registry record.
	Description *string

	// The typed descriptors that define the content of the registry record.
	Descriptors *types.Descriptors

	// The human-readable display name of the registry record.
	DisplayName *string

	// The version identifier of the registry record.
	RecordVersion *string

	// The reason for the current status. Typically populated when the status
	// indicates a failure state.
	StatusReason *string

	// Metadata pertaining to the operation's result.
	ResultMetadata middleware.Metadata

	noSmithyDocumentSerde
}

func (v *GetRegistryRecordOutput) Serialize(s smithy.ShapeSerializer) {
	s.WriteStruct(schemas.GetRegistryRecordResponse)
	v.SerializeMembers(s)
	s.CloseStruct()
}

func (v *GetRegistryRecordOutput) SerializeMembers(s smithy.ShapeSerializer) {
	if v.CreatedAt != nil {
		s.WriteTime(schemas.GetRegistryRecordResponse_createdAt, *v.CreatedAt)
	}
	if v.Description != nil {
		s.WriteString(schemas.GetRegistryRecordResponse_description, *v.Description)
	}
	if v.Descriptors != nil {
		s.WriteStruct(schemas.GetRegistryRecordResponse_descriptors)
		v.Descriptors.SerializeMembers(s)
		s.CloseStruct()
	}
	if v.DisplayName != nil {
		s.WriteString(schemas.GetRegistryRecordResponse_displayName, *v.DisplayName)
	}
	if v.Name != nil {
		s.WriteString(schemas.GetRegistryRecordResponse_name, *v.Name)
	}
	if v.RecordArn != nil {
		s.WriteString(schemas.GetRegistryRecordResponse_recordArn, *v.RecordArn)
	}
	if v.RecordId != nil {
		s.WriteString(schemas.GetRegistryRecordResponse_recordId, *v.RecordId)
	}
	if v.RecordType != "" {
		s.WriteString(schemas.GetRegistryRecordResponse_recordType, string(v.RecordType))
	}
	if v.RecordVersion != nil {
		s.WriteString(schemas.GetRegistryRecordResponse_recordVersion, *v.RecordVersion)
	}
	if v.RegistryArn != nil {
		s.WriteString(schemas.GetRegistryRecordResponse_registryArn, *v.RegistryArn)
	}
	if v.Status != "" {
		s.WriteString(schemas.GetRegistryRecordResponse_status, string(v.Status))
	}
	if v.StatusReason != nil {
		s.WriteString(schemas.GetRegistryRecordResponse_statusReason, *v.StatusReason)
	}
	if v.UpdatedAt != nil {
		s.WriteTime(schemas.GetRegistryRecordResponse_updatedAt, *v.UpdatedAt)
	}
}
func (v *GetRegistryRecordOutput) Deserialize(d smithy.ShapeDeserializer) error {
	return smithy.ReadStruct(d, schemas.GetRegistryRecordResponse, func(s *smithy.Schema) error {
		switch s {
		case schemas.GetRegistryRecordResponse_createdAt:
			v.CreatedAt = new(time.Time)
			return d.ReadTime(schemas.GetRegistryRecordResponse_createdAt, v.CreatedAt)
		case schemas.GetRegistryRecordResponse_description:
			v.Description = new(string)
			return d.ReadString(schemas.GetRegistryRecordResponse_description, v.Description)
		case schemas.GetRegistryRecordResponse_descriptors:
			v.Descriptors = &types.Descriptors{}
			return v.Descriptors.Deserialize(d)
		case schemas.GetRegistryRecordResponse_displayName:
			v.DisplayName = new(string)
			return d.ReadString(schemas.GetRegistryRecordResponse_displayName, v.DisplayName)
		case schemas.GetRegistryRecordResponse_name:
			v.Name = new(string)
			return d.ReadString(schemas.GetRegistryRecordResponse_name, v.Name)
		case schemas.GetRegistryRecordResponse_recordArn:
			v.RecordArn = new(string)
			return d.ReadString(schemas.GetRegistryRecordResponse_recordArn, v.RecordArn)
		case schemas.GetRegistryRecordResponse_recordId:
			v.RecordId = new(string)
			return d.ReadString(schemas.GetRegistryRecordResponse_recordId, v.RecordId)
		case schemas.GetRegistryRecordResponse_recordType:
			var ev string
			if err := d.ReadString(schemas.GetRegistryRecordResponse_recordType, &ev); err != nil {
				return err
			}
			v.RecordType = types.RecordType(ev)
			return nil
		case schemas.GetRegistryRecordResponse_recordVersion:
			v.RecordVersion = new(string)
			return d.ReadString(schemas.GetRegistryRecordResponse_recordVersion, v.RecordVersion)
		case schemas.GetRegistryRecordResponse_registryArn:
			v.RegistryArn = new(string)
			return d.ReadString(schemas.GetRegistryRecordResponse_registryArn, v.RegistryArn)
		case schemas.GetRegistryRecordResponse_status:
			var ev string
			if err := d.ReadString(schemas.GetRegistryRecordResponse_status, &ev); err != nil {
				return err
			}
			v.Status = types.RegistryRecordStatus(ev)
			return nil
		case schemas.GetRegistryRecordResponse_statusReason:
			v.StatusReason = new(string)
			return d.ReadString(schemas.GetRegistryRecordResponse_statusReason, v.StatusReason)
		case schemas.GetRegistryRecordResponse_updatedAt:
			v.UpdatedAt = new(time.Time)
			return d.ReadTime(schemas.GetRegistryRecordResponse_updatedAt, v.UpdatedAt)
		}
		return nil
	})
}
func (c *Client) addOperationGetRegistryRecordMiddlewares(stack *middleware.Stack, options Options) (err error) {
	if err := stack.Serialize.Add(&serializeRequestMiddleware{options: &options, operationSchema: smithy.NewOperationSchema(schemas.GetRegistryRecord, schemas.GetRegistryRecordRequest, schemas.GetRegistryRecordResponse)}, middleware.After); err != nil {
		return err
	}
	if err := stack.Deserialize.Add(&deserializeResponseMiddleware{options: &options, operationSchema: smithy.NewOperationSchema(schemas.GetRegistryRecord, schemas.GetRegistryRecordRequest, schemas.GetRegistryRecordResponse), output: &GetRegistryRecordOutput{}}, middleware.After); err != nil {
		return err
	}

	if err = addlegacyEndpointContextSetter(stack, options); err != nil {
		return err
	}
	if err = addComputeContentLength(stack); err != nil {
		return err
	}
	if err = addResolveEndpointMiddleware(stack, options); err != nil {
		return err
	}
	if err = addComputePayloadSHA256(stack); err != nil {
		return err
	}
	if err = addRecordResponseTiming(stack, options); err != nil {
		return err
	}
	if err = smithyhttp.AddErrorCloseResponseBodyMiddleware(stack); err != nil {
		return err
	}
	if err = smithyhttp.AddCloseResponseBodyMiddleware(stack); err != nil {
		return err
	}
	if err = addCredentialSource(stack, options); err != nil {
		return err
	}
	if err = addOpGetRegistryRecordValidationMiddleware(stack); err != nil {
		return err
	}
	if err = stack.Initialize.Add(newServiceMetadataMiddleware(options.Region, "GetRegistryRecord"), middleware.Before); err != nil {
		return err
	}
	if err = addRequestIDRetrieverMiddleware(stack); err != nil {
		return err
	}
	if err = addResponseErrorMiddleware(stack); err != nil {
		return err
	}
	if err = addRequestResponseLogging(stack, options); err != nil {
		return err
	}
	if err = addDisableHTTPSMiddleware(stack, options); err != nil {
		return err
	}
	if err = addInterceptors(stack, options); err != nil {
		return err
	}
	return nil
}

// RegistryRecordApprovedWaiterOptions are waiter options for
// RegistryRecordApprovedWaiter
type RegistryRecordApprovedWaiterOptions struct {

	// Set of options to modify how an operation is invoked. These apply to all
	// operations invoked for this client. Use functional options on operation call to
	// modify this list for per operation behavior.
	//
	// Passing options here is functionally equivalent to passing values to this
	// config's ClientOptions field that extend the inner client's APIOptions directly.
	APIOptions []func(*middleware.Stack) error

	// Functional options to be passed to all operations invoked by this client.
	//
	// Function values that modify the inner APIOptions are applied after the waiter
	// config's own APIOptions modifiers.
	ClientOptions []func(*Options)

	// MinDelay is the minimum amount of time to delay between retries. If unset,
	// RegistryRecordApprovedWaiter will use default minimum delay of 30 seconds. Note
	// that MinDelay must resolve to a value lesser than or equal to the MaxDelay.
	MinDelay time.Duration

	// MaxDelay is the maximum amount of time to delay between retries. If unset or
	// set to zero, RegistryRecordApprovedWaiter will use default max delay of 120
	// seconds. Note that MaxDelay must resolve to value greater than or equal to the
	// MinDelay.
	MaxDelay time.Duration

	// LogWaitAttempts is used to enable logging for waiter retry attempts
	LogWaitAttempts bool

	// Retryable is function that can be used to override the service defined
	// waiter-behavior based on operation output, or returned error. This function is
	// used by the waiter to decide if a state is retryable or a terminal state.
	//
	// By default service-modeled logic will populate this option. This option can
	// thus be used to define a custom waiter state with fall-back to service-modeled
	// waiter state mutators.The function returns an error in case of a failure state.
	// In case of retry state, this function returns a bool value of true and nil
	// error, while in case of success it returns a bool value of false and nil error.
	Retryable func(context.Context, *GetRegistryRecordInput, *GetRegistryRecordOutput, error) (bool, error)
}

// RegistryRecordApprovedWaiter defines the waiters for RegistryRecordApproved
type RegistryRecordApprovedWaiter struct {
	client GetRegistryRecordAPIClient

	options RegistryRecordApprovedWaiterOptions
}

// NewRegistryRecordApprovedWaiter constructs a RegistryRecordApprovedWaiter.
func NewRegistryRecordApprovedWaiter(client GetRegistryRecordAPIClient, optFns ...func(*RegistryRecordApprovedWaiterOptions)) *RegistryRecordApprovedWaiter {
	options := RegistryRecordApprovedWaiterOptions{}
	options.MinDelay = 30 * time.Second
	options.MaxDelay = 120 * time.Second
	options.Retryable = registryRecordApprovedStateRetryable

	for _, fn := range optFns {
		fn(&options)
	}
	return &RegistryRecordApprovedWaiter{
		client:  client,
		options: options,
	}
}

// Wait calls the waiter function for RegistryRecordApproved waiter. The
// maxWaitDur is the maximum wait duration the waiter will wait. The maxWaitDur is
// required and must be greater than zero.
func (w *RegistryRecordApprovedWaiter) Wait(ctx context.Context, params *GetRegistryRecordInput, maxWaitDur time.Duration, optFns ...func(*RegistryRecordApprovedWaiterOptions)) error {
	_, err := w.WaitForOutput(ctx, params, maxWaitDur, optFns...)
	return err
}

// WaitForOutput calls the waiter function for RegistryRecordApproved waiter and
// returns the output of the successful operation. The maxWaitDur is the maximum
// wait duration the waiter will wait. The maxWaitDur is required and must be
// greater than zero.
func (w *RegistryRecordApprovedWaiter) WaitForOutput(ctx context.Context, params *GetRegistryRecordInput, maxWaitDur time.Duration, optFns ...func(*RegistryRecordApprovedWaiterOptions)) (*GetRegistryRecordOutput, error) {
	if maxWaitDur <= 0 {
		return nil, fmt.Errorf("maximum wait time for waiter must be greater than zero")
	}

	options := w.options
	for _, fn := range optFns {
		fn(&options)
	}

	if options.MaxDelay <= 0 {
		options.MaxDelay = 120 * time.Second
	}

	if options.MinDelay > options.MaxDelay {
		return nil, fmt.Errorf("minimum waiter delay %v must be lesser than or equal to maximum waiter delay of %v.", options.MinDelay, options.MaxDelay)
	}

	ctx, cancelFn := context.WithTimeout(ctx, maxWaitDur)
	defer cancelFn()

	logger := smithywaiter.Logger{}
	remainingTime := maxWaitDur

	var attempt int64
	for {

		attempt++
		apiOptions := options.APIOptions
		start := time.Now()

		if options.LogWaitAttempts {
			logger.Attempt = attempt
			apiOptions = append([]func(*middleware.Stack) error{}, options.APIOptions...)
			apiOptions = append(apiOptions, logger.AddLogger)
		}

		out, err := w.client.GetRegistryRecord(ctx, params, func(o *Options) {
			baseOpts := []func(*Options){
				addIsWaiterUserAgent,
			}
			o.APIOptions = append(o.APIOptions, apiOptions...)
			for _, opt := range baseOpts {
				opt(o)
			}
			for _, opt := range options.ClientOptions {
				opt(o)
			}
		})

		retryable, err := options.Retryable(ctx, params, out, err)
		if err != nil {
			return nil, err
		}
		if !retryable {
			return out, nil
		}

		remainingTime -= time.Since(start)
		if remainingTime < options.MinDelay || remainingTime <= 0 {
			break
		}

		// compute exponential backoff between waiter retries
		delay, err := smithywaiter.ComputeDelay(
			attempt, options.MinDelay, options.MaxDelay, remainingTime,
		)
		if err != nil {
			return nil, fmt.Errorf("error computing waiter delay, %w", err)
		}

		remainingTime -= delay
		// sleep for the delay amount before invoking a request
		if err := smithytime.SleepWithContext(ctx, delay); err != nil {
			return nil, fmt.Errorf("request cancelled while waiting, %w", err)
		}
	}
	return nil, fmt.Errorf("exceeded max wait time for RegistryRecordApproved waiter")
}

func registryRecordApprovedStateRetryable(ctx context.Context, input *GetRegistryRecordInput, output *GetRegistryRecordOutput, err error) (bool, error) {

	if err == nil {
		v1 := output.Status
		expectedValue := "APPROVED"
		var pathValue string
		pathValue = string(v1)
		if pathValue == expectedValue {
			return false, nil
		}
	}

	if err == nil {
		v1 := output.Status
		expectedValue := "REJECTED"
		var pathValue string
		pathValue = string(v1)
		if pathValue == expectedValue {
			return false, fmt.Errorf("waiter state transitioned to Failure")
		}
	}

	if err == nil {
		v1 := output.Status
		expectedValue := "CREATE_FAILED"
		var pathValue string
		pathValue = string(v1)
		if pathValue == expectedValue {
			return false, fmt.Errorf("waiter state transitioned to Failure")
		}
	}

	if err == nil {
		v1 := output.Status
		expectedValue := "UPDATE_FAILED"
		var pathValue string
		pathValue = string(v1)
		if pathValue == expectedValue {
			return false, fmt.Errorf("waiter state transitioned to Failure")
		}
	}

	if err != nil {
		return false, err
	}
	return true, nil
}

// GetRegistryRecordAPIClient is a client that implements the GetRegistryRecord
// operation.
type GetRegistryRecordAPIClient interface {
	GetRegistryRecord(context.Context, *GetRegistryRecordInput, ...func(*Options)) (*GetRegistryRecordOutput, error)
}

var _ GetRegistryRecordAPIClient = (*Client)(nil)
