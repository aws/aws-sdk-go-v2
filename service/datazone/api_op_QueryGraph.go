// Code generated by smithy-go-codegen DO NOT EDIT.

package datazone

import (
	"context"
	"fmt"
	awsmiddleware "github.com/aws/aws-sdk-go-v2/aws/middleware"
	"github.com/aws/aws-sdk-go-v2/service/datazone/types"
	"github.com/aws/smithy-go/middleware"
	smithyhttp "github.com/aws/smithy-go/transport/http"
)

// Queries entities in the graph store.
func (c *Client) QueryGraph(ctx context.Context, params *QueryGraphInput, optFns ...func(*Options)) (*QueryGraphOutput, error) {
	if params == nil {
		params = &QueryGraphInput{}
	}

	result, metadata, err := c.invokeOperation(ctx, "QueryGraph", params, optFns, c.addOperationQueryGraphMiddlewares)
	if err != nil {
		return nil, err
	}

	out := result.(*QueryGraphOutput)
	out.ResultMetadata = metadata
	return out, nil
}

type QueryGraphInput struct {

	// The identifier of the Amazon DataZone domain.
	//
	// This member is required.
	DomainIdentifier *string

	// List of query match clauses.
	//
	// This member is required.
	Match []types.MatchClause

	// Additional details on the queried entity that can be requested in the response.
	AdditionalAttributes *types.AdditionalAttributes

	// The maximum number of entities to return in a single call to QueryGraph . When
	// the number of entities to be listed is greater than the value of MaxResults ,
	// the response contains a NextToken value that you can use in a subsequent call
	// to QueryGraph to list the next set of entities.
	MaxResults *int32

	// When the number of entities is greater than the default value for the MaxResults
	// parameter, or if you explicitly specify a value for MaxResults that is less
	// than the number of entities, the response includes a pagination token named
	// NextToken . You can specify this NextToken value in a subsequent call to
	// QueryGraph to list the next set of entities.
	NextToken *string

	noSmithyDocumentSerde
}

type QueryGraphOutput struct {

	// The results of the QueryGraph action.
	Items []types.ResultItem

	// When the number of entities is greater than the default value for the MaxResults
	// parameter, or if you explicitly specify a value for MaxResults that is less
	// than the number of entities, the response includes a pagination token named
	// NextToken . You can specify this NextToken value in a subsequent call to
	// QueryGraph to list the next set of entities.
	NextToken *string

	// Metadata pertaining to the operation's result.
	ResultMetadata middleware.Metadata

	noSmithyDocumentSerde
}

func (c *Client) addOperationQueryGraphMiddlewares(stack *middleware.Stack, options Options) (err error) {
	if err := stack.Serialize.Add(&setOperationInputMiddleware{}, middleware.After); err != nil {
		return err
	}
	err = stack.Serialize.Add(&awsRestjson1_serializeOpQueryGraph{}, middleware.After)
	if err != nil {
		return err
	}
	err = stack.Deserialize.Add(&awsRestjson1_deserializeOpQueryGraph{}, middleware.After)
	if err != nil {
		return err
	}
	if err := addProtocolFinalizerMiddlewares(stack, options, "QueryGraph"); err != nil {
		return fmt.Errorf("add protocol finalizers: %v", err)
	}

	if err = addlegacyEndpointContextSetter(stack, options); err != nil {
		return err
	}
	if err = addSetLoggerMiddleware(stack, options); err != nil {
		return err
	}
	if err = addClientRequestID(stack); err != nil {
		return err
	}
	if err = addComputeContentLength(stack); err != nil {
		return err
	}
	if err = addResolveEndpointMiddleware(stack, options); err != nil {
		return err
	}
	if err = addComputePayloadSHA256(stack); err != nil {
		return err
	}
	if err = addRetry(stack, options); err != nil {
		return err
	}
	if err = addRawResponseToMetadata(stack); err != nil {
		return err
	}
	if err = addRecordResponseTiming(stack); err != nil {
		return err
	}
	if err = addSpanRetryLoop(stack, options); err != nil {
		return err
	}
	if err = addClientUserAgent(stack, options); err != nil {
		return err
	}
	if err = smithyhttp.AddErrorCloseResponseBodyMiddleware(stack); err != nil {
		return err
	}
	if err = smithyhttp.AddCloseResponseBodyMiddleware(stack); err != nil {
		return err
	}
	if err = addSetLegacyContextSigningOptionsMiddleware(stack); err != nil {
		return err
	}
	if err = addTimeOffsetBuild(stack, c); err != nil {
		return err
	}
	if err = addUserAgentRetryMode(stack, options); err != nil {
		return err
	}
	if err = addCredentialSource(stack, options); err != nil {
		return err
	}
	if err = addOpQueryGraphValidationMiddleware(stack); err != nil {
		return err
	}
	if err = stack.Initialize.Add(newServiceMetadataMiddleware_opQueryGraph(options.Region), middleware.Before); err != nil {
		return err
	}
	if err = addRecursionDetection(stack); err != nil {
		return err
	}
	if err = addRequestIDRetrieverMiddleware(stack); err != nil {
		return err
	}
	if err = addResponseErrorMiddleware(stack); err != nil {
		return err
	}
	if err = addRequestResponseLogging(stack, options); err != nil {
		return err
	}
	if err = addDisableHTTPSMiddleware(stack, options); err != nil {
		return err
	}
	if err = addInterceptBeforeRetryLoop(stack, options); err != nil {
		return err
	}
	if err = addInterceptAttempt(stack, options); err != nil {
		return err
	}
	if err = addInterceptors(stack, options); err != nil {
		return err
	}
	return nil
}

// QueryGraphPaginatorOptions is the paginator options for QueryGraph
type QueryGraphPaginatorOptions struct {
	// The maximum number of entities to return in a single call to QueryGraph . When
	// the number of entities to be listed is greater than the value of MaxResults ,
	// the response contains a NextToken value that you can use in a subsequent call
	// to QueryGraph to list the next set of entities.
	Limit int32

	// Set to true if pagination should stop if the service returns a pagination token
	// that matches the most recent token provided to the service.
	StopOnDuplicateToken bool
}

// QueryGraphPaginator is a paginator for QueryGraph
type QueryGraphPaginator struct {
	options   QueryGraphPaginatorOptions
	client    QueryGraphAPIClient
	params    *QueryGraphInput
	nextToken *string
	firstPage bool
}

// NewQueryGraphPaginator returns a new QueryGraphPaginator
func NewQueryGraphPaginator(client QueryGraphAPIClient, params *QueryGraphInput, optFns ...func(*QueryGraphPaginatorOptions)) *QueryGraphPaginator {
	if params == nil {
		params = &QueryGraphInput{}
	}

	options := QueryGraphPaginatorOptions{}
	if params.MaxResults != nil {
		options.Limit = *params.MaxResults
	}

	for _, fn := range optFns {
		fn(&options)
	}

	return &QueryGraphPaginator{
		options:   options,
		client:    client,
		params:    params,
		firstPage: true,
		nextToken: params.NextToken,
	}
}

// HasMorePages returns a boolean indicating whether more pages are available
func (p *QueryGraphPaginator) HasMorePages() bool {
	return p.firstPage || (p.nextToken != nil && len(*p.nextToken) != 0)
}

// NextPage retrieves the next QueryGraph page.
func (p *QueryGraphPaginator) NextPage(ctx context.Context, optFns ...func(*Options)) (*QueryGraphOutput, error) {
	if !p.HasMorePages() {
		return nil, fmt.Errorf("no more pages available")
	}

	params := *p.params
	params.NextToken = p.nextToken

	var limit *int32
	if p.options.Limit > 0 {
		limit = &p.options.Limit
	}
	params.MaxResults = limit

	optFns = append([]func(*Options){
		addIsPaginatorUserAgent,
	}, optFns...)
	result, err := p.client.QueryGraph(ctx, &params, optFns...)
	if err != nil {
		return nil, err
	}
	p.firstPage = false

	prevToken := p.nextToken
	p.nextToken = result.NextToken

	if p.options.StopOnDuplicateToken &&
		prevToken != nil &&
		p.nextToken != nil &&
		*prevToken == *p.nextToken {
		p.nextToken = nil
	}

	return result, nil
}

// QueryGraphAPIClient is a client that implements the QueryGraph operation.
type QueryGraphAPIClient interface {
	QueryGraph(context.Context, *QueryGraphInput, ...func(*Options)) (*QueryGraphOutput, error)
}

var _ QueryGraphAPIClient = (*Client)(nil)

func newServiceMetadataMiddleware_opQueryGraph(region string) *awsmiddleware.RegisterServiceMetadata {
	return &awsmiddleware.RegisterServiceMetadata{
		Region:        region,
		ServiceID:     ServiceID,
		OperationName: "QueryGraph",
	}
}
