// Code generated by smithy-go-codegen DO NOT EDIT.

package marketplacediscovery

import (
	"context"
	"fmt"
	awsmiddleware "github.com/aws/aws-sdk-go-v2/aws/middleware"
	"github.com/aws/aws-sdk-go-v2/service/marketplacediscovery/types"
	"github.com/aws/smithy-go/middleware"
	smithyhttp "github.com/aws/smithy-go/transport/http"
)

// Returns the fulfillment options available for a product, including deployment
// details such as version information, operating systems, usage instructions, and
// release notes.
func (c *Client) ListFulfillmentOptions(ctx context.Context, params *ListFulfillmentOptionsInput, optFns ...func(*Options)) (*ListFulfillmentOptionsOutput, error) {
	if params == nil {
		params = &ListFulfillmentOptionsInput{}
	}

	result, metadata, err := c.invokeOperation(ctx, "ListFulfillmentOptions", params, optFns, c.addOperationListFulfillmentOptionsMiddlewares)
	if err != nil {
		return nil, err
	}

	out := result.(*ListFulfillmentOptionsOutput)
	out.ResultMetadata = metadata
	return out, nil
}

type ListFulfillmentOptionsInput struct {

	// The unique identifier of the product for which to list fulfillment options.
	//
	// This member is required.
	ProductId *string

	// The maximum number of results that are returned per call. You can use nextToken
	// to get more results.
	MaxResults *int32

	// If nextToken is returned, there are more results available. Make the call again
	// using the returned token to retrieve the next page.
	NextToken *string

	noSmithyDocumentSerde
}

type ListFulfillmentOptionsOutput struct {

	// The fulfillment options available for the product. Each option describes how
	// the buyer can deploy or access the product.
	//
	// This member is required.
	FulfillmentOptions []types.FulfillmentOption

	// If nextToken is returned, there are more results available. Make the call again
	// using the returned token to retrieve the next page.
	NextToken *string

	// Metadata pertaining to the operation's result.
	ResultMetadata middleware.Metadata

	noSmithyDocumentSerde
}

func (c *Client) addOperationListFulfillmentOptionsMiddlewares(stack *middleware.Stack, options Options) (err error) {
	if err := stack.Serialize.Add(&setOperationInputMiddleware{}, middleware.After); err != nil {
		return err
	}
	err = stack.Serialize.Add(&awsRestjson1_serializeOpListFulfillmentOptions{}, middleware.After)
	if err != nil {
		return err
	}
	err = stack.Deserialize.Add(&awsRestjson1_deserializeOpListFulfillmentOptions{}, middleware.After)
	if err != nil {
		return err
	}
	if err := addProtocolFinalizerMiddlewares(stack, options, "ListFulfillmentOptions"); err != nil {
		return fmt.Errorf("add protocol finalizers: %v", err)
	}

	if err = addlegacyEndpointContextSetter(stack, options); err != nil {
		return err
	}
	if err = addSetLoggerMiddleware(stack, options); err != nil {
		return err
	}
	if err = addClientRequestID(stack); err != nil {
		return err
	}
	if err = addComputeContentLength(stack); err != nil {
		return err
	}
	if err = addResolveEndpointMiddleware(stack, options); err != nil {
		return err
	}
	if err = addComputePayloadSHA256(stack); err != nil {
		return err
	}
	if err = addRetry(stack, options, c); err != nil {
		return err
	}
	if err = addRawResponseToMetadata(stack); err != nil {
		return err
	}
	if err = addRecordResponseTiming(stack); err != nil {
		return err
	}
	if err = addSpanRetryLoop(stack, options); err != nil {
		return err
	}
	if err = addClientUserAgent(stack, options); err != nil {
		return err
	}
	if err = smithyhttp.AddErrorCloseResponseBodyMiddleware(stack); err != nil {
		return err
	}
	if err = smithyhttp.AddCloseResponseBodyMiddleware(stack); err != nil {
		return err
	}
	if err = addSetLegacyContextSigningOptionsMiddleware(stack); err != nil {
		return err
	}
	if err = addUserAgentRetryMode(stack, options); err != nil {
		return err
	}
	if err = addCredentialSource(stack, options); err != nil {
		return err
	}
	if err = addOpListFulfillmentOptionsValidationMiddleware(stack); err != nil {
		return err
	}
	if err = stack.Initialize.Add(newServiceMetadataMiddleware_opListFulfillmentOptions(options.Region), middleware.Before); err != nil {
		return err
	}
	if err = addRecursionDetection(stack); err != nil {
		return err
	}
	if err = addRequestIDRetrieverMiddleware(stack); err != nil {
		return err
	}
	if err = addResponseErrorMiddleware(stack); err != nil {
		return err
	}
	if err = addRequestResponseLogging(stack, options); err != nil {
		return err
	}
	if err = addDisableHTTPSMiddleware(stack, options); err != nil {
		return err
	}
	if err = addInterceptBeforeRetryLoop(stack, options); err != nil {
		return err
	}
	if err = addInterceptAttempt(stack, options); err != nil {
		return err
	}
	if err = addInterceptors(stack, options); err != nil {
		return err
	}
	return nil
}

// ListFulfillmentOptionsPaginatorOptions is the paginator options for
// ListFulfillmentOptions
type ListFulfillmentOptionsPaginatorOptions struct {
	// The maximum number of results that are returned per call. You can use nextToken
	// to get more results.
	Limit int32

	// Set to true if pagination should stop if the service returns a pagination token
	// that matches the most recent token provided to the service.
	StopOnDuplicateToken bool
}

// ListFulfillmentOptionsPaginator is a paginator for ListFulfillmentOptions
type ListFulfillmentOptionsPaginator struct {
	options   ListFulfillmentOptionsPaginatorOptions
	client    ListFulfillmentOptionsAPIClient
	params    *ListFulfillmentOptionsInput
	nextToken *string
	firstPage bool
}

// NewListFulfillmentOptionsPaginator returns a new ListFulfillmentOptionsPaginator
func NewListFulfillmentOptionsPaginator(client ListFulfillmentOptionsAPIClient, params *ListFulfillmentOptionsInput, optFns ...func(*ListFulfillmentOptionsPaginatorOptions)) *ListFulfillmentOptionsPaginator {
	if params == nil {
		params = &ListFulfillmentOptionsInput{}
	}

	options := ListFulfillmentOptionsPaginatorOptions{}
	if params.MaxResults != nil {
		options.Limit = *params.MaxResults
	}

	for _, fn := range optFns {
		fn(&options)
	}

	return &ListFulfillmentOptionsPaginator{
		options:   options,
		client:    client,
		params:    params,
		firstPage: true,
		nextToken: params.NextToken,
	}
}

// HasMorePages returns a boolean indicating whether more pages are available
func (p *ListFulfillmentOptionsPaginator) HasMorePages() bool {
	return p.firstPage || (p.nextToken != nil && len(*p.nextToken) != 0)
}

// NextPage retrieves the next ListFulfillmentOptions page.
func (p *ListFulfillmentOptionsPaginator) NextPage(ctx context.Context, optFns ...func(*Options)) (*ListFulfillmentOptionsOutput, error) {
	if !p.HasMorePages() {
		return nil, fmt.Errorf("no more pages available")
	}

	params := *p.params
	params.NextToken = p.nextToken

	var limit *int32
	if p.options.Limit > 0 {
		limit = &p.options.Limit
	}
	params.MaxResults = limit

	optFns = append([]func(*Options){
		addIsPaginatorUserAgent,
	}, optFns...)
	result, err := p.client.ListFulfillmentOptions(ctx, &params, optFns...)
	if err != nil {
		return nil, err
	}
	p.firstPage = false

	prevToken := p.nextToken
	p.nextToken = result.NextToken

	if p.options.StopOnDuplicateToken &&
		prevToken != nil &&
		p.nextToken != nil &&
		*prevToken == *p.nextToken {
		p.nextToken = nil
	}

	return result, nil
}

// ListFulfillmentOptionsAPIClient is a client that implements the
// ListFulfillmentOptions operation.
type ListFulfillmentOptionsAPIClient interface {
	ListFulfillmentOptions(context.Context, *ListFulfillmentOptionsInput, ...func(*Options)) (*ListFulfillmentOptionsOutput, error)
}

var _ ListFulfillmentOptionsAPIClient = (*Client)(nil)

func newServiceMetadataMiddleware_opListFulfillmentOptions(region string) *awsmiddleware.RegisterServiceMetadata {
	return &awsmiddleware.RegisterServiceMetadata{
		Region:        region,
		ServiceID:     ServiceID,
		OperationName: "ListFulfillmentOptions",
	}
}
