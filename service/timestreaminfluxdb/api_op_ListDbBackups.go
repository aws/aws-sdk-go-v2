// Code generated by smithy-go-codegen DO NOT EDIT.

package timestreaminfluxdb

import (
	"context"
	"fmt"
	"github.com/aws/aws-sdk-go-v2/service/timestreaminfluxdb/types"
	"github.com/aws/smithy-go/middleware"
)

// Returns a list of Timestream for InfluxDB backups.
func (c *Client) ListDbBackups(ctx context.Context, params *ListDbBackupsInput, optFns ...func(*Options)) (*ListDbBackupsOutput, error) {
	if params == nil {
		params = &ListDbBackupsInput{}
	}

	result, metadata, err := c.invokeOperation(ctx, "ListDbBackups", params, optFns, c.addOperationListDbBackupsMiddlewares)
	if err != nil {
		return nil, err
	}

	out := result.(*ListDbBackupsOutput)
	out.ResultMetadata = metadata
	return out, nil
}

type ListDbBackupsInput struct {

	// The identifier of the DB instance or DB cluster to list backups for. If not
	// specified, returns all backups in the account and region.
	DbResourceId *string

	// The maximum number of items to return in the output. If the total number of
	// items available is more than the value specified, a nextToken is provided in the
	// output. To resume pagination, provide the nextToken value as an argument of a
	// subsequent API invocation.
	MaxResults *int32

	// The pagination token. To resume pagination, provide the nextToken value as an
	// argument of a subsequent API invocation.
	NextToken *string

	noSmithyDocumentSerde
}

type ListDbBackupsOutput struct {

	// A list of Timestream for InfluxDB backup summaries.
	//
	// This member is required.
	Items []types.DbBackupSummary

	// Token from a previous call of the operation. When this value is provided, the
	// service returns results from where the previous response left off.
	NextToken *string

	// Metadata pertaining to the operation's result.
	ResultMetadata middleware.Metadata

	noSmithyDocumentSerde
}

func (c *Client) addOperationListDbBackupsMiddlewares(stack *middleware.Stack, options Options) (err error) {
	err = stack.Serialize.Add(&awsAwsjson10_serializeOpListDbBackups{}, middleware.After)
	if err != nil {
		return err
	}
	err = stack.Deserialize.Add(&awsAwsjson10_deserializeOpListDbBackups{}, middleware.After)
	if err != nil {
		return err
	}

	if err = addResolveEndpointMiddleware(stack, options); err != nil {
		return err
	}
	if err = addComputePayloadSHA256(stack); err != nil {
		return err
	}
	if err = addRecordResponseTiming(stack, options); err != nil {
		return err
	}
	if err = addCredentialSource(stack, options); err != nil {
		return err
	}
	if err = addRequestIDRetrieverMiddleware(stack); err != nil {
		return err
	}
	if err = addResponseErrorMiddleware(stack); err != nil {
		return err
	}
	if err = addRequestResponseLogging(stack, options); err != nil {
		return err
	}
	if err = addDisableHTTPSMiddleware(stack, options); err != nil {
		return err
	}
	if err = addInterceptors(stack, options); err != nil {
		return err
	}
	return nil
}

// ListDbBackupsPaginatorOptions is the paginator options for ListDbBackups
type ListDbBackupsPaginatorOptions struct {
	// The maximum number of items to return in the output. If the total number of
	// items available is more than the value specified, a nextToken is provided in the
	// output. To resume pagination, provide the nextToken value as an argument of a
	// subsequent API invocation.
	Limit int32

	// Set to true if pagination should stop if the service returns a pagination token
	// that matches the most recent token provided to the service.
	StopOnDuplicateToken bool
}

// ListDbBackupsPaginator is a paginator for ListDbBackups
type ListDbBackupsPaginator struct {
	options   ListDbBackupsPaginatorOptions
	client    ListDbBackupsAPIClient
	params    *ListDbBackupsInput
	nextToken *string
	firstPage bool
}

// NewListDbBackupsPaginator returns a new ListDbBackupsPaginator
func NewListDbBackupsPaginator(client ListDbBackupsAPIClient, params *ListDbBackupsInput, optFns ...func(*ListDbBackupsPaginatorOptions)) *ListDbBackupsPaginator {
	if params == nil {
		params = &ListDbBackupsInput{}
	}

	options := ListDbBackupsPaginatorOptions{}
	if params.MaxResults != nil {
		options.Limit = *params.MaxResults
	}

	for _, fn := range optFns {
		fn(&options)
	}

	return &ListDbBackupsPaginator{
		options:   options,
		client:    client,
		params:    params,
		firstPage: true,
		nextToken: params.NextToken,
	}
}

// HasMorePages returns a boolean indicating whether more pages are available
func (p *ListDbBackupsPaginator) HasMorePages() bool {
	return p.firstPage || (p.nextToken != nil && len(*p.nextToken) != 0)
}

// NextPage retrieves the next ListDbBackups page.
func (p *ListDbBackupsPaginator) NextPage(ctx context.Context, optFns ...func(*Options)) (*ListDbBackupsOutput, error) {
	if !p.HasMorePages() {
		return nil, fmt.Errorf("no more pages available")
	}

	params := *p.params
	params.NextToken = p.nextToken

	var limit *int32
	if p.options.Limit > 0 {
		limit = &p.options.Limit
	}
	params.MaxResults = limit

	optFns = append([]func(*Options){
		addIsPaginatorUserAgent,
	}, optFns...)
	result, err := p.client.ListDbBackups(ctx, &params, optFns...)
	if err != nil {
		return nil, err
	}
	p.firstPage = false

	prevToken := p.nextToken
	p.nextToken = result.NextToken

	if p.options.StopOnDuplicateToken &&
		prevToken != nil &&
		p.nextToken != nil &&
		*prevToken == *p.nextToken {
		p.nextToken = nil
	}

	return result, nil
}

// ListDbBackupsAPIClient is a client that implements the ListDbBackups operation.
type ListDbBackupsAPIClient interface {
	ListDbBackups(context.Context, *ListDbBackupsInput, ...func(*Options)) (*ListDbBackupsOutput, error)
}

var _ ListDbBackupsAPIClient = (*Client)(nil)
