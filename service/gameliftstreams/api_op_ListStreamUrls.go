// Code generated by smithy-go-codegen DO NOT EDIT.

package gameliftstreams

import (
	"context"
	"fmt"
	"github.com/aws/aws-sdk-go-v2/service/gameliftstreams/types"
	"github.com/aws/smithy-go/middleware"
)

// Retrieves a list of the stream URLs in the current Amazon Web Services Region
// for your Amazon Web Services account. You can filter the results by status or by
// stream group. Use the pagination parameters to retrieve results as a set of
// sequential pages. If you delete the stream group or application that backs a
// stream URL, this operation updates that stream URL's status to REVOKED .
func (c *Client) ListStreamUrls(ctx context.Context, params *ListStreamUrlsInput, optFns ...func(*Options)) (*ListStreamUrlsOutput, error) {
	if params == nil {
		params = &ListStreamUrlsInput{}
	}

	result, metadata, err := c.invokeOperation(ctx, "ListStreamUrls", params, optFns, c.addOperationListStreamUrlsMiddlewares)
	if err != nil {
		return nil, err
	}

	out := result.(*ListStreamUrlsOutput)
	out.ResultMetadata = metadata
	return out, nil
}

type ListStreamUrlsInput struct {

	// The maximum number of results to return per page. Valid values are 1-100. The
	// default is 25.
	MaxResults *int32

	// The token that marks the start of the next set of results. Use this token when
	// you retrieve results as sequential pages. To get the first page of results, omit
	// a token value. To get the remaining pages, provide the token returned with the
	// previous result set.
	NextToken *string

	// Filters the list to stream URLs with the specified status.
	//
	//   - ACTIVE : The stream URL is valid and can start stream sessions.
	//
	//   - EXPIRED : The stream URL has passed its expiration time and can no longer
	//   start stream sessions.
	//
	//   - REVOKED : The stream URL was revoked and can no longer start stream sessions.
	//
	//   - LIMIT_REACHED : The stream URL has been used the maximum number of times and
	//   can no longer start stream sessions.
	Status types.StreamUrlStatus

	// Filters the list to stream URLs that belong to the specified stream group.
	//
	// This value is an [Amazon Resource Name (ARN)] or ID that uniquely identifies the stream group resource.
	// Example ARN:
	// arn:aws:gameliftstreams:us-west-2:111122223333:streamgroup/sg-1AB2C3De4 .
	// Example ID: sg-1AB2C3De4 .
	//
	// [Amazon Resource Name (ARN)]: https://docs.aws.amazon.com/IAM/latest/UserGuide/reference-arns.html
	StreamGroupIdentifier *string

	noSmithyDocumentSerde
}

type ListStreamUrlsOutput struct {

	// A collection of stream URL summaries. Each summary includes the identity,
	// status, and usage of the stream URL, but not its full configuration.
	Items []types.StreamUrlSummary

	// A token that marks the start of the next sequential page of results. If an
	// operation doesn't return a token, you've reached the end of the list.
	NextToken *string

	// Metadata pertaining to the operation's result.
	ResultMetadata middleware.Metadata

	noSmithyDocumentSerde
}

func (c *Client) addOperationListStreamUrlsMiddlewares(stack *middleware.Stack, options Options) (err error) {
	err = stack.Serialize.Add(&awsRestjson1_serializeOpListStreamUrls{}, middleware.After)
	if err != nil {
		return err
	}
	err = stack.Deserialize.Add(&awsRestjson1_deserializeOpListStreamUrls{}, middleware.After)
	if err != nil {
		return err
	}

	if err = addResolveEndpointMiddleware(stack, options); err != nil {
		return err
	}
	if err = addComputePayloadSHA256(stack); err != nil {
		return err
	}
	if err = addRecordResponseTiming(stack, options); err != nil {
		return err
	}
	if err = addCredentialSource(stack, options); err != nil {
		return err
	}
	if err = addRequestIDRetrieverMiddleware(stack); err != nil {
		return err
	}
	if err = addResponseErrorMiddleware(stack); err != nil {
		return err
	}
	if err = addRequestResponseLogging(stack, options); err != nil {
		return err
	}
	if err = addDisableHTTPSMiddleware(stack, options); err != nil {
		return err
	}
	if err = addInterceptors(stack, options); err != nil {
		return err
	}
	return nil
}

// ListStreamUrlsPaginatorOptions is the paginator options for ListStreamUrls
type ListStreamUrlsPaginatorOptions struct {
	// The maximum number of results to return per page. Valid values are 1-100. The
	// default is 25.
	Limit int32

	// Set to true if pagination should stop if the service returns a pagination token
	// that matches the most recent token provided to the service.
	StopOnDuplicateToken bool
}

// ListStreamUrlsPaginator is a paginator for ListStreamUrls
type ListStreamUrlsPaginator struct {
	options   ListStreamUrlsPaginatorOptions
	client    ListStreamUrlsAPIClient
	params    *ListStreamUrlsInput
	nextToken *string
	firstPage bool
}

// NewListStreamUrlsPaginator returns a new ListStreamUrlsPaginator
func NewListStreamUrlsPaginator(client ListStreamUrlsAPIClient, params *ListStreamUrlsInput, optFns ...func(*ListStreamUrlsPaginatorOptions)) *ListStreamUrlsPaginator {
	if params == nil {
		params = &ListStreamUrlsInput{}
	}

	options := ListStreamUrlsPaginatorOptions{}
	if params.MaxResults != nil {
		options.Limit = *params.MaxResults
	}

	for _, fn := range optFns {
		fn(&options)
	}

	return &ListStreamUrlsPaginator{
		options:   options,
		client:    client,
		params:    params,
		firstPage: true,
		nextToken: params.NextToken,
	}
}

// HasMorePages returns a boolean indicating whether more pages are available
func (p *ListStreamUrlsPaginator) HasMorePages() bool {
	return p.firstPage || (p.nextToken != nil && len(*p.nextToken) != 0)
}

// NextPage retrieves the next ListStreamUrls page.
func (p *ListStreamUrlsPaginator) NextPage(ctx context.Context, optFns ...func(*Options)) (*ListStreamUrlsOutput, error) {
	if !p.HasMorePages() {
		return nil, fmt.Errorf("no more pages available")
	}

	params := *p.params
	params.NextToken = p.nextToken

	var limit *int32
	if p.options.Limit > 0 {
		limit = &p.options.Limit
	}
	params.MaxResults = limit

	optFns = append([]func(*Options){
		addIsPaginatorUserAgent,
	}, optFns...)
	result, err := p.client.ListStreamUrls(ctx, &params, optFns...)
	if err != nil {
		return nil, err
	}
	p.firstPage = false

	prevToken := p.nextToken
	p.nextToken = result.NextToken

	if p.options.StopOnDuplicateToken &&
		prevToken != nil &&
		p.nextToken != nil &&
		*prevToken == *p.nextToken {
		p.nextToken = nil
	}

	return result, nil
}

// ListStreamUrlsAPIClient is a client that implements the ListStreamUrls
// operation.
type ListStreamUrlsAPIClient interface {
	ListStreamUrls(context.Context, *ListStreamUrlsInput, ...func(*Options)) (*ListStreamUrlsOutput, error)
}

var _ ListStreamUrlsAPIClient = (*Client)(nil)
