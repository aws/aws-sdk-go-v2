// Code generated by smithy-go-codegen DO NOT EDIT.

package dsql

import (
	"context"
	"errors"
	"fmt"
	"github.com/aws/aws-sdk-go-v2/service/dsql/schemas"
	"github.com/aws/aws-sdk-go-v2/service/dsql/types"
	smithy "github.com/aws/smithy-go"
	"github.com/aws/smithy-go/middleware"
	smithytime "github.com/aws/smithy-go/time"
	smithywaiter "github.com/aws/smithy-go/waiter"
	"time"
)

// Retrieves information about a stream.
func (c *Client) GetStream(ctx context.Context, params *GetStreamInput, optFns ...func(*Options)) (*GetStreamOutput, error) {
	if params == nil {
		params = &GetStreamInput{}
	}

	result, metadata, err := c.invokeOperation(ctx, "GetStream", params, optFns, c.addOperationGetStreamMiddlewares)
	if err != nil {
		return nil, err
	}

	out := result.(*GetStreamOutput)
	out.ResultMetadata = metadata
	return out, nil
}

type GetStreamInput struct {

	// The ID of the cluster containing the stream to retrieve.
	//
	// This member is required.
	ClusterIdentifier *string

	// The ID of the stream to retrieve.
	//
	// This member is required.
	StreamIdentifier *string

	noSmithyDocumentSerde
}

func (v *GetStreamInput) Serialize(s smithy.ShapeSerializer) {
	s.WriteStruct(schemas.GetStreamInput)
	v.SerializeMembers(s)
	s.CloseStruct()
}

func (v *GetStreamInput) SerializeMembers(s smithy.ShapeSerializer) {
	if v.ClusterIdentifier != nil {
		s.WriteString(schemas.GetStreamInput_clusterIdentifier, *v.ClusterIdentifier)
	}
	if v.StreamIdentifier != nil {
		s.WriteString(schemas.GetStreamInput_streamIdentifier, *v.StreamIdentifier)
	}
}

// The output of a retrieved stream.
type GetStreamOutput struct {

	// The ARN of the retrieved stream.
	//
	// This member is required.
	Arn *string

	// The ID of the cluster for the retrieved stream.
	//
	// This member is required.
	ClusterIdentifier *string

	// The time when the stream was created.
	//
	// This member is required.
	CreationTime *time.Time

	// The format of the stream records.
	//
	// This member is required.
	Format types.StreamFormat

	// The ordering mode of the stream.
	//
	// This member is required.
	Ordering types.StreamOrdering

	// The current status of the retrieved stream.
	//
	// This member is required.
	Status types.StreamStatus

	// The ID of the retrieved stream.
	//
	// This member is required.
	StreamIdentifier *string

	// Stream status reason with error code and timestamp (if applicable).
	StatusReason *types.StatusReason

	// A map of tags associated with the stream.
	Tags map[string]string

	// The target definition for the stream destination.
	TargetDefinition types.TargetDefinition

	// Metadata pertaining to the operation's result.
	ResultMetadata middleware.Metadata

	noSmithyDocumentSerde
}

func (v *GetStreamOutput) Serialize(s smithy.ShapeSerializer) {
	s.WriteStruct(schemas.GetStreamOutput)
	v.SerializeMembers(s)
	s.CloseStruct()
}

func (v *GetStreamOutput) SerializeMembers(s smithy.ShapeSerializer) {
	if v.Arn != nil {
		s.WriteString(schemas.GetStreamOutput_arn, *v.Arn)
	}
	if v.ClusterIdentifier != nil {
		s.WriteString(schemas.GetStreamOutput_clusterIdentifier, *v.ClusterIdentifier)
	}
	if v.CreationTime != nil {
		s.WriteTime(schemas.GetStreamOutput_creationTime, *v.CreationTime)
	}
	if v.Format != "" {
		s.WriteString(schemas.GetStreamOutput_format, string(v.Format))
	}
	if v.Ordering != "" {
		s.WriteString(schemas.GetStreamOutput_ordering, string(v.Ordering))
	}
	if v.Status != "" {
		s.WriteString(schemas.GetStreamOutput_status, string(v.Status))
	}
	if v.StatusReason != nil {
		s.WriteStruct(schemas.GetStreamOutput_statusReason)
		v.StatusReason.SerializeMembers(s)
		s.CloseStruct()
	}
	if v.StreamIdentifier != nil {
		s.WriteString(schemas.GetStreamOutput_streamIdentifier, *v.StreamIdentifier)
	}
	serializeTagMap(s, schemas.GetStreamOutput_tags, v.Tags)
	serializeTargetDefinition(s, schemas.GetStreamOutput_targetDefinition, v.TargetDefinition)
}
func (v *GetStreamOutput) Deserialize(d smithy.ShapeDeserializer) error {
	return smithy.ReadStruct(d, schemas.GetStreamOutput, func(s *smithy.Schema) error {
		switch s {
		case schemas.GetStreamOutput_arn:
			v.Arn = new(string)
			return d.ReadString(schemas.GetStreamOutput_arn, v.Arn)
		case schemas.GetStreamOutput_clusterIdentifier:
			v.ClusterIdentifier = new(string)
			return d.ReadString(schemas.GetStreamOutput_clusterIdentifier, v.ClusterIdentifier)
		case schemas.GetStreamOutput_creationTime:
			v.CreationTime = new(time.Time)
			return d.ReadTime(schemas.GetStreamOutput_creationTime, v.CreationTime)
		case schemas.GetStreamOutput_format:
			var ev string
			if err := d.ReadString(schemas.GetStreamOutput_format, &ev); err != nil {
				return err
			}
			v.Format = types.StreamFormat(ev)
			return nil
		case schemas.GetStreamOutput_ordering:
			var ev string
			if err := d.ReadString(schemas.GetStreamOutput_ordering, &ev); err != nil {
				return err
			}
			v.Ordering = types.StreamOrdering(ev)
			return nil
		case schemas.GetStreamOutput_status:
			var ev string
			if err := d.ReadString(schemas.GetStreamOutput_status, &ev); err != nil {
				return err
			}
			v.Status = types.StreamStatus(ev)
			return nil
		case schemas.GetStreamOutput_statusReason:
			v.StatusReason = &types.StatusReason{}
			return v.StatusReason.Deserialize(d)
		case schemas.GetStreamOutput_streamIdentifier:
			v.StreamIdentifier = new(string)
			return d.ReadString(schemas.GetStreamOutput_streamIdentifier, v.StreamIdentifier)
		case schemas.GetStreamOutput_tags:
			return deserializeTagMap(d, schemas.GetStreamOutput_tags, &v.Tags)
		case schemas.GetStreamOutput_targetDefinition:
			return deserializeTargetDefinition(d, schemas.GetStreamOutput_targetDefinition, &v.TargetDefinition)
		}
		return nil
	})
}
func (c *Client) addOperationGetStreamMiddlewares(stack *middleware.Stack, options Options) (err error) {
	if err := stack.Serialize.Add(&serializeRequestMiddleware{options: &options, operationSchema: smithy.NewOperationSchema(schemas.GetStream, schemas.GetStreamInput, schemas.GetStreamOutput)}, middleware.After); err != nil {
		return err
	}
	if err := stack.Deserialize.Add(&deserializeResponseMiddleware{options: &options, operationSchema: smithy.NewOperationSchema(schemas.GetStream, schemas.GetStreamInput, schemas.GetStreamOutput), output: &GetStreamOutput{}}, middleware.After); err != nil {
		return err
	}

	if err = addResolveEndpointMiddleware(stack, options); err != nil {
		return err
	}
	if err = addComputePayloadSHA256(stack); err != nil {
		return err
	}
	if err = addRecordResponseTiming(stack, options); err != nil {
		return err
	}
	if err = addCredentialSource(stack, options); err != nil {
		return err
	}
	if err = addOpGetStreamValidationMiddleware(stack); err != nil {
		return err
	}
	if err = addRequestIDRetrieverMiddleware(stack); err != nil {
		return err
	}
	if err = addResponseErrorMiddleware(stack); err != nil {
		return err
	}
	if err = addRequestResponseLogging(stack, options); err != nil {
		return err
	}
	if err = addDisableHTTPSMiddleware(stack, options); err != nil {
		return err
	}
	if err = addInterceptors(stack, options); err != nil {
		return err
	}
	return nil
}

// StreamActiveWaiterOptions are waiter options for StreamActiveWaiter
type StreamActiveWaiterOptions struct {

	// Set of options to modify how an operation is invoked. These apply to all
	// operations invoked for this client. Use functional options on operation call to
	// modify this list for per operation behavior.
	//
	// Passing options here is functionally equivalent to passing values to this
	// config's ClientOptions field that extend the inner client's APIOptions directly.
	APIOptions []func(*middleware.Stack) error

	// Functional options to be passed to all operations invoked by this client.
	//
	// Function values that modify the inner APIOptions are applied after the waiter
	// config's own APIOptions modifiers.
	ClientOptions []func(*Options)

	// MinDelay is the minimum amount of time to delay between retries. If unset,
	// StreamActiveWaiter will use default minimum delay of 2 seconds. Note that
	// MinDelay must resolve to a value lesser than or equal to the MaxDelay.
	MinDelay time.Duration

	// MaxDelay is the maximum amount of time to delay between retries. If unset or
	// set to zero, StreamActiveWaiter will use default max delay of 120 seconds. Note
	// that MaxDelay must resolve to value greater than or equal to the MinDelay.
	MaxDelay time.Duration

	// LogWaitAttempts is used to enable logging for waiter retry attempts
	LogWaitAttempts bool

	// Retryable is function that can be used to override the service defined
	// waiter-behavior based on operation output, or returned error. This function is
	// used by the waiter to decide if a state is retryable or a terminal state.
	//
	// By default service-modeled logic will populate this option. This option can
	// thus be used to define a custom waiter state with fall-back to service-modeled
	// waiter state mutators.The function returns an error in case of a failure state.
	// In case of retry state, this function returns a bool value of true and nil
	// error, while in case of success it returns a bool value of false and nil error.
	Retryable func(context.Context, *GetStreamInput, *GetStreamOutput, error) (bool, error)
}

// StreamActiveWaiter defines the waiters for StreamActive
type StreamActiveWaiter struct {
	client GetStreamAPIClient

	options StreamActiveWaiterOptions
}

// NewStreamActiveWaiter constructs a StreamActiveWaiter.
func NewStreamActiveWaiter(client GetStreamAPIClient, optFns ...func(*StreamActiveWaiterOptions)) *StreamActiveWaiter {
	options := StreamActiveWaiterOptions{}
	options.MinDelay = 2 * time.Second
	options.MaxDelay = 120 * time.Second
	options.Retryable = streamActiveStateRetryable

	for _, fn := range optFns {
		fn(&options)
	}
	return &StreamActiveWaiter{
		client:  client,
		options: options,
	}
}

// Wait calls the waiter function for StreamActive waiter. The maxWaitDur is the
// maximum wait duration the waiter will wait. The maxWaitDur is required and must
// be greater than zero.
func (w *StreamActiveWaiter) Wait(ctx context.Context, params *GetStreamInput, maxWaitDur time.Duration, optFns ...func(*StreamActiveWaiterOptions)) error {
	_, err := w.WaitForOutput(ctx, params, maxWaitDur, optFns...)
	return err
}

// WaitForOutput calls the waiter function for StreamActive waiter and returns the
// output of the successful operation. The maxWaitDur is the maximum wait duration
// the waiter will wait. The maxWaitDur is required and must be greater than zero.
func (w *StreamActiveWaiter) WaitForOutput(ctx context.Context, params *GetStreamInput, maxWaitDur time.Duration, optFns ...func(*StreamActiveWaiterOptions)) (*GetStreamOutput, error) {
	if maxWaitDur <= 0 {
		return nil, fmt.Errorf("maximum wait time for waiter must be greater than zero")
	}

	options := w.options
	for _, fn := range optFns {
		fn(&options)
	}

	if options.MaxDelay <= 0 {
		options.MaxDelay = 120 * time.Second
	}

	if options.MinDelay > options.MaxDelay {
		return nil, fmt.Errorf("minimum waiter delay %v must be lesser than or equal to maximum waiter delay of %v.", options.MinDelay, options.MaxDelay)
	}

	ctx, cancelFn := context.WithTimeout(ctx, maxWaitDur)
	defer cancelFn()

	logger := smithywaiter.Logger{}
	remainingTime := maxWaitDur

	var attempt int64
	for {

		attempt++
		apiOptions := options.APIOptions
		start := time.Now()

		if options.LogWaitAttempts {
			logger.Attempt = attempt
			apiOptions = append([]func(*middleware.Stack) error{}, options.APIOptions...)
			apiOptions = append(apiOptions, logger.AddLogger)
		}

		out, err := w.client.GetStream(ctx, params, func(o *Options) {
			baseOpts := []func(*Options){
				addIsWaiterUserAgent,
			}
			o.APIOptions = append(o.APIOptions, apiOptions...)
			for _, opt := range baseOpts {
				opt(o)
			}
			for _, opt := range options.ClientOptions {
				opt(o)
			}
		})

		retryable, err := options.Retryable(ctx, params, out, err)
		if err != nil {
			return nil, err
		}
		if !retryable {
			return out, nil
		}

		remainingTime -= time.Since(start)
		if remainingTime < options.MinDelay || remainingTime <= 0 {
			break
		}

		// compute exponential backoff between waiter retries
		delay, err := smithywaiter.ComputeDelay(
			attempt, options.MinDelay, options.MaxDelay, remainingTime,
		)
		if err != nil {
			return nil, fmt.Errorf("error computing waiter delay, %w", err)
		}

		remainingTime -= delay
		// sleep for the delay amount before invoking a request
		if err := smithytime.SleepWithContext(ctx, delay); err != nil {
			return nil, fmt.Errorf("request cancelled while waiting, %w", err)
		}
	}
	return nil, fmt.Errorf("exceeded max wait time for StreamActive waiter")
}

func streamActiveStateRetryable(ctx context.Context, input *GetStreamInput, output *GetStreamOutput, err error) (bool, error) {

	if err == nil {
		v1 := output.Status
		expectedValue := "ACTIVE"
		var pathValue string
		pathValue = string(v1)
		if pathValue == expectedValue {
			return false, nil
		}
	}

	if err != nil {
		return false, err
	}
	return true, nil
}

// StreamNotExistsWaiterOptions are waiter options for StreamNotExistsWaiter
type StreamNotExistsWaiterOptions struct {

	// Set of options to modify how an operation is invoked. These apply to all
	// operations invoked for this client. Use functional options on operation call to
	// modify this list for per operation behavior.
	//
	// Passing options here is functionally equivalent to passing values to this
	// config's ClientOptions field that extend the inner client's APIOptions directly.
	APIOptions []func(*middleware.Stack) error

	// Functional options to be passed to all operations invoked by this client.
	//
	// Function values that modify the inner APIOptions are applied after the waiter
	// config's own APIOptions modifiers.
	ClientOptions []func(*Options)

	// MinDelay is the minimum amount of time to delay between retries. If unset,
	// StreamNotExistsWaiter will use default minimum delay of 2 seconds. Note that
	// MinDelay must resolve to a value lesser than or equal to the MaxDelay.
	MinDelay time.Duration

	// MaxDelay is the maximum amount of time to delay between retries. If unset or
	// set to zero, StreamNotExistsWaiter will use default max delay of 120 seconds.
	// Note that MaxDelay must resolve to value greater than or equal to the MinDelay.
	MaxDelay time.Duration

	// LogWaitAttempts is used to enable logging for waiter retry attempts
	LogWaitAttempts bool

	// Retryable is function that can be used to override the service defined
	// waiter-behavior based on operation output, or returned error. This function is
	// used by the waiter to decide if a state is retryable or a terminal state.
	//
	// By default service-modeled logic will populate this option. This option can
	// thus be used to define a custom waiter state with fall-back to service-modeled
	// waiter state mutators.The function returns an error in case of a failure state.
	// In case of retry state, this function returns a bool value of true and nil
	// error, while in case of success it returns a bool value of false and nil error.
	Retryable func(context.Context, *GetStreamInput, *GetStreamOutput, error) (bool, error)
}

// StreamNotExistsWaiter defines the waiters for StreamNotExists
type StreamNotExistsWaiter struct {
	client GetStreamAPIClient

	options StreamNotExistsWaiterOptions
}

// NewStreamNotExistsWaiter constructs a StreamNotExistsWaiter.
func NewStreamNotExistsWaiter(client GetStreamAPIClient, optFns ...func(*StreamNotExistsWaiterOptions)) *StreamNotExistsWaiter {
	options := StreamNotExistsWaiterOptions{}
	options.MinDelay = 2 * time.Second
	options.MaxDelay = 120 * time.Second
	options.Retryable = streamNotExistsStateRetryable

	for _, fn := range optFns {
		fn(&options)
	}
	return &StreamNotExistsWaiter{
		client:  client,
		options: options,
	}
}

// Wait calls the waiter function for StreamNotExists waiter. The maxWaitDur is
// the maximum wait duration the waiter will wait. The maxWaitDur is required and
// must be greater than zero.
func (w *StreamNotExistsWaiter) Wait(ctx context.Context, params *GetStreamInput, maxWaitDur time.Duration, optFns ...func(*StreamNotExistsWaiterOptions)) error {
	_, err := w.WaitForOutput(ctx, params, maxWaitDur, optFns...)
	return err
}

// WaitForOutput calls the waiter function for StreamNotExists waiter and returns
// the output of the successful operation. The maxWaitDur is the maximum wait
// duration the waiter will wait. The maxWaitDur is required and must be greater
// than zero.
func (w *StreamNotExistsWaiter) WaitForOutput(ctx context.Context, params *GetStreamInput, maxWaitDur time.Duration, optFns ...func(*StreamNotExistsWaiterOptions)) (*GetStreamOutput, error) {
	if maxWaitDur <= 0 {
		return nil, fmt.Errorf("maximum wait time for waiter must be greater than zero")
	}

	options := w.options
	for _, fn := range optFns {
		fn(&options)
	}

	if options.MaxDelay <= 0 {
		options.MaxDelay = 120 * time.Second
	}

	if options.MinDelay > options.MaxDelay {
		return nil, fmt.Errorf("minimum waiter delay %v must be lesser than or equal to maximum waiter delay of %v.", options.MinDelay, options.MaxDelay)
	}

	ctx, cancelFn := context.WithTimeout(ctx, maxWaitDur)
	defer cancelFn()

	logger := smithywaiter.Logger{}
	remainingTime := maxWaitDur

	var attempt int64
	for {

		attempt++
		apiOptions := options.APIOptions
		start := time.Now()

		if options.LogWaitAttempts {
			logger.Attempt = attempt
			apiOptions = append([]func(*middleware.Stack) error{}, options.APIOptions...)
			apiOptions = append(apiOptions, logger.AddLogger)
		}

		out, err := w.client.GetStream(ctx, params, func(o *Options) {
			baseOpts := []func(*Options){
				addIsWaiterUserAgent,
			}
			o.APIOptions = append(o.APIOptions, apiOptions...)
			for _, opt := range baseOpts {
				opt(o)
			}
			for _, opt := range options.ClientOptions {
				opt(o)
			}
		})

		retryable, err := options.Retryable(ctx, params, out, err)
		if err != nil {
			return nil, err
		}
		if !retryable {
			return out, nil
		}

		remainingTime -= time.Since(start)
		if remainingTime < options.MinDelay || remainingTime <= 0 {
			break
		}

		// compute exponential backoff between waiter retries
		delay, err := smithywaiter.ComputeDelay(
			attempt, options.MinDelay, options.MaxDelay, remainingTime,
		)
		if err != nil {
			return nil, fmt.Errorf("error computing waiter delay, %w", err)
		}

		remainingTime -= delay
		// sleep for the delay amount before invoking a request
		if err := smithytime.SleepWithContext(ctx, delay); err != nil {
			return nil, fmt.Errorf("request cancelled while waiting, %w", err)
		}
	}
	return nil, fmt.Errorf("exceeded max wait time for StreamNotExists waiter")
}

func streamNotExistsStateRetryable(ctx context.Context, input *GetStreamInput, output *GetStreamOutput, err error) (bool, error) {

	if err != nil {
		var errorType *types.ResourceNotFoundException
		if errors.As(err, &errorType) {
			return false, nil
		}
	}

	if err != nil {
		return false, err
	}
	return true, nil
}

// GetStreamAPIClient is a client that implements the GetStream operation.
type GetStreamAPIClient interface {
	GetStream(context.Context, *GetStreamInput, ...func(*Options)) (*GetStreamOutput, error)
}

var _ GetStreamAPIClient = (*Client)(nil)
