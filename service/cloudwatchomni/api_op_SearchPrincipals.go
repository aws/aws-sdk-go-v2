// Code generated by smithy-go-codegen DO NOT EDIT.

package cloudwatchomni

import (
	"context"
	"fmt"
	"github.com/aws/aws-sdk-go-v2/service/cloudwatchomni/schemas"
	"github.com/aws/aws-sdk-go-v2/service/cloudwatchomni/types"
	smithy "github.com/aws/smithy-go"
	"github.com/aws/smithy-go/middleware"
)

// Searches Identity Center for users and groups in a domain. The domain must be
// configured with Identity Center.
//
// To grant access to a result, pass its principalId to CreateAccessGrant with a
// principalType of IDC_USER for a user or IDC_GROUP for a group.
func (c *Client) SearchPrincipals(ctx context.Context, params *SearchPrincipalsInput, optFns ...func(*Options)) (*SearchPrincipalsOutput, error) {
	if params == nil {
		params = &SearchPrincipalsInput{}
	}

	result, metadata, err := c.invokeOperation(ctx, "SearchPrincipals", params, optFns, c.addOperationSearchPrincipalsMiddlewares)
	if err != nil {
		return nil, err
	}

	out := result.(*SearchPrincipalsOutput)
	out.ResultMetadata = metadata
	return out, nil
}

type SearchPrincipalsInput struct {

	// The ID of the domain to search within.
	//
	// This member is required.
	DomainId *string

	// A search term to match against user names, display names, and IDs. Pass * to
	// list all principals. Maximum 128 characters.
	//
	// This member is required.
	SearchQuery *string

	// The maximum number of results to return. Defaults to 10. Valid only when
	// searchQuery is *; other searches reject this parameter and return at most 10
	// results.
	MaxResults *int32

	// A token to retrieve the next page of results. Valid only when searchQuery is *;
	// other searches do not paginate and reject this parameter. Tokens expire after 24
	// hours.
	NextToken *string

	noSmithyDocumentSerde
}

func (v *SearchPrincipalsInput) Serialize(s smithy.ShapeSerializer) {
	s.WriteStruct(schemas.SearchPrincipalsInput)
	v.SerializeMembers(s)
	s.CloseStruct()
}

func (v *SearchPrincipalsInput) SerializeMembers(s smithy.ShapeSerializer) {
	if v.DomainId != nil {
		s.WriteString(schemas.SearchPrincipalsInput_domainId, *v.DomainId)
	}
	if v.MaxResults != nil {
		s.WriteInt32(schemas.SearchPrincipalsInput_maxResults, *v.MaxResults)
	}
	if v.NextToken != nil {
		s.WriteString(schemas.SearchPrincipalsInput_nextToken, *v.NextToken)
	}
	if v.SearchQuery != nil {
		s.WriteString(schemas.SearchPrincipalsInput_searchQuery, *v.SearchQuery)
	}
}

type SearchPrincipalsOutput struct {

	// The list of matching principals.
	//
	// This member is required.
	Results []types.PrincipalSearchResult

	// A token to retrieve the next page of results, or null if there are no more
	// results.
	NextToken *string

	// Metadata pertaining to the operation's result.
	ResultMetadata middleware.Metadata

	noSmithyDocumentSerde
}

func (v *SearchPrincipalsOutput) Serialize(s smithy.ShapeSerializer) {
	s.WriteStruct(schemas.SearchPrincipalsOutput)
	v.SerializeMembers(s)
	s.CloseStruct()
}

func (v *SearchPrincipalsOutput) SerializeMembers(s smithy.ShapeSerializer) {
	if v.NextToken != nil {
		s.WriteString(schemas.SearchPrincipalsOutput_nextToken, *v.NextToken)
	}
	serializePrincipalSearchResultList(s, schemas.SearchPrincipalsOutput_results, v.Results)
}
func (v *SearchPrincipalsOutput) Deserialize(d smithy.ShapeDeserializer) error {
	return smithy.ReadStruct(d, schemas.SearchPrincipalsOutput, func(s *smithy.Schema) error {
		switch s {
		case schemas.SearchPrincipalsOutput_nextToken:
			v.NextToken = new(string)
			return d.ReadString(schemas.SearchPrincipalsOutput_nextToken, v.NextToken)
		case schemas.SearchPrincipalsOutput_results:
			return deserializePrincipalSearchResultList(d, schemas.SearchPrincipalsOutput_results, &v.Results)
		}
		return nil
	})
}
func (c *Client) addOperationSearchPrincipalsMiddlewares(stack *middleware.Stack, options Options) (err error) {
	if err := stack.Serialize.Add(&serializeRequestMiddleware{options: &options, operationSchema: smithy.NewOperationSchema(schemas.SearchPrincipals, schemas.SearchPrincipalsInput, schemas.SearchPrincipalsOutput)}, middleware.After); err != nil {
		return err
	}
	if err := stack.Deserialize.Add(&deserializeResponseMiddleware{options: &options, operationSchema: smithy.NewOperationSchema(schemas.SearchPrincipals, schemas.SearchPrincipalsInput, schemas.SearchPrincipalsOutput), output: &SearchPrincipalsOutput{}}, middleware.After); err != nil {
		return err
	}

	if err = addResolveEndpointMiddleware(stack, options); err != nil {
		return err
	}
	if err = addComputePayloadSHA256(stack); err != nil {
		return err
	}
	if err = addRecordResponseTiming(stack, options); err != nil {
		return err
	}
	if err = addUserAgentFeatureProtocolRPCV2CBOR(stack, options); err != nil {
		return err
	}
	if err = addCredentialSource(stack, options); err != nil {
		return err
	}
	if err = addOpSearchPrincipalsValidationMiddleware(stack); err != nil {
		return err
	}
	if err = addRequestIDRetrieverMiddleware(stack); err != nil {
		return err
	}
	if err = addResponseErrorMiddleware(stack); err != nil {
		return err
	}
	if err = addRequestResponseLogging(stack, options); err != nil {
		return err
	}
	if err = addDisableHTTPSMiddleware(stack, options); err != nil {
		return err
	}
	if err = addInterceptors(stack, options); err != nil {
		return err
	}
	return nil
}

// SearchPrincipalsPaginatorOptions is the paginator options for SearchPrincipals
type SearchPrincipalsPaginatorOptions struct {
	// The maximum number of results to return. Defaults to 10. Valid only when
	// searchQuery is *; other searches reject this parameter and return at most 10
	// results.
	Limit int32

	// Set to true if pagination should stop if the service returns a pagination token
	// that matches the most recent token provided to the service.
	StopOnDuplicateToken bool
}

// SearchPrincipalsPaginator is a paginator for SearchPrincipals
type SearchPrincipalsPaginator struct {
	options   SearchPrincipalsPaginatorOptions
	client    SearchPrincipalsAPIClient
	params    *SearchPrincipalsInput
	nextToken *string
	firstPage bool
}

// NewSearchPrincipalsPaginator returns a new SearchPrincipalsPaginator
func NewSearchPrincipalsPaginator(client SearchPrincipalsAPIClient, params *SearchPrincipalsInput, optFns ...func(*SearchPrincipalsPaginatorOptions)) *SearchPrincipalsPaginator {
	if params == nil {
		params = &SearchPrincipalsInput{}
	}

	options := SearchPrincipalsPaginatorOptions{}
	if params.MaxResults != nil {
		options.Limit = *params.MaxResults
	}

	for _, fn := range optFns {
		fn(&options)
	}

	return &SearchPrincipalsPaginator{
		options:   options,
		client:    client,
		params:    params,
		firstPage: true,
		nextToken: params.NextToken,
	}
}

// HasMorePages returns a boolean indicating whether more pages are available
func (p *SearchPrincipalsPaginator) HasMorePages() bool {
	return p.firstPage || (p.nextToken != nil && len(*p.nextToken) != 0)
}

// NextPage retrieves the next SearchPrincipals page.
func (p *SearchPrincipalsPaginator) NextPage(ctx context.Context, optFns ...func(*Options)) (*SearchPrincipalsOutput, error) {
	if !p.HasMorePages() {
		return nil, fmt.Errorf("no more pages available")
	}

	params := *p.params
	params.NextToken = p.nextToken

	var limit *int32
	if p.options.Limit > 0 {
		limit = &p.options.Limit
	}
	params.MaxResults = limit

	optFns = append([]func(*Options){
		addIsPaginatorUserAgent,
	}, optFns...)
	result, err := p.client.SearchPrincipals(ctx, &params, optFns...)
	if err != nil {
		return nil, err
	}
	p.firstPage = false

	prevToken := p.nextToken
	p.nextToken = result.NextToken

	if p.options.StopOnDuplicateToken &&
		prevToken != nil &&
		p.nextToken != nil &&
		*prevToken == *p.nextToken {
		p.nextToken = nil
	}

	return result, nil
}

// SearchPrincipalsAPIClient is a client that implements the SearchPrincipals
// operation.
type SearchPrincipalsAPIClient interface {
	SearchPrincipals(context.Context, *SearchPrincipalsInput, ...func(*Options)) (*SearchPrincipalsOutput, error)
}

var _ SearchPrincipalsAPIClient = (*Client)(nil)
