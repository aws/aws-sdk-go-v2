// Code generated by smithy-go-codegen DO NOT EDIT.

package supportauthz

import (
	"context"
	"fmt"
	"github.com/aws/aws-sdk-go-v2/service/supportauthz/types"
	"github.com/aws/smithy-go/middleware"
	smithyhttp "github.com/aws/smithy-go/transport/http"
)

// Lists all support permits in the caller's account. Use pagination to ensure
// that the operation returns quickly and successfully.
func (c *Client) ListSupportPermits(ctx context.Context, params *ListSupportPermitsInput, optFns ...func(*Options)) (*ListSupportPermitsOutput, error) {
	if params == nil {
		params = &ListSupportPermitsInput{}
	}

	result, metadata, err := c.invokeOperation(ctx, "ListSupportPermits", params, optFns, c.addOperationListSupportPermitsMiddlewares)
	if err != nil {
		return nil, err
	}

	out := result.(*ListSupportPermitsOutput)
	out.ResultMetadata = metadata
	return out, nil
}

type ListSupportPermitsInput struct {

	// The maximum number of results to return in a single call. Valid range is 1 to
	// 100.
	MaxResults *int32

	// The token for the next page of results.
	NextToken *string

	// Filters the results by support permit status. Valid values: ACTIVE, INACTIVE,
	// DELETING.
	SupportPermitStatuses []types.SupportPermitStatus

	noSmithyDocumentSerde
}

type ListSupportPermitsOutput struct {

	// The list of support permits.
	//
	// This member is required.
	SupportPermits []types.SupportPermitSummary

	// The token for the next page of results, or null if there are no more results.
	NextToken *string

	// Metadata pertaining to the operation's result.
	ResultMetadata middleware.Metadata

	noSmithyDocumentSerde
}

func (c *Client) addOperationListSupportPermitsMiddlewares(stack *middleware.Stack, options Options) (err error) {
	err = stack.Serialize.Add(&awsRestjson1_serializeOpListSupportPermits{}, middleware.After)
	if err != nil {
		return err
	}
	err = stack.Deserialize.Add(&awsRestjson1_deserializeOpListSupportPermits{}, middleware.After)
	if err != nil {
		return err
	}

	if err = addlegacyEndpointContextSetter(stack, options); err != nil {
		return err
	}
	if err = addComputeContentLength(stack); err != nil {
		return err
	}
	if err = addResolveEndpointMiddleware(stack, options); err != nil {
		return err
	}
	if err = addComputePayloadSHA256(stack); err != nil {
		return err
	}
	if err = addRecordResponseTiming(stack); err != nil {
		return err
	}
	if err = smithyhttp.AddErrorCloseResponseBodyMiddleware(stack); err != nil {
		return err
	}
	if err = smithyhttp.AddCloseResponseBodyMiddleware(stack); err != nil {
		return err
	}
	if err = addCredentialSource(stack, options); err != nil {
		return err
	}
	if err = stack.Initialize.Add(newServiceMetadataMiddleware(options.Region, "ListSupportPermits"), middleware.Before); err != nil {
		return err
	}
	if err = addRequestIDRetrieverMiddleware(stack); err != nil {
		return err
	}
	if err = addResponseErrorMiddleware(stack); err != nil {
		return err
	}
	if err = addRequestResponseLogging(stack, options); err != nil {
		return err
	}
	if err = addDisableHTTPSMiddleware(stack, options); err != nil {
		return err
	}
	if err = addInterceptors(stack, options); err != nil {
		return err
	}
	return nil
}

// ListSupportPermitsPaginatorOptions is the paginator options for
// ListSupportPermits
type ListSupportPermitsPaginatorOptions struct {
	// The maximum number of results to return in a single call. Valid range is 1 to
	// 100.
	Limit int32

	// Set to true if pagination should stop if the service returns a pagination token
	// that matches the most recent token provided to the service.
	StopOnDuplicateToken bool
}

// ListSupportPermitsPaginator is a paginator for ListSupportPermits
type ListSupportPermitsPaginator struct {
	options   ListSupportPermitsPaginatorOptions
	client    ListSupportPermitsAPIClient
	params    *ListSupportPermitsInput
	nextToken *string
	firstPage bool
}

// NewListSupportPermitsPaginator returns a new ListSupportPermitsPaginator
func NewListSupportPermitsPaginator(client ListSupportPermitsAPIClient, params *ListSupportPermitsInput, optFns ...func(*ListSupportPermitsPaginatorOptions)) *ListSupportPermitsPaginator {
	if params == nil {
		params = &ListSupportPermitsInput{}
	}

	options := ListSupportPermitsPaginatorOptions{}
	if params.MaxResults != nil {
		options.Limit = *params.MaxResults
	}

	for _, fn := range optFns {
		fn(&options)
	}

	return &ListSupportPermitsPaginator{
		options:   options,
		client:    client,
		params:    params,
		firstPage: true,
		nextToken: params.NextToken,
	}
}

// HasMorePages returns a boolean indicating whether more pages are available
func (p *ListSupportPermitsPaginator) HasMorePages() bool {
	return p.firstPage || (p.nextToken != nil && len(*p.nextToken) != 0)
}

// NextPage retrieves the next ListSupportPermits page.
func (p *ListSupportPermitsPaginator) NextPage(ctx context.Context, optFns ...func(*Options)) (*ListSupportPermitsOutput, error) {
	if !p.HasMorePages() {
		return nil, fmt.Errorf("no more pages available")
	}

	params := *p.params
	params.NextToken = p.nextToken

	var limit *int32
	if p.options.Limit > 0 {
		limit = &p.options.Limit
	}
	params.MaxResults = limit

	optFns = append([]func(*Options){
		addIsPaginatorUserAgent,
	}, optFns...)
	result, err := p.client.ListSupportPermits(ctx, &params, optFns...)
	if err != nil {
		return nil, err
	}
	p.firstPage = false

	prevToken := p.nextToken
	p.nextToken = result.NextToken

	if p.options.StopOnDuplicateToken &&
		prevToken != nil &&
		p.nextToken != nil &&
		*prevToken == *p.nextToken {
		p.nextToken = nil
	}

	return result, nil
}

// ListSupportPermitsAPIClient is a client that implements the ListSupportPermits
// operation.
type ListSupportPermitsAPIClient interface {
	ListSupportPermits(context.Context, *ListSupportPermitsInput, ...func(*Options)) (*ListSupportPermitsOutput, error)
}

var _ ListSupportPermitsAPIClient = (*Client)(nil)
